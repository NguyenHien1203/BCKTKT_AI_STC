# PLAN.md — Kế hoạch triển khai theo từng Use Case

Trạng thái: `todo` (chưa làm) | `doing` (đang làm) | `done` (code xong) | `tested` (test pass, được coi là HOÀN THÀNH)

Nguyên tắc: chỉ chuyển UC tiếp theo sang `doing` khi UC trước đã `tested`. Thứ tự triển khai theo thứ tự phụ thuộc: nhóm I trước (nền tảng auth/tổ chức), sau đó II→VIII.


## Nhóm: QUẢN TRỊ HỆ THỐNG — service: `auth-identity-service`

| UC | Tên | Tác nhân | Status |
|---|---|---|---|
| UC-001 | Quản lý cơ cấu tổ chức | Quản trị hệ thống | done (code+test viết xong, chờ bạn chạy `pytest` xác nhận pass) |
| UC-002 | Quản lý người dùng (CRUD) | Quản trị hệ thống | done (code+test viết xong, đồng bộ Keycloak dùng NoOp stub — chờ bạn chạy `pytest` xác nhận pass) |
| UC-003 | Quản lý vòng đời người dùng | Quản trị hệ thống | tested (code+test viết xong: khoá/mở khoá, buộc đăng xuất, đồng bộ thủ công IdP, chuyển đơn vị có lưu lịch sử; đã fix fixture `sample_user` thiếu `external_id` khiến test lock/unlock sai; `pytest` đã chạy pass) |
| UC-004 | Quản lý quyền người dùng | Quản trị hệ thống | tested (code+test viết xong: xem/gán vai trò, cấu hình permitted_domains+unit, cấu hình mức nhạy cảm — permission_context tự khởi tạo mặc định lần đầu truy vấn; đã có giao diện `/permissions`; đã fix bug `configure_sensitivity` thiếu gán `self.sensitivity_level`; `pytest` đã chạy pass) |
| UC-005 | Quản lý vai trò người dùng | Quản trị hệ thống | done (code+test viết xong: CRUD vai trò, sửa lưu version mới, xoá kiểm tra ràng buộc còn user dùng; đã có giao diện `/roles`; `pytest` đã chạy pass) |
| UC-006 | Quản lý cấu hình hệ thống chung | Quản trị hệ thống | tested (code+test viết xong: xem cấu hình chung — tự khởi tạo mặc định lần đầu, sửa cấu hình lưu + áp dụng ngay không cần khởi động lại (đọc thẳng CSDL mỗi request); đã có giao diện `/system-config`; `pytest` đã chạy pass toàn bộ 97/97, gồm cả 2 fix phụ ở UC-03/UC-04) |
| UC-007 | Quản lý cấu hình tích hợp | Quản trị hệ thống | tested (backend `integration_config_router.py` đủ endpoint GET/PUT/recheck cho Keycloak + LGSP, dùng `NoOpConnectionChecker` stub — xem `connection_checker.py`; đã fix bug thiếu route `/integration-config` trong `App.jsx`; đã test trực tiếp trên UI: lưu cấu hình Keycloak, lưu cấu hình LGSP, kiểm tra lại (recheck) đều pass) |
| UC-008 | Quản lý cấu hình kênh thông báo | Quản trị hệ thống | done (backend đầy đủ: `notification_channel_router.py` — GET/PUT/test cho SMTP, SMS, Webhook/Slack; validate riêng theo loại kênh (`NotificationChannel.configure`); lưu tự động gửi thử qua `NoOpNotificationSender` stub — xem `infrastructure/notification_sender.py`, đổi sang implementation thật khi tích hợp SMTP/SMS/Slack thật; migration `0008_uc08_notification_channels.py`; đã có giao diện `/notification-channels` (3 card SMTP/SMS/Webhook) + route + mục nav; `pytest` đã chạy pass 139/139; `npm run build` frontend pass; **chưa chạy `alembic upgrade head` trên Postgres thật và chưa test qua UI** — cần bạn xác nhận trước khi chuyển `tested`) |
| UC-009 | Quản lý nhật ký truy cập và thao tác | Quản trị hệ thống, Kiểm toán viên | done (backend `audit_log_router.py`: GET/POST/export cho nhật ký append-only; xem toàn bộ + lọc theo tài khoản/thời gian; xuất báo cáo ATTT định kỳ PDF qua `reportlab` (`audit_report_generator.py`); migration `0009_uc09_audit_logs.py`; frontend `/audit-logs` (bộ lọc + bảng + nút xuất PDF) + route + mục nav; **chưa tự chạy được `pytest`/`npm run build` trong sandbox này do không có Internet để cài fastapi/sqlalchemy/npm packages — cần bạn chạy `pytest services/auth-identity-service -q` và `npm run build` để xác nhận trước khi chuyển `tested`**) |
| UC-010 | Quản trị AI Audit Log | Kiểm toán viên, AI Rà soát | done (backend `ai_audit_log_router.py`: GET/POST cho nhật ký AI query append-only + GET theo `trace_id` (toàn bộ chuỗi prompt/response/sources/permission_snapshot/model/prompt_version) + lọc theo `user_id`/thời gian; xuất báo cáo AI Audit định kỳ tuần/tháng PDF qua `reportlab` (`ai_audit_report_generator.py`); migration `0010_uc10_ai_audit_logs.py`; frontend `/ai-audit-logs` (bộ lọc + bảng + xem chi tiết trace_id + xuất PDF theo kỳ) + route + mục nav; **chưa tự chạy được `pytest`/`npm run build` trong sandbox này do không có Internet để cài fastapi/sqlalchemy/npm packages — cần bạn chạy `pytest services/auth-identity-service -q` và `npm run build` để xác nhận trước khi chuyển `tested`**) |
| UC-011 | Quản trị tài liệu hướng dẫn sử dụng | Quản trị hệ thống | tested (backend `guide_document_router.py`: POST/PUT/PATCH/DELETE/GET + `/versions` + `/download`; thêm tài liệu lưu tệp qua cổng `FileStorage` (`infrastructure/file_storage.py` — `MinioFileStorage` thật khi có `MINIO_ENDPOINT`, `LocalDiskFileStorage` cho dev/test); sửa tài liệu kèm tệp mới tự tăng `current_version` + ghi lịch sử `GuideDocumentVersion` (append-only); xoá tài liệu là xoá mềm (`is_active=False`) + có endpoint khôi phục; migration `0011_uc11_guide_documents.py` (bảng `guide_documents`, `guide_document_versions`, đã kiểm tra `alembic history` xâu chuỗi đúng 0010→0011); đã có giao diện `/guide-documents` (thêm/sửa/xoá/khôi phục/xem lịch sử phiên bản/tải tệp) + route + mục nav; `pytest services/auth-identity-service -q` đã chạy pass 187/187 (bao gồm 10 test mới `test_uc11_guide_document_api.py`); `npm run build` frontend đã chạy pass; **chưa chạy `alembic upgrade head` trên Postgres thật — cần bạn xác nhận**) |
| UC-012 | Đăng nhập / Đăng xuất hệ thống (SSO) | Tất cả người dùng | done (đăng nhập nội bộ username/password + session token, tạm thay cho SSO Keycloak thật — xem ADR-003 trong ARCHITECTURE.md; chờ chạy `pytest` xác nhận pass) |
| UC-013 | Đổi mật khẩu / Cấp lại mật khẩu | Tất cả người dùng, Quản trị hệ thống | done (backend `password_router.py` đã có sẵn từ trước: `/auth/change-password`, `/auth/forgot-password`, `/auth/reset-password`, `/users/{id}/reset-password` (admin); vừa bổ sung frontend: `api/password.js` (client gọi 4 endpoint); trang `/change-password` (tự đổi mật khẩu, đăng xuất lại sau khi đổi); trang `/forgot-password` (nhập username, luôn hiển thị thông điệp trung lập tránh dò quét tài khoản); trang `/reset-password` (đặt mật khẩu mới bằng token từ query `?token=`, khớp `reset_link_base` backend sinh ra); nút "Quên mật khẩu?" ở `LoginPage`; nút đổi mật khẩu ở topbar `AppLayout`; nút "Cấp lại mật khẩu tạm" cho admin trong `UsersPage` (gọi `admin_reset_password`, không hiển thị mật khẩu tạm trên UI); **chưa chạy `npm run build` để xác nhận, chưa viết test — cần bạn xác nhận trước khi chuyển `tested`**) |
| UC-014 | Quản lý phiên đăng nhập | Quản trị hệ thống | done (backend mới: mở rộng `SessionRepository` (`get_by_id`, `list_for_user`, `list_all`, `revoke_by_id`) + implement trong `SqlAlchemySessionRepository`; use case `manage_session.py` (`SessionManagementService`: liệt kê phiên toàn hệ thống/theo user kèm enrich username+full_name, thu hồi 1 phiên cụ thể); router mới `session_router.py`: `GET /sessions` (lọc `user_id`, `only_active`), `GET /users/{id}/sessions`, `DELETE /sessions/{id}`; khác UC-03 "buộc đăng xuất" (thu hồi toàn bộ phiên 1 lúc) — UC-14 cho thu hồi từng phiên riêng lẻ; đã thêm 2 file test `test_uc14_session_service.py` (unit, fake repo) + `test_uc14_session_api.py` (API qua TestClient) và bổ sung các phương thức mới vào `FakeSessionRepository` ở 2 file test cũ (`test_uc12_auth_service.py`, `test_uc03_user_lifecycle_service.py`) để không vỡ ABC; frontend: `api/sessions.js`, trang `/sessions` (`SessionsPage.jsx` — bộ lọc theo người dùng + trạng thái, bảng phiên kèm token rút gọn, nút thu hồi từng phiên) + route + mục nav + thẻ module ở trang chủ; **đã `py_compile` toàn bộ file Python thay đổi (pass), nhưng sandbox không có Internet để cài fastapi/sqlalchemy/npm packages — cần bạn chạy `pytest services/auth-identity-service -q` và `npm run build` để xác nhận trước khi chuyển `tested`**) |

## Nhóm: TIẾP NHẬN VÀ ĐỒNG BỘ DỮ LIỆU — service: `ingestion-service`

| UC | Tên | Tác nhân | Status |
|---|---|---|---|
| UC-015 | Đăng ký và quản lý nguồn dữ liệu | Quản trị Tích hợp | tested (backend `data_source_router.py`: POST đăng ký (validate `source_system` ∈ {TABMIS, QLVBDH, MISA, QL_GIA, PMSTT}, không trùng `code`), GET danh sách (lọc `only_active`, `source_system`), GET chi tiết, PATCH sửa nhà cung cấp/chủ sở hữu/mức nhạy cảm, POST activate/deactivate; migration `alembic/versions/0001_uc015_create_sources.py` (tạo schema `staging` + bảng `staging.sources`); đã fix `Dockerfile` thiếu `COPY alembic.ini`/`COPY alembic` khiến `alembic upgrade head` báo "No config file"; đã fix `alembic/env.py` dùng `version_table="alembic_version_ingestion"` riêng vì bảng `alembic_version` mặc định bị dùng chung với `auth-identity-service` trên cùng 1 Postgres database (gây lỗi "Can't locate revision"); frontend: `api/dataSources.js`, trang `/data-sources` (`pages/ingestion/DataSourcesPage.jsx` — form đăng ký/sửa, bảng lọc theo hệ thống nguồn + trạng thái, nút kích hoạt/vô hiệu hoá) + route + mục nav "Nguồn dữ liệu"; `pytest services/ingestion-service -q` đã chạy pass 8/8; `npm run build` frontend đã chạy pass; **đã sửa lỗi Dockerfile + version_table, nhưng chưa có xác nhận cuối cùng `docker compose exec ingestion-service alembic upgrade head` chạy thành công trên Postgres thật — cần bạn xác nhận trước khi chuyển sang trạng thái hoàn toàn ổn định**) |
| UC-016 | Quản lý thư viện bộ kết nối | Quản trị Tích hợp | done (backend `connector_router.py`: GET danh sách bộ kết nối (lọc `only_active`, `connector_type` ∈ {FILE, REST_API, JDBC, SOAP}), POST đăng ký plugin mới — không trùng `code`, mô phỏng bước "hệ thống nạp mô-đun + kiểm tra giao diện" qua `Connector.check_interface(entry_point)` (yêu cầu định dạng `package.module:ClassName`, trả lỗi 409 `CONNECTOR_INTERFACE_INVALID` nếu sai định dạng), GET chi tiết, PATCH `/connectors/{id}/version` cập nhật phiên bản — mô phỏng "hệ thống khởi động lại luân phiên tiến trình nhận sự kiện" bằng cách tăng `restart_count` mỗi lần đổi phiên bản, POST activate/deactivate; migration `alembic/versions/0002_uc016_create_connectors.py` (nối tiếp `0001`, tạo bảng `staging.connectors`); frontend: `api/connectors.js` (dùng chung `ingestionClient` với `dataSources.js`), trang `/connectors` (`pages/ingestion/ConnectorsPage.jsx` — form đăng ký plugin, bảng lọc theo loại + trạng thái, ô nhập + nút cập nhật phiên bản riêng từng dòng, nút kích hoạt/vô hiệu hoá) + route + mục nav "Thư viện bộ kết nối" (nhóm "Dữ liệu") + thẻ module ở trang chủ; đã `py_compile` toàn bộ file Python thay đổi (pass); **sandbox không có Internet để cài fastapi/sqlalchemy/npm packages — cần bạn chạy `pytest services/ingestion-service -q` (file test mới `tests/test_uc016_connector.py`) và `npm run build` để xác nhận trước khi chuyển `tested`, sau đó `docker compose exec ingestion-service alembic upgrade head` trên Postgres thật**) |
| UC-017 | Cấu hình kết nối nguồn (credentials/cert) | Quản trị Tích hợp, DBA | done<br>**Backend — router:**<br>• `app/interfaces/api/source_connection_router.py` (prefix `/source-connections`): `POST ""` cấu hình connection API/DB/File — validate `connection_type` ∈ {API, DB, FILE}, kiểm tra `data_source_id` tồn tại (404 `DATA_SOURCE_NOT_FOUND` nếu không), mã hoá `credentials` qua cổng `CredentialCrypto` trước khi lưu vào `encrypted_credentials`; `GET ""` (lọc `data_source_id`, `connection_type`, `only_active`) + `GET "/{id}"`; `PATCH "/{id}"` sửa `config`/`credentials` (mã hoá lại nếu có credentials mới); `POST "/{id}/test"` gọi `ConnectionTester.test()` rồi ghi `last_test_status`/`last_test_message`/`last_tested_at` qua `SourceConnection.record_test_result()`; `POST "/{id}/deactivate"` , `POST "/{id}/activate"`. Response `SourceConnectionResponse` **không có trường credentials nào** (đã kiểm tra bằng test).<br>• `app/interfaces/api/credential_asset_router.py` (prefix `/credential-assets`): `POST ""` đăng ký certificate/API key cho 1 `connection_id` (404 `SOURCE_CONNECTION_NOT_FOUND` nếu kết nối chưa tồn tại), mã hoá `secret_value` qua `CredentialCrypto` thành `encrypted_value`; `GET ""` (lọc `connection_id`, `asset_type`, `only_active`) + `GET "/{id}"`; `POST "/{id}/rotate"` gọi `CredentialAsset.rotate()` — đẩy bản ghi cũ (`rotated_at`, `previous_expires_at`) vào `rotation_history`, tăng `rotation_count`; `POST "/{id}/deactivate"`, `POST "/{id}/activate"`; `POST "/check-expiring?days_ahead=30"` quét toàn bộ asset `is_active=True` có `days_until_expiry(now) <= days_ahead`, gọi `CredentialAlertSender.send_expiry_alert()` cho từng cái, trả về danh sách `ExpiryAlertResult` (asset_id, connection_id, asset_type, expires_at, days_remaining, alert_sent, alert_message). Response `CredentialAssetResponse` **không có trường `encrypted_value`/bản rõ**.<br>**Domain** (`app/domain/entities.py`): entity `SourceConnection` (`CONNECTION_TYPES=(API,DB,FILE)`, `TEST_STATUSES=(UNTESTED,SUCCESS,FAILED)`, method `record_test_result()`) và `CredentialAsset` (`ASSET_TYPES=(CERTIFICATE,API_KEY)`, methods `rotate()`, `days_until_expiry(now)`, `is_expiring_within(days_ahead, now)`). `app/domain/exceptions.py` thêm `SourceConnectionNotFound`, `InvalidSourceConnection`, `CredentialAssetNotFound`, `InvalidCredentialAsset`. `app/domain/repositories.py` thêm interface `SourceConnectionRepository`, `CredentialAssetRepository` + 3 cổng (port) `CredentialCrypto` (encrypt/decrypt), `ConnectionTester` (test), `CredentialAlertSender` (send_expiry_alert).<br>**Application**: `app/application/use_cases/manage_source_connection.py` (`SourceConnectionService`: configure, update_config, get, list_connections, test_connection, activate/deactivate — tự mã hoá/giải mã credentials bằng `json.dumps`/`json.loads` + cổng crypto) và `manage_credential_asset.py` (`CredentialAssetService`: register, get, list_assets, rotate, activate/deactivate, `check_expiring(days_ahead)`).<br>**Infrastructure — DB**: `app/infrastructure/db/models.py` thêm `SourceConnectionModel` (bảng `source_connections`, FK `data_source_id → sources.id`) và `CredentialAssetModel` (bảng `credential_assets`, FK `connection_id → source_connections.id`, cột `rotation_history` lưu JSON dạng `Text`). `app/infrastructure/db/repository_impl.py` thêm `SqlAlchemySourceConnectionRepository`, `SqlAlchemyCredentialAssetRepository` (serialize/deserialize JSON cho `config`/`rotation_history`).<br>**Infrastructure — cổng/stub** (thay bằng implementation thật khi tích hợp, chỉ cần đổi factory trong router, không sửa domain/application):<br>• `app/infrastructure/credential_crypto.py` → `SimpleCredentialCrypto`: mã hoá **XOR + base64** dùng khoá từ biến môi trường `CREDENTIAL_ENCRYPTION_KEY` (có key mặc định `dev-only-insecure-...` CHỈ cho dev). **⚠️ CHƯA phải mã hoá bền vững cho production** — bắt buộc thay bằng `FernetCredentialCrypto` (`cryptography` + `Fernet.generate_key()` lưu KMS/Vault) hoặc tích hợp KMS/Vault Transit thật, và đặt `CREDENTIAL_ENCRYPTION_KEY` là khoá mạnh khác nhau theo môi trường trước khi deploy.<br>• `app/infrastructure/connection_tester.py` → `NoOpConnectionTester`: kiểm tra đủ trường bắt buộc theo loại kết nối (API cần `base_url`+`api_key`; DB cần `host`,`database`+`username`,`password`; FILE cần `path`) — KHÔNG gọi mạng/DB/file thật. Thay bằng `HttpConnectionTester`/`JdbcConnectionTester`/`FileConnectionTester` khi tích hợp thật.<br>• `app/infrastructure/alert_sender.py` → `NoOpAlertmanagerAlertSender`: chỉ log ra console, KHÔNG gọi Alertmanager thật. Thay bằng `AlertmanagerAlertSender` (POST `/api/v2/alerts`) khi tích hợp thật.<br>**Migration**: `alembic/versions/0003_uc017_create_source_connections.py` (nối tiếp `0002` → `down_revision="0002"`) tạo bảng `staging.source_connections` (FK `staging.sources.id`) và `staging.credential_assets` (FK `staging.source_connections.id`) kèm index trên `data_source_id`, `connection_type`, `connection_id`, `asset_type`, `expires_at`.<br>**Test**: `tests/test_uc017_source_connection.py` — 21 test case qua `TestClient` (SQLite in-memory): cấu hình connection + xác nhận credentials KHÔNG lộ ra response; 404 khi `data_source_id` không tồn tại; 422 khi `connection_type` sai; test kết nối SUCCESS (đủ field)/FAILED (thiếu credentials); 404 khi test connection không tồn tại; lọc danh sách theo loại; cập nhật config+credentials rồi test lại; activate/deactivate; đăng ký certificate/API key + xác nhận `encrypted_value`/bản rõ KHÔNG lộ; 404 khi đăng ký asset cho connection không tồn tại; rotate lưu đúng `rotation_history`/`rotation_count`; 404 khi rotate asset không tồn tại; `check-expiring` gửi cảnh báo đúng asset sắp hết hạn và bỏ qua asset còn xa hạn; asset đã `deactivate` bị loại khỏi `check-expiring`; lọc danh sách asset theo `connection_id`.<br>**Frontend**: `frontend/src/api/sourceConnections.js` (10 hàm gọi API, dùng chung `ingestionClient`); `frontend/src/pages/ingestion/SourceConnectionsPage.jsx` — form cấu hình connection (chọn nguồn dữ liệu, loại kết nối, nhập `config`/`credentials` dạng JSON tự do có validate cú pháp phía client), bảng danh sách kết nối kèm nút kiểm thử (hiển thị badge SUCCESS/FAILED/UNTESTED) + kích hoạt/vô hiệu hoá; form đăng ký + bảng certificate/API key (hiển thị ngày hết hạn, số lần luân chuyển) với ô nhập giá trị mới + ngày hết hạn mới + nút luân chuyển riêng từng dòng, ô nhập số ngày + nút "Quét & gửi cảnh báo" gọi `check-expiring`; cập nhật `App.jsx` (route `/source-connections` + thẻ module trang chủ) và `nav.js` (mục "Cấu hình kết nối nguồn" trong nhóm "Dữ liệu").<br>**Đã tự kiểm tra**: `python3 -m py_compile` toàn bộ file Python thay đổi (pass); kiểm tra cân bằng ngoặc `()[]{}`  các file JS/JSX mới (pass).<br>**⚠️ Còn tồn đọng — cần bạn xác nhận trước khi chuyển `tested`**: sandbox không có Internet nên chưa tự chạy được `pytest services/ingestion-service -q` và `npm run build`; chưa chạy `docker compose exec ingestion-service alembic upgrade head` trên Postgres thật; chưa đặt `CREDENTIAL_ENCRYPTION_KEY` production-grade (đang dùng key mặc định dev — **không được deploy nguyên trạng**). |
| UC-018 | Định nghĩa tập dữ liệu của nguồn | Quản trị Tích hợp | done (backend `dataset_router.py` (prefix `/datasets`) đủ 4 bước theo luồng nghiệp vụ: (1) `POST /datasets` định nghĩa tập dữ liệu + lược đồ (`schema_fields`: name/data_type/nullable/description, validate kiểu dữ liệu ∈ {STRING,INTEGER,BIGINT,DECIMAL,BOOLEAN,DATE,DATETIME,JSON}, không trùng `code` trong cùng `data_source_id`) + `PUT /datasets/{id}/schema` định nghĩa lại lược đồ (tự reset khoá chính/cột phân mảnh nếu trường bị xoá khỏi lược đồ mới); (2) `POST /datasets/{id}/partitioning` khai báo khoá chính (≥1 trường, phải thuộc lược đồ) + chiến lược phân mảnh (`NONE/RANGE/LIST/HASH`, bắt buộc `partition_column` nếu khác NONE); (3) `POST /datasets/{id}/critical-fields` khai báo trường bắt buộc (NOT NULL) — validate thuộc lược đồ, lưu kiểu thay thế toàn bộ (idempotent) + `GET .../critical-fields`; (4) `POST /datasets/{id}/schema-versions` đăng ký vào Schema Registry — bắt buộc đã khai báo khoá chính (bước 2) trước, mỗi lần đăng ký tăng `current_schema_version` + lưu snapshot lịch sử (`schema_fields`+`primary_key`+`partition_strategy`+`partition_column`+`critical_fields`), `GET .../schema-versions` (mới nhất trước) + `GET .../schema-versions/{version}`; có thêm activate/deactivate dataset. Domain: entity `Dataset`/`CriticalField`/`SchemaVersion` (`app/domain/entities.py`), exceptions `DatasetCodeAlreadyExists`/`DatasetNotFound`/`InvalidDataset`/`SchemaVersionNotFound`. Infrastructure: `DatasetModel`/`CriticalFieldModel`/`SchemaVersionModel` (bảng `dataset_catalog`/`critical_fields`/`dataset_schema_versions`) + `SqlAlchemy*Repository` tương ứng trong `app/infrastructure/db/models.py` + `repository_impl.py`; **lưu ý**: tài liệu BCKTKT gốc ghi lưu vào "metadata.dataset_catalog"/"metadata.critical_fields" nhưng theo ADR-001 đã có sẵn (1 schema Postgres/service — ingestion-service là `staging`), 3 bảng trên đặt tên đúng yêu cầu nghiệp vụ nhưng nằm trong schema `staging` (nhất quán với `sources`/`connectors`/`source_connections`), có ghi chú rõ trong code; migration `alembic/versions/0004_uc018_create_dataset_catalog.py` (nối tiếp `0003` → `down_revision="0003"`); frontend: `api/datasets.js` (10 hàm gọi API), trang `/datasets` (`pages/ingestion/DatasetsPage.jsx` — bố cục master-detail: cột trái danh sách dataset + form định nghĩa mới (bảng lược đồ động thêm/xoá dòng trường), cột phải chi tiết dataset đã chọn theo đúng 4 bước: xem lược đồ, chọn khoá chính (checkbox) + chọn chiến lược/cột phân mảnh, chọn trường bắt buộc (checkbox), đăng ký Schema Registry (nút bị khoá nếu chưa có khoá chính) + bảng lịch sử phiên bản) + route `/datasets` + thẻ module trang chủ + mục nav "Định nghĩa tập dữ liệu của nguồn" (nhóm "Dữ liệu"); **đã tự chạy `pytest services/ingestion-service -q` pass 44/44 (gồm 19 test mới `test_uc018_dataset.py`) và `npm run build` frontend pass trong sandbox này (cài fastapi/sqlalchemy/pydantic/pytest qua pypi.org + npm install thành công)**; **chưa chạy `docker compose exec ingestion-service alembic upgrade head` trên Postgres thật — cần bạn xác nhận trước khi chuyển `tested`**) |
| UC-019 | Cấu hình tác vụ điều phối | Quản trị Tích hợp | done (backend: entity `ScheduledTask` (`app/domain/entities.py`) — validate lịch cron (5 trường), `sync_mode` ∈ {FULL, INCREMENTAL} (đầy đủ/tăng dần), chính sách thử lại (`retry_max_attempts`, `retry_delay_seconds`, `retry_backoff` ∈ {NONE, FIXED, EXPONENTIAL}), bật/tắt (`enable`/`disable`), `record_run_status` (IDLE/RUNNING/SUCCESS/FAILED) để hệ thống cập nhật trạng thái; use case `app/application/use_cases/manage_scheduled_task.py` (`ScheduledTaskService`: configure — kiểm tra `dataset_id` tồn tại (404 `DATASET_NOT_FOUND`) + `code` không trùng (409 `SCHEDULED_TASK_CODE_EXISTS`), update_config, enable/disable, record_run_status, get/list); router mới `app/interfaces/api/scheduled_task_router.py` (prefix `/scheduled-tasks`): `POST ""` cấu hình tác vụ (lịch cron + đầy đủ/tăng dần + chính sách thử lại — hệ thống lưu), `GET ""` (lọc `dataset_id`, `only_enabled`) + `GET "/{id}"`, `PUT "/{id}"` sửa cấu hình, `POST "/{id}/enable"` + `POST "/{id}/disable"` (bật/tắt — hệ thống cập nhật trạng thái tác vụ điều phối), `POST "/{id}/status"` (hệ thống/Bộ điều phối cập nhật trạng thái thực thi sau 1 phiên, dùng bởi UC-025); domain: `app/domain/exceptions.py` thêm `ScheduledTaskCodeAlreadyExists`/`ScheduledTaskNotFound`/`InvalidScheduledTask`, `app/domain/repositories.py` thêm interface `ScheduledTaskRepository`; infrastructure: `app/infrastructure/db/models.py` thêm `ScheduledTaskModel` (bảng `scheduled_tasks`, FK `dataset_id → dataset_catalog.id`, unique `code`), `app/infrastructure/db/repository_impl.py` thêm `SqlAlchemyScheduledTaskRepository`; schema Pydantic mới trong `app/interfaces/api/schemas.py` (`ScheduledTaskCreate`, `ScheduledTaskConfigUpdate`, `ScheduledTaskRunStatusUpdate`, `ScheduledTaskResponse`); `app/main.py` đã `include_router(scheduled_task_router)`; migration `alembic/versions/0005_uc019_create_scheduled_tasks.py` (nối tiếp `0004` → `down_revision="0004"`, tạo bảng `staging.scheduled_tasks` + index `dataset_id`/`code`); test mới `tests/test_uc019_scheduled_task.py` (18 test case qua `TestClient`: cấu hình lưu đúng config FULL/INCREMENTAL, 404 khi dataset không tồn tại, 409 khi trùng mã, 422 khi cron sai định dạng, sửa cấu hình, bật/tắt, lọc `only_enabled`, hệ thống cập nhật trạng thái RUNNING/SUCCESS/FAILED, 404/422 các trường hợp lỗi); frontend: `frontend/src/api/scheduledTasks.js` (6 hàm gọi API, dùng chung `ingestionClient`), trang `/scheduled-tasks` (`frontend/src/pages/ingestion/ScheduledTasksPage.jsx` — form cấu hình (chọn tập dữ liệu từ danh sách UC-018, mã/tên, chế độ đồng bộ, lịch cron, chính sách thử lại), bảng danh sách lọc theo tập dữ liệu + trạng thái bật, hiển thị trạng thái thực thi (badge IDLE/RUNNING/SUCCESS/FAILED) + nút sửa/bật/tắt) + cập nhật `frontend/src/App.jsx` (import `ScheduledTasksPage`, icon `CalendarClock`, route `/scheduled-tasks`, thẻ module trang chủ) + `frontend/src/nav.js` (mục "Cấu hình tác vụ điều phối" trong nhóm "Dữ liệu"); đã tự kiểm tra `python3 -m py_compile` toàn bộ file Python thay đổi (pass) và kiểm tra cân bằng ngoặc `()[]{}` các file JS/JSX mới/sửa (pass); **sandbox không có Internet nên chưa tự chạy được `pytest services/ingestion-service -q` (file test mới `test_uc019_scheduled_task.py`) và `npm run build` — cần bạn chạy để xác nhận trước khi chuyển `tested`, sau đó `docker compose exec ingestion-service alembic upgrade head` trên Postgres thật**) |
| UC-020 | Xem lịch đầy đủ dữ liệu + lịch sử chạy | Quản trị Tích hợp, Phụ trách Dữ liệu | done (backend: entity `IngestionRun` (`app/domain/entities.py`) — 1 phiên ingest gắn `dataset_id` (bắt buộc) + `scheduled_task_id` (tuỳ chọn), `trigger` ∈ {MANUAL, SCHEDULED, RETRY}, `status` ∈ {RUNNING, SUCCESS, FAILED, PARTIAL}, `append_log()` ghi log theo dòng, `complete()` ghi trạng thái cuối + tổng kiểm soát (control totals: records_read/records_loaded/records_failed + `control_totals` JSON tự do); use case `app/application/use_cases/manage_ingestion_run.py` (`IngestionRunService`): `start_run`/`append_log`/`complete_run` (hạ tầng ghi `ingestion.runs`, dùng bởi UC-021 chạy lại phiên lỗi + UC-025 đồng bộ tăng dần — chưa làm), `list_run_history` (bước 1: hệ thống truy vấn `ingestion.runs`, lọc theo dataset/tác vụ/trạng thái/khoảng thời gian, mới nhất trước), `get_data_calendar` (bước 2: tổng hợp phiên theo từng ngày trong khoảng [date_from, date_to] để vẽ heatmap — ngày không có phiên SUCCESS nào được đánh dấu `is_missing=True`, tức kỳ thiếu dữ liệu), `get_run_detail` (bước 3: trả về log_entries + control_totals của 1 phiên); router mới `app/interfaces/api/ingestion_run_router.py` (prefix `/ingestion-runs`): `POST ""` bắt đầu phiên, `POST "/{id}/logs"` ghi log, `POST "/{id}/complete"` kết thúc phiên, `GET ""` lịch sử chạy (lọc `dataset_id`/`scheduled_task_id`/`status`/`date_from`/`date_to`), `GET "/calendar"` heatmap (`dataset_id` + `date_from`/`date_to` dạng YYYY-MM-DD), `GET "/{id}"` chi tiết phiên; domain: `app/domain/exceptions.py` thêm `IngestionRunNotFound`/`InvalidIngestionRun`, `app/domain/repositories.py` thêm interface `IngestionRunRepository`; infrastructure: `app/infrastructure/db/models.py` thêm `IngestionRunModel` (bảng `ingestion_runs`, FK `dataset_id → dataset_catalog.id` + `scheduled_task_id → scheduled_tasks.id`), `app/infrastructure/db/repository_impl.py` thêm `SqlAlchemyIngestionRunRepository`; schema Pydantic mới trong `app/interfaces/api/schemas.py` (`IngestionRunStart`, `IngestionRunLogAppend`, `IngestionRunComplete`, `IngestionRunResponse`, `IngestionRunListItemResponse`, `CalendarDayResponse`); `app/main.py` đã `include_router(ingestion_run_router)`; migration `alembic/versions/0006_uc020_create_ingestion_runs.py` (nối tiếp `0005` → `down_revision="0005"`, tạo bảng `staging.ingestion_runs` — tên bảng theo yêu cầu nghiệp vụ "ingestion.runs", đặt trong schema `staging` nhất quán ADR-001 như các bảng khác của service); đã kiểm tra `alembic history` xâu chuỗi đúng 0001→0006; test mới `tests/test_uc020_ingestion_run.py` (24 test case qua `TestClient`: bắt đầu/ghi log/kết thúc phiên, 404 khi dataset/phiên không tồn tại, 422 khi status kết thúc không hợp lệ, lọc lịch sử theo dataset/trạng thái/tác vụ điều phối + 404 khi dataset lọc không tồn tại, sắp xếp mới nhất trước, heatmap đánh dấu đúng ngày thiếu dữ liệu/ngày có SUCCESS/ngày chỉ có FAILED vẫn tính là thiếu dữ liệu, 404/409 các trường hợp lỗi heatmap, xem chi tiết phiên đủ log + control totals); `pytest services/ingestion-service -q` đã chạy pass toàn bộ 78/78 (gồm 24 test mới); frontend: `frontend/src/api/ingestionRuns.js` (7 hàm gọi API dùng chung `ingestionClient`), trang `/ingestion-runs` (`frontend/src/pages/ingestion/IngestionRunsPage.jsx` — 2 tab: "Lịch sử chạy" (bảng + bộ lọc dataset/trạng thái/khoảng ngày) và "Lịch đầy đủ dữ liệu" (heatmap dạng lưới theo ngày, màu xanh/vàng/đỏ tương ứng đầy đủ/một phần/thiếu dữ liệu, click 1 ô để nhảy sang tab lịch sử lọc đúng ngày đó); click "Xem chi tiết" mở modal hiển thị log (dạng dòng lệnh) + bảng tổng kiểm soát (control totals)) + cập nhật `frontend/src/App.jsx` (import trang, route `/ingestion-runs`, thẻ module trang chủ) + `frontend/src/nav.js` (mục "Lịch đầy đủ dữ liệu + Lịch sử chạy" trong nhóm "Dữ liệu"); đã tự chạy `npm run build` pass; **chưa chạy `docker compose exec ingestion-service alembic upgrade head` trên Postgres thật — cần bạn xác nhận trước khi chuyển `tested`**) |
| UC-021 | Chạy lại phiên ingest lỗi | Quản trị Tích hợp | tested (đã xác nhận `pytest services/ingestion-service -q` pass (gồm 10 test mới `test_uc021_retry_ingestion_run.py`), `npm run build` frontend pass, và `docker compose exec ingestion-service alembic upgrade head` áp migration `0007` thành công trên Postgres thật. Tái sử dụng hạ tầng `ingestion.runs` của UC-020, không tạo bảng/repository mới ngoài 1 cột bổ sung. Backend: domain `app/domain/entities.py` — entity `IngestionRun` thêm field `retry_of_run_id` (Optional[int]), validate `trigger` phải là `RETRY` khi có giá trị này; `app/domain/exceptions.py` thêm `IngestionRunNotFailed` (409, chỉ được chạy lại phiên FAILED) và `IngestionRunRetryInProgress` (409, khoá chống trùng); `app/domain/repositories.py` thêm `IngestionRunRepository.find_active_retry(run_id)` (tìm phiên RETRY đang RUNNING của cùng phiên gốc — dùng làm khoá chống trùng) + `list_retries(run_id)` (lịch sử chạy lại), và port mới `IngestionRetryExecutor` (đại diện Bộ điều phối). Infrastructure: `app/infrastructure/retry_executor.py` (mới) — `NoOpIngestionRetryExecutor`: mô phỏng Bộ điều phối chạy lại thành công ngay (dùng lại `records_read` phiên gốc) — thay bằng implementation gọi Bộ điều phối thật (API/queue) khi tích hợp, chỉ cần đổi factory ở router; `app/infrastructure/db/models.py` thêm cột `retry_of_run_id` (FK `ingestion_runs.id`) vào `IngestionRunModel`; `app/infrastructure/db/repository_impl.py` cập nhật `_ingestion_run_to_entity`/`add()` để lưu/đọc `retry_of_run_id`, thêm `find_active_retry()`/`list_retries()`. Application: `app/application/use_cases/retry_ingestion_run.py` (mới) — `RetryIngestionRunService`: bước 1 `get_failure_reason(run_id)` (trả về nguyên nhân: error_message + các dòng log mức ERROR + cờ `retryable`), bước 2-4 `retry_run(run_id)` (kiểm tra phiên gốc đang FAILED → kiểm tra khoá chống trùng qua `find_active_retry` → tạo phiên mới `trigger=RETRY`, `retry_of_run_id=<phiên gốc>`, ghi log "Bộ điều phối kích hoạt chạy lại phiên #X" → gọi `IngestionRetryExecutor.execute_retry()` → `IngestionRun.complete()` ghi trạng thái cuối + tổng kiểm soát vào `ingestion.runs`), `list_retries(run_id)`. Interface: `app/interfaces/api/schemas.py` thêm `retry_of_run_id` vào `IngestionRunResponse`/`IngestionRunListItemResponse` + schema mới `IngestionRunFailureReasonResponse`; `app/interfaces/api/ingestion_run_router.py` thêm `GET /ingestion-runs/{id}/failure-reason` (bước 1), `POST /ingestion-runs/{id}/retry` (bước 2-4, trả về phiên RETRY mới, 404 nếu không tìm thấy phiên gốc, 409 nếu phiên gốc chưa FAILED hoặc đang có lượt chạy lại khác RUNNING), `GET /ingestion-runs/{id}/retries` (lịch sử chạy lại); không cần sửa `app/main.py` (dùng chung router UC-020 đã được include). Migration: `alembic/versions/0007_uc021_add_retry_of_run_id.py` (mới, nối tiếp `0006` → `down_revision="0006"`) — `ALTER TABLE staging.ingestion_runs ADD COLUMN retry_of_run_id` + FK tự tham chiếu + index. Test: `tests/test_uc021_retry_ingestion_run.py` (mới, 10 test case qua `TestClient`): xem nguyên nhân lỗi (200 + `retryable=true`) và 404 khi phiên không tồn tại; chạy lại thành công (tạo phiên mới `trigger=RETRY` gắn đúng `retry_of_run_id`, ghi log kích hoạt, trạng thái cuối được cập nhật); 404 khi phiên gốc không tồn tại; 409 `INGESTION_RUN_NOT_FAILED` khi phiên gốc chưa FAILED (còn RUNNING); có thể chạy lại tiếp sau khi lượt chạy lại trước đã hoàn tất; khoá chống trùng 409 `INGESTION_RUN_RETRY_IN_PROGRESS` khi đang có 1 phiên RETRY khác RUNNING cho cùng phiên gốc (test ghi thẳng vào repository để mô phỏng đúng thời điểm Bộ điều phối thật chưa trả kết quả) + `failure-reason` trả `retryable=false` lúc đó; xem lịch sử chạy lại mới nhất trước + 404 khi phiên gốc không tồn tại; phiên gốc vẫn còn trong lịch sử chạy (`GET /ingestion-runs`) sau khi đã chạy lại. Frontend: `frontend/src/api/ingestionRuns.js` thêm 3 hàm `getFailureReason`, `retryIngestionRun`, `listRetries` (dùng chung `ingestionClient`); `frontend/src/pages/ingestion/IngestionRunsPage.jsx` (sửa, không thêm route/trang mới — gắn thẳng vào tab "Lịch sử chạy" đã có ở UC-020 theo đúng luồng "Chọn phiên bị lỗi"): nút "Chạy lại" ngay trên dòng bảng có `status=FAILED`; trong modal "Xem chi tiết" thêm khối "UC-021 — Nguyên nhân lỗi" (hiển thị `error_message` + các dòng log ERROR, nút "Chạy lại phiên này" nếu `retryable=true`, thông báo khoá chống trùng nếu đang có lượt chạy lại khác chạy) và bảng "Lịch sử chạy lại" (danh sách các phiên RETRY của phiên gốc, xem lại phiên gốc từ 1 phiên RETRY qua `retry_of_run_id`); cập nhật subtitle trang. Không cần sửa `App.jsx`/`nav.js`. **Đã tự kiểm tra**: `python3 -m py_compile` toàn bộ file Python thay đổi (pass); kiểm tra cân bằng ngoặc `()[]{}` cho `IngestionRunsPage.jsx` (pass, 235/235/265/265/41/41). đã kiểm tra `down_revision` xâu chuỗi đúng `0006`→`0007`) |
| UC-022 | Tiếp nhận file thủ công TABMIS (upload) | Cán bộ nộp file | code hoàn chỉnh (backend `manage_tabmis_intake.py` + `tabmis_intake_router.py` + migration `0008_uc022_create_tabmis_intake_sessions.py`; frontend `api/tabmisIntake.js` + `pages/ingestion/TabmisIntakePage.jsx` + route `/tabmis-intake`); **chưa chạy lại `pytest`/`npm run build` trong môi trường này (không có mạng để cài `fastapi`/`sqlalchemy`/node_modules) — cần bạn tự chạy để xác nhận** |
| UC-023 | Xem trạng thái + sửa lỗi intake TABMIS | Cán bộ nộp file | code hoàn chỉnh (backend đã có đủ 3 bước: `GET /tabmis-intake/{id}/status`, `GET /tabmis-intake/{id}/row-errors`, `POST /tabmis-intake/{id}/reupload`, migration `0009_uc023_create_tabmis_intake_error.py`, test `tests/test_uc023_tabmis_intake_error.py`; frontend `pages/ingestion/TabmisIntakeDetailPage.jsx` đã có đủ 3 bước + sơ đồ máy trạng thái, nhưng **thiếu route `/tabmis-intake/:id` và import trong `App.jsx`** — đã bổ sung route này trong phiên làm việc hiện tại; **chưa chạy lại `pytest`/`npm run build` trong môi trường này (không có mạng) — cần bạn tự chạy để xác nhận**) |
| UC-024 | Tiếp nhận thủ công văn bản từ QLVBĐH (upload định kỳ) | Cán bộ nộp văn bản | code hoàn chỉnh (backend: entity `VanBanIntake` + bảng `staging.stg_van_ban` (`models.py`/migration `0010_uc024_create_stg_van_ban.py`), `manage_van_ban_intake.py` (nhập metadata + tệp PDF trong 1 lần nộp, khử trùng lặp theo `so_ky_hieu` trước khi lưu, publish sự kiện `ocr.requested`), `van_ban_intake_router.py` (`POST/GET /qlvbdh-intake/documents`), cổng `EventPublisher` mới (`infrastructure/event_publisher.py`: `LoggingEventPublisher` cho dev/test, `RabbitMqEventPublisher` cho thật — thêm `pika` vào `requirements.txt`), `get_document_file_storage()` (bucket `raw-documents`, tách khỏi bucket `tabmis-intake`), test `tests/test_uc024_van_ban_intake.py` (8 case); frontend: `api/vanBanIntake.js`, trang `/qlvbdh-intake` (`pages/ingestion/VanBanIntakePage.jsx` — form nộp văn bản + bảng lịch sử), route + mục nav "Tiếp nhận văn bản QLVBĐH" + thẻ ở trang Tổng quan; **chưa chạy lại `pytest`/`npm run build` trong môi trường này (không có mạng để cài `fastapi`/`sqlalchemy`/`pika`/node_modules) — cần bạn tự chạy để xác nhận, đặc biệt migration `0010` trên Postgres thật**) |
| UC-025 | Đồng bộ tăng dần từ API/DB | Hệ thống tự động (Bộ điều phối) | done (không tạo bảng mới — tái sử dụng hạ tầng `ingestion.runs` UC-020 (điểm kiểm tra = `control_totals.last_synced_updated_at` của phiên `INCREMENTAL`+`SUCCESS` gần nhất), cùng tinh thần UC-021. Domain (`app/domain/entities.py`): `IngestionRun` đã có sẵn `sync_mode`; thêm exceptions `IncrementalSyncSourceSystemNotSupported`, `IncrementalSyncConnectionNotConfigured`, `IncrementalSyncAlreadyRunning` (`app/domain/exceptions.py`); thêm interface cổng `IncrementalSourceConnector` (`fetch_changes(connection, credentials, since)`) trong `app/domain/repositories.py`. Application: `app/application/use_cases/sync_incremental.py` (`IncrementalSyncService`): bước 1 `get_checkpoint(dataset_id)` đọc điểm kiểm tra từ `ingestion.runs`; `run_sync(dataset_id, scheduled_task_id=None, trigger="SCHEDULED")` — chỉ áp dụng `data_source.source_system` ∈ {MISA, QL_GIA, PMSTT} (404/409 nếu không hỗ trợ), khoá chống trùng (409 nếu đang có phiên INCREMENTAL RUNNING), tìm 1 `SourceConnection` API/DB đang active (409 nếu chưa cấu hình), bước 2 gọi cổng `IncrementalSourceConnector.fetch_changes` lấy bản ghi mới/đổi theo checkpoint, bước 3 lưu raw JSON vào MinIO qua `FileStorage` (chỉ khi có bản ghi) + cập nhật checkpoint mới vào `control_totals` khi `IngestionRun.complete()`, bước 4 đẩy sự kiện `parsing.requested` qua `EventPublisher` (chỉ khi có bản ghi mới) cho `data-quality-service` UC-029. Infrastructure: `app/infrastructure/incremental_connector.py` — `NoOpIncrementalSourceConnector` (mô phỏng bộ kết nối, không gọi mạng thật; thay bằng `HttpIncrementalSourceConnector`/`JdbcIncrementalSourceConnector` khi tích hợp MISA/QL Giá/PMSTT thật). Interface: `app/interfaces/api/incremental_sync_router.py` (prefix `/incremental-sync`): `GET "/{dataset_id}/checkpoint"` (bước 1), `POST "/{dataset_id}/run"` (kích hoạt phiên đồng bộ, body `scheduled_task_id`+`trigger` — dùng bởi `scheduled-tasks` UC-019 khi tác vụ đến hạn, hoặc gọi thủ công `trigger=MANUAL`); schema mới trong `schemas.py` (`IncrementalSyncCheckpointResponse`, `IncrementalSyncRunRequest`); đã `include_router` trong `app/main.py`. Không cần migration mới (không thêm bảng/cột). Test: `tests/test_uc025_incremental_sync.py` — bao phủ: xem checkpoint chưa từng đồng bộ (null) và sau khi đã có phiên SUCCESS; chạy đồng bộ lần đầu (checkpoint null) lưu MinIO + đẩy sự kiện; chạy lần 2 dùng đúng checkpoint từ lần trước; không có bản ghi mới → không lưu MinIO, không đẩy sự kiện; 404 dataset/data source không tồn tại; 409 khi `source_system` không thuộc {MISA,QL_GIA,PMSTT}; 409 khi chưa cấu hình `SourceConnection` API/DB active; 409 khoá chống trùng khi đang có phiên INCREMENTAL RUNNING; bộ kết nối lỗi → phiên `FAILED` + giữ nguyên checkpoint cũ trong `control_totals`. Frontend: `frontend/src/api/incrementalSync.js` (2 hàm: `getIncrementalSyncCheckpoint`, `runIncrementalSync`, dùng chung `ingestionClient`); trang `/incremental-sync` (`frontend/src/pages/ingestion/IncrementalSyncPage.jsx` — chọn nguồn dữ liệu (lọc đúng 3 hệ thống hỗ trợ) rồi chọn tập dữ liệu, xem điểm kiểm tra hiện tại, nút "Chạy đồng bộ tăng dần" (trigger=MANUAL) + hiển thị kết quả phiên vừa chạy, bảng lịch sử các phiên INCREMENTAL của dataset tái dùng `listRunHistory` UC-020); vừa bổ sung trong phiên làm việc này: import `IncrementalSyncPage` + `RefreshCw` icon, route `/incremental-sync`, thẻ module ở trang chủ (`App.jsx`), mục nav "Đồng bộ tăng dần từ API/DB" trong nhóm "Dữ liệu" (`nav.js`) — trước đó code đã viết xong nhưng thiếu 3 chỗ wiring này. **Đã tự kiểm tra**: `python3 -m py_compile` toàn bộ file Python thay đổi trong service (pass); kiểm tra cân bằng ngoặc `()[]{}` cho `App.jsx`/`nav.js` sau khi sửa (pass). **Chưa chạy được**: sandbox không có Internet để cài `fastapi`/`sqlalchemy`/npm packages — cần bạn chạy `pytest services/ingestion-service -q` (đặc biệt `test_uc025_incremental_sync.py`) và `npm run build` frontend để xác nhận trước khi chuyển `tested`; không cần chạy alembic (không có migration mới cho UC-025)) |
| UC-026 | Kiểm tra Schema Registry | Hệ thống tự động | done (không tạo bảng "lược đồ đã đăng ký" mới — tái sử dụng hạ tầng Schema Registry có sẵn từ UC-018 (`dataset_schema_versions` qua `SchemaVersionRepository`, đối chiếu với `schema_snapshot.schema_fields` của phiên bản mới nhất); chỉ thêm 1 bảng lịch sử `schema_registry_checks` để lưu lại từng lượt kiểm tra. Domain (`app/domain/entities.py`): entity `SchemaRegistryCheck` (`status` ∈ {COMPATIBLE, BREAKING}, property `allowed` = `status=="COMPATIBLE"`); `app/domain/exceptions.py` thêm `SchemaNotRegisteredForCheck` (409, dataset chưa đăng ký lược đồ nào), `SchemaRegistryCheckNotFound` (404), `InvalidSchemaRegistryCheck` (422, vd `incoming_fields` rỗng); `app/domain/repositories.py` thêm interface `SchemaRegistryCheckRepository`. Application: `app/application/use_cases/check_schema_registry.py` (`SchemaRegistryCheckService`): bước 1 `check_schema(dataset_id, incoming_fields, ingestion_run_id=None)` — lấy `SchemaVersion` mới nhất đã đăng ký (404 dataset không tồn tại, 409 nếu chưa từng đăng ký), so sánh tên+kiểu dữ liệu trường giữa `incoming_fields` và lược đồ đã đăng ký: mất trường đã đăng ký hoặc đổi kiểu dữ liệu 1 trường đã có -> `BREAKING` (bước 2: hệ thống DỪNG — trả `allowed=false` + phát sự kiện `schema_registry.compatibility_broken` qua `EventPublisher` để cảnh báo Quản trị Tích hợp); chỉ thêm trường mới (hoặc giống hệt) -> `COMPATIBLE` (bước 3: `allowed=true`, ghi nhận `added_fields`, không phát sự kiện); mọi lượt kiểm tra đều lưu vào `schema_registry_checks` qua `SchemaRegistryCheckRepository`; `get_check`/`list_checks` (lọc `status`, mới nhất trước) xem lịch sử. Infrastructure: `app/infrastructure/db/models.py` thêm `SchemaRegistryCheckModel` (bảng `schema_registry_checks`, FK `dataset_id → dataset_catalog.id`, FK tuỳ chọn `ingestion_run_id → ingestion_runs.id`), `app/infrastructure/db/repository_impl.py` thêm `SqlAlchemySchemaRegistryCheckRepository`; dùng lại `EventPublisher`/`get_event_publisher()` có sẵn từ UC-024/UC-025 (không cần cổng cảnh báo mới). Interface: `app/interfaces/api/schema_registry_router.py` (prefix `/schema-registry`): `POST "/{dataset_id}/check"` (bước 1-3, trả `SchemaRegistryCheckResponse` gồm `status`/`allowed`/`added_fields`/`removed_fields`/`changed_type_fields`/`message`), `GET "/{dataset_id}/checks"` (lọc `status`), `GET "/checks/{id}"`; schema Pydantic mới trong `schemas.py` (`SchemaFieldInput`, `SchemaRegistryCheckRequest`, `SchemaChangedTypeField`, `SchemaRegistryCheckResponse`); đã `include_router` trong `app/main.py`. Migration: `alembic/versions/0011_uc026_create_schema_registry_checks.py` (nối tiếp `0010` → `down_revision="0010"`, tạo bảng `staging.schema_registry_checks` + index `dataset_id`/`status`/`ingestion_run_id`). Test: `tests/test_uc026_schema_registry_check.py` (16 test case qua `TestClient`): 409 khi chưa đăng ký lược đồ, 404 dataset không tồn tại, 422 `incoming_fields` rỗng; tương thích khi chỉ thêm trường/giống hệt (không phát sự kiện cảnh báo); phá vỡ tương thích khi mất trường/đổi kiểu dữ liệu (có phát đúng 1 sự kiện `schema_registry.compatibility_broken` với đúng payload); lịch sử mới nhất trước + lọc theo status + 404 dataset không tồn tại; xem chi tiết theo id + 404; lưu đúng `ingestion_run_id` khi truyền vào. Frontend: `frontend/src/api/schemaRegistry.js` (3 hàm: `checkSchemaRegistry`, `listSchemaRegistryChecks`, `getSchemaRegistryCheck`, dùng chung `ingestionClient`); trang `/schema-registry-checks` (`frontend/src/pages/ingestion/SchemaRegistryChecksPage.jsx` — chọn tập dữ liệu, bảng lược đồ nguồn có thể chỉnh sửa (nạp sẵn từ lược đồ hiện tại của dataset, mô phỏng lược đồ đọc được trước khi phân tích) theo đúng cấu trúc bảng lược đồ ở `DatasetsPage.jsx` (UC-018), nút \"So sánh với lược đồ đã đăng ký\", hiển thị kết quả (badge Tương thích/Phá vỡ tương thích + trường thêm/mất/đổi kiểu), bảng lịch sử kiểm tra lọc theo trạng thái) + import + route `/schema-registry-checks` + thẻ module trang chủ (`App.jsx`) + mục nav \"Kiểm tra Schema Registry\" trong nhóm \"Dữ liệu\" (`nav.js`). **Đã tự kiểm tra**: `python3 -m py_compile` toàn bộ file Python thay đổi trong service (pass); kiểm tra cân bằng ngoặc `()[]{}` cho `App.jsx`/`nav.js`/`api/schemaRegistry.js`/`SchemaRegistryChecksPage.jsx` (pass). **Chưa chạy được**: sandbox không có Internet để cài `fastapi`/`sqlalchemy`/npm packages — cần bạn chạy `pytest services/ingestion-service -q` (đặc biệt `test_uc026_schema_registry_check.py`) và `npm run build` frontend, sau đó `docker compose exec ingestion-service alembic upgrade head` (áp migration `0011`) trên Postgres thật, để xác nhận trước khi chuyển `tested`) |
| UC-027 | Đối soát phiên intake | Quản trị Tích hợp, Phụ trách Dữ liệu | done (không tạo lại hạ tầng "phiên tiếp nhận" — tái sử dụng `TabmisIntakeSessionRepository` (UC-022/023) để lấy `control_totals` của `TabmisIntakeSession` làm tổng kiểm soát hiển thị; chỉ thêm 1 bảng `intake_reconciliations` để lưu từng lượt đối soát + phát hiện thiếu/sai. Domain (`app/domain/entities.py`): entity `IntakeReconciliation` (`status` ∈ {OPEN, CLOSED}, `findings`: danh sách dict `{finding_type ∈ {MISSING, INCORRECT}, field_name, description, status ∈ {OPEN, RESOLVED}, recorded_at, resolved_at}`); method `mark_finding()` (bước 3-4, chỉ cho phép khi `is_open`), `resolve_finding()` (đánh dấu 1 phát hiện đã xử lý xong), `close()` (bước 5-6, chỉ cho phép đóng khi `open_finding_count()==0` — điều kiện "đạt yêu cầu"); `app/domain/exceptions.py` thêm `IntakeReconciliationNotFound` (404), `InvalidIntakeReconciliation` (422), `IntakeReconciliationAlreadyClosed` (422), `IntakeReconciliationHasUnresolvedFindings` (422, đóng khi còn phát hiện OPEN), `IntakeReconciliationFindingNotFound` (404, index phát hiện không hợp lệ); `app/domain/repositories.py` thêm interface `IntakeReconciliationRepository` (`add`/`update`/`get_by_id`/`find_open_for_session`/`list`). Application: `app/application/use_cases/manage_intake_reconciliation.py` (`IntakeReconciliationService`): bước 1-2 `open_or_get(session_id, reconciled_by)` — 404 nếu phiên tiếp nhận không tồn tại (tái dùng `TabmisIntakeSessionRepository`), nếu phiên đã có 1 lượt đối soát đang mở thì trả về lượt đó (không tạo trùng, qua `find_open_for_session`), ngược lại mở mới + snapshot `control_totals` của phiên tiếp nhận; bước 3-4 `mark_finding()` — 404 nếu phiên đối soát không tồn tại, 422 nếu đã đóng hoặc dữ liệu không hợp lệ, lưu ngay qua repository; `resolve_finding()` — đánh dấu 1 phát hiện đã xử lý xong (404 nếu index không hợp lệ); bước 5-6 `close()` — 404 nếu không tồn tại, 422 nếu đã đóng hoặc còn phát hiện OPEN (đúng ngữ nghĩa "đạt yêu cầu"), cập nhật `status: OPEN → CLOSED` + `closed_by`/`closed_at`/`close_note`; `get`/`list_reconciliations` xem lại lịch sử. Infrastructure: `app/infrastructure/db/models.py` thêm `IntakeReconciliationModel` (bảng `intake_reconciliations`, FK `session_id → tabmis_intake_sessions.id`), `app/infrastructure/db/repository_impl.py` thêm `SqlAlchemyIntakeReconciliationRepository`. Interface: `app/interfaces/api/intake_reconciliation_router.py` (prefix `/intake-reconciliations`): `POST ""` (bước 1-2, mở/tái sử dụng phiên đối soát), `GET ""` (lọc `session_id`/`status`), `GET "/{id}"`, `POST "/{id}/findings"` (bước 3-4), `POST "/{id}/findings/{index}/resolve"`, `POST "/{id}/close"` (bước 5-6); schema Pydantic mới trong `schemas.py` (`IntakeReconciliationOpenRequest`, `IntakeReconciliationFindingRequest`, `IntakeReconciliationCloseRequest`, `IntakeReconciliationFindingResponse`, `IntakeReconciliationResponse`); đã `include_router` trong `app/main.py`. Migration: `alembic/versions/0012_uc027_create_intake_reconciliations.py` (nối tiếp `0011` → `down_revision="0011"`, tạo bảng `staging.intake_reconciliations` + index `session_id`/`status`). Test: `tests/test_uc027_intake_reconciliation.py` (15 test case qua `TestClient`, **đã chạy pass**): mở đối soát trả về snapshot tổng kiểm soát; mở lại cùng phiên tái sử dụng lượt đang mở (không tạo trùng); 404 phiên tiếp nhận không tồn tại; 422 `reconciled_by` rỗng; đánh dấu phát hiện MISSING/INCORRECT được lưu (truy vấn lại vẫn còn); 404 phiên đối soát không tồn tại; 422 `finding_type` không hợp lệ; 422 khi đã đóng vẫn đánh dấu; đóng thành công khi không còn phát hiện; 422 khi còn phát hiện chưa xử lý (đúng ngữ nghĩa "đạt yêu cầu"); đóng thành công sau khi xử lý hết phát hiện; 404 resolve index không hợp lệ; 422 đóng khi đã đóng trước đó; 404 đóng khi không tồn tại; danh sách lọc theo `session_id`/`status`. **Đã tự kiểm tra và chạy được** (sandbox có Internet nội bộ tới PyPI): `pip install -r requirements.txt` + `pytest -q` toàn bộ service **158 passed** (không phá vỡ UC-015..026 hiện có); kiểm tra chuỗi migration Alembic bằng `ScriptDirectory.walk_revisions()` — head đúng `0012 → 0011 → ... → 0001`. Frontend: `frontend/src/api/intakeReconciliation.js` (6 hàm: `openIntakeReconciliation`, `listIntakeReconciliations`, `getIntakeReconciliation`, `markIntakeReconciliationFinding`, `resolveIntakeReconciliationFinding`, `closeIntakeReconciliation`, dùng chung `ingestionClient`); trang `/intake-reconciliation` (`frontend/src/pages/ingestion/IntakeReconciliationPage.jsx` — bước 1 chọn phiên tiếp nhận TABMIS + người thực hiện đối soát, bước 2 bảng tổng kiểm soát snapshot, bước 3-4 form đánh dấu phát hiện thiếu/sai + bảng danh sách phát hiện (nút "Đánh dấu đã xử lý" cho từng phát hiện OPEN), bước 5-6 form đóng phiên đối soát — nút bị khoá + cảnh báo khi còn phát hiện chưa xử lý) + import + route `/intake-reconciliation` + thẻ module trang chủ (`App.jsx`) + mục nav "Đối soát phiên intake" trong nhóm "Dữ liệu" (`nav.js`). **Chưa chạy được**: sandbox không cài Node/npm packages cho frontend — cần bạn chạy `npm install && npm run build` (frontend) để xác nhận biên dịch JSX/lint không lỗi, và `docker compose exec ingestion-service alembic upgrade head` (áp migration `0012`) trên Postgres thật, trước khi chuyển `tested`) |
| UC-028 | Xử lý ticket đối soát với chủ quản nguồn | Quản trị Tích hợp | done (không tạo lại hạ tầng "phiên đối soát" — tái sử dụng `IntakeReconciliationRepository` (UC-027) chỉ để xác nhận `reconciliation_id` tồn tại khi mở ticket; thêm 1 bảng `reconciliation_tickets` để lưu ticket + lịch sử xử lý với chủ quản nguồn. Domain (`app/domain/entities.py`): entity `ReconciliationTicket` (`status` ∈ {OPEN, IN_PROGRESS, RESOLVED, CLOSED}, `history`: danh sách dict `{note, updated_by, status, updated_at}`); method `add_progress()` (bước 2-3, chỉ cho phép khi chưa `CLOSED`, ghi 1 mốc vào `history`, tuỳ chọn chuyển trạng thái OPEN/IN_PROGRESS/RESOLVED), `close()` (bước 4-5, chỉ cho phép đóng khi đang `RESOLVED` — điều kiện "khi resolved" — chuyển RESOLVED → CLOSED + tự ghi thêm 1 mốc `CLOSED` vào `history` — "ghi nhật ký"); `app/domain/exceptions.py` thêm `ReconciliationTicketNotFound` (404), `InvalidReconciliationTicket` (422), `ReconciliationTicketAlreadyClosed` (422), `ReconciliationTicketNotResolved` (422, đóng khi chưa RESOLVED); `app/domain/repositories.py` thêm interface `ReconciliationTicketRepository` (`add`/`update`/`get_by_id`/`list`). Application: `app/application/use_cases/manage_reconciliation_ticket.py` (`ReconciliationTicketService`): bước 1 `open_ticket(reconciliation_id, source_owner, title, description, opened_by)` — 404 nếu phiên đối soát không tồn tại (tái dùng `IntakeReconciliationRepository`), 422 nếu `source_owner`/`title` rỗng, lưu ticket + đặt `notified=True` (mô phỏng thông báo cho chủ quản nguồn); `add_progress()` — 404 nếu ticket không tồn tại, 422 nếu đã đóng hoặc dữ liệu không hợp lệ, lưu ngay qua repository; `close()` — 404 nếu không tồn tại, 422 nếu đã đóng hoặc chưa RESOLVED, cập nhật `status: RESOLVED → CLOSED` + `closed_by`/`closed_at`/`close_note`; `get`/`list_tickets` xem lại lịch sử. Infrastructure: `app/infrastructure/db/models.py` thêm `ReconciliationTicketModel` (bảng `reconciliation_tickets`, FK `reconciliation_id → intake_reconciliations.id`), `app/infrastructure/db/repository_impl.py` thêm `SqlAlchemyReconciliationTicketRepository`. Interface: `app/interfaces/api/reconciliation_ticket_router.py` (prefix `/reconciliation-tickets`): `POST ""` (bước 1, mở ticket), `GET ""` (lọc `reconciliation_id`/`status`), `GET "/{id}"`, `POST "/{id}/progress"` (bước 2-3), `POST "/{id}/close"` (bước 4-5); schema Pydantic mới trong `schemas.py` (`ReconciliationTicketOpenRequest`, `ReconciliationTicketProgressRequest`, `ReconciliationTicketCloseRequest`, `ReconciliationTicketHistoryResponse`, `ReconciliationTicketResponse`); đã `include_router` trong `app/main.py`. Migration: `alembic/versions/0013_uc028_create_reconciliation_tickets.py` (nối tiếp `0012` → `down_revision="0012"`, tạo bảng `staging.reconciliation_tickets` + index `reconciliation_id`/`status`). Test: `tests/test_uc028_reconciliation_ticket.py` (16 test case qua `TestClient`, **đã chạy pass**): mở ticket lưu + `notified=True`; 404 phiên đối soát không tồn tại; 422 `source_owner`/`title` rỗng; cập nhật tiến độ được lưu vào `history` (kèm chuyển trạng thái IN_PROGRESS/RESOLVED); 404 ticket không tồn tại; 422 `note` rỗng; 422 cập nhật tiến độ khi đã đóng; đóng thành công khi đã RESOLVED (kèm ghi thêm 1 mốc `CLOSED` vào `history`); 422 đóng khi chưa RESOLVED; 422 đóng khi đã đóng trước đó; 404 đóng khi không tồn tại; danh sách lọc theo `reconciliation_id`/`status`. **Đã tự kiểm tra và chạy được**: `pip install -r requirements.txt` + `pytest -q` toàn bộ service **171 passed** (không phá vỡ UC-015..027 hiện có; nhân tiện phát hiện + sửa 1 lỗi có sẵn ở UC-027 — `IntakeReconciliation.close()` chưa từng lưu `close_note`); kiểm tra chuỗi migration Alembic bằng `ScriptDirectory.walk_revisions()` — head đúng `0013 → 0012 → ... → 0001`. **Chưa chạy được**: `docker compose exec ingestion-service alembic upgrade head` (áp migration `0013`) trên Postgres thật, trước khi chuyển `tested`. Frontend (**đã làm + đã build thành công trong lượt sau**): `frontend/src/api/reconciliationTicket.js` (5 hàm: `openReconciliationTicket`, `listReconciliationTickets`, `getReconciliationTicket`, `addReconciliationTicketProgress`, `closeReconciliationTicket`, dùng chung `ingestionClient`); trang `/reconciliation-tickets` (`frontend/src/pages/ingestion/ReconciliationTicketPage.jsx` — bước 1 chọn phiên đối soát (UC-027) + nhập chủ quản nguồn/tiêu đề/mô tả để mở ticket, danh sách ticket theo phiên đối soát, bước 2 form cập nhật tiến độ (kèm chuyển trạng thái) + bảng lịch sử, bước 3 form đóng ticket — nút bị khoá khi chưa RESOLVED) + import + route `/reconciliation-tickets` + thẻ module trang chủ (`App.jsx`) + mục nav "Xử lý ticket đối soát với chủ quản nguồn" trong nhóm "Dữ liệu" (`nav.js`). Đã chạy `npm install && npm run build` — biên dịch thành công, không lỗi.) |

## Nhóm: CHUẨN HÓA VÀ QUẢN TRỊ DỮ LIỆU — service: `data-quality-service`

| UC | Tên | Tác nhân | Status |
|---|---|---|---|
| UC-029 | Phân tích dữ liệu có cấu trúc | Hệ thống tự động (Bộ phân tích cú pháp) | done (service `data-quality-service` — lần đầu implement UC cho service này (đã có scaffold Clean Architecture trống). Domain (`app/domain/entities.py`): entity `ParsingJob` (1 lượt xử lý = 1 sự kiện `parsing.requested`, `SOURCE_FORMATS`=(CSV,EXCEL,JSON,XML), `DATA_TYPES` giống UC-018, `STATUSES`=(RECEIVED,RUNNING,MAPPED,FAILED), `append_log()`/`start_running()`/`complete()`), `ParsingRowError` (lỗi ép kiểu/ánh xạ từng dòng), `ParsedRecord` (bản ghi đầu ra bước 4); `app/domain/exceptions.py` (`ParsingJobNotFound`, `InvalidParsingJob`, `RawObjectNotFound`, `UnsupportedSourceFormat`); `app/domain/repositories.py` (interface `ParsingJobRepository`, `StgStructuredRowRepository`, `ParsedRecordRepository`, `ParsingRowErrorRepository`, cổng `FileStorage`, `EventPublisher`). Application: `app/application/use_cases/parse_structured_data.py` (`StructuredParsingService.receive_and_process()` chạy trọn 6 bước tự động trong 1 lần gọi — actor là hệ thống tự động nên không có thao tác thủ công xen giữa: (1) nhận `parsing.requested` tạo `ParsingJob`; (2) đọc dữ liệu thô qua `FileStorage.download(raw_object_key)` → lưu từng dòng vào bảng `stg_structured_rows` ("stg_*"); (3) `parse_raw_bytes()` phân tích CSV/JSON/XML/Excel theo `source_format` (tự suy luận theo đuôi tệp nếu không truyền); (4) `map_and_cast_row()` ánh xạ tên trường (theo `field_mapping` tường minh hoặc tự khớp tên đã chuẩn hoá) + ép kiểu theo `schema_fields` — lỗi ép kiểu từng trường được ghi vào `parsing_row_errors`, không làm hỏng cả dòng/cả job; (5-6) kích hoạt + đẩy sự kiện `mapping.requested` qua `EventPublisher` cho UC-031, CHỈ khi có ≥1 bản ghi ánh xạ thành công (nếu 0 bản ghi thành công → job `FAILED`, không đẩy sự kiện)). Infrastructure: `app/infrastructure/structured_parser.py` (thuần Python, không phụ thuộc DB/HTTP: `parse_raw_bytes()` cho CSV/JSON/XML/Excel qua `openpyxl`, `cast_value()` ép kiểu 8 data_type, `build_auto_field_mapping()` tự khớp tên cột chuẩn hoá); `app/infrastructure/file_storage.py` (`LocalDiskFileStorage`/`MinioFileStorage`/`get_raw_data_storage()` — dùng CHUNG bucket mặc định `tabmis-intake` với ingestion-service qua biến môi trường `MINIO_BUCKET_TABMIS_INTAKE`, vì raw_object_key trong sự kiện `parsing.requested` trỏ tới tệp do ingestion-service ghi); `app/infrastructure/event_publisher.py` (`LoggingEventPublisher`/`RabbitMqEventPublisher`/`get_event_publisher()`, exchange `data-quality.events`, cùng pattern UC-024); `app/infrastructure/db/models.py` (`ParsingJobModel` bảng `parsing_jobs`, `StgStructuredRowModel` bảng `stg_structured_rows`, `ParsedStructuredRecordModel` bảng `parsed_structured_records`, `ParsingRowErrorModel` bảng `parsing_row_errors` — schema `curated` khi Postgres, bỏ qua khi SQLite dev/test cùng pattern ingestion-service), `app/infrastructure/db/repository_impl.py` (4 `SqlAlchemy*Repository` tương ứng). Interface: `app/interfaces/api/schemas.py` (`ParsingRequestedEvent` mô phỏng đúng payload sự kiện thật, `ParsingJobResponse`, `ParsingRowErrorResponse`, `ParsedRecordResponse`); `app/interfaces/api/parsing_job_router.py` (prefix `/parsing-jobs`): `POST ""` nhận sự kiện `parsing.requested` + chạy trọn pipeline (201, trả `ParsingJobResponse` dù thành công hay `FAILED` — lỗi hạ tầng/parse không raise HTTP mà phản ánh qua `status=FAILED`+`error_message`), `GET ""` (lọc `dataset_id`/`status`/`ingestion_run_id`), `GET "/{id}"` (404 nếu không có), `GET "/{id}/row-errors"`, `GET "/{id}/parsed-records"` (để UC-031 xem lại), `GET "/{id}/stg-rows"` (xem dữ liệu thô đã đọc bước 2); `app/main.py` đã `include_router` + tự `create_all` khi SQLite dev/test. `requirements.txt` thêm `openpyxl`, `minio`, `pika`; `Dockerfile` bổ sung `COPY alembic.ini`/`alembic`/`tests` (thiếu từ trước). Migration: `alembic.ini` + `alembic/env.py` (mới cho service này, `version_table="alembic_version_data_quality"` — tránh xung đột với `ingestion-service` trên cùng Postgres) + `alembic/versions/0001_uc029_create_parsing_tables.py` (tạo `CREATE SCHEMA IF NOT EXISTS curated` + 4 bảng trên + index). Sửa nhỏ ở `ingestion-service` (`app/application/use_cases/sync_incremental.py`, UC-025): bổ sung 2 trường `source_format="JSON"` và `schema_fields=dataset.schema_fields` (denormalized) vào payload sự kiện `parsing.requested` đã có sẵn, để UC-029 không phải gọi đồng bộ ngược lại ingestion-service chỉ để lấy lược đồ — không phá vỡ test UC-025 hiện có (test không assert exact dict). Test: `tests/test_uc029_structured_parsing.py` (23 test case qua `TestClient`, SQLite in-memory, **đã tự chạy pass**): parse CSV/JSON (dạng list và dạng `{"records":[...]}`)/XML/Excel happy path + đẩy đúng sự kiện `mapping.requested`; tự suy luận `source_format` theo đuôi tệp + 422 khi không suy luận được và không truyền; ánh xạ tường minh qua `field_mapping`; tự ánh xạ theo tên chuẩn hoá (khoảng trắng/hoa thường); cột thừa không khớp bị bỏ qua; cột đích thiếu trong nguồn → null (không phải lỗi); 1 dòng lỗi ép kiểu vẫn giữ job `MAPPED` (ghi vào `row-errors`, `records_failed` tăng đúng); toàn bộ dòng lỗi ép kiểu → job `FAILED`, KHÔNG đẩy sự kiện `mapping.requested`; ép kiểu BOOLEAN/DATE nhiều biến thể; không tìm thấy `raw_object_key` trên storage → job `FAILED`; 422 khi `schema_fields` rỗng hoặc `data_type` không hợp lệ; JSON thô lỗi cú pháp → job `FAILED`; lọc danh sách theo `dataset_id`/`status`/`ingestion_run_id`; 404 khi xem job/`row-errors` không tồn tại; xem lại dữ liệu thô đã lưu bước 2 (`stg-rows`); `ingestion_run_id`/`data_source_id` được lưu + lọc đúng. **Đã tự chạy**: `pytest services/data-quality-service -q` **24 passed** (23 test mới + `test_health.py`); `pytest services/ingestion-service -q` **171 passed** (không phá vỡ UC-015..028 hiện có sau khi sửa `sync_incremental.py`); `python -m py_compile` toàn bộ file Python thay đổi (pass); kiểm tra cú pháp file Alembic mới bằng `ast.parse` (pass). Frontend: `frontend/src/api/parsingJobs.js` (5 hàm gọi API, `dataQualityClient` baseURL `/api/data-quality` mới); trang `/parsing-jobs` (`frontend/src/pages/dataquality/ParsingJobsPage.jsx` — form mô phỏng kích hoạt thủ công sự kiện `parsing.requested`: chọn tập dữ liệu đã đăng ký ở ingestion-service (tự lấy `schema_fields` từ dataset đã chọn), nhập `raw_object_key` + chọn/để trống `source_format`, nút gửi sự kiện; hiển thị kết quả (trạng thái, tổng kiểm soát, cờ đã đẩy `mapping.requested`, bảng lỗi từng dòng, nhật ký từng bước); bảng lịch sử các phiên phân tích của tập dữ liệu đã chọn); `frontend/vite.config.js` thêm proxy dev mới `/api/data-quality` → `http://localhost:8003` (data-quality-service trước đó CHƯA có proxy dev nào, đây là proxy đầu tiên cho service này); cập nhật `frontend/src/App.jsx` (import `ParsingJobsPage`, route `/parsing-jobs`) + `frontend/src/nav.js` (bỏ mục placeholder `disabled` "Chuẩn hoá dữ liệu" ở nhóm "Dữ liệu", thay bằng nhóm nav mới "Chuẩn hóa và quản trị dữ liệu" chứa mục thật "Phân tích dữ liệu có cấu trúc" — đúng tên nhóm nghiệp vụ trong PLAN.md/`docs/use_cases.json`). **Đã tự chạy `npm install && npm run build`** — biên dịch thành công, không lỗi (đã dọn `node_modules`/`dist` sau khi build thử). `docker-compose.yml`: bổ sung biến môi trường `MINIO_ENDPOINT`/`MINIO_ROOT_USER`/`MINIO_ROOT_PASSWORD`/`MINIO_BUCKET_TABMIS_INTAKE`/`MINIO_SECURE` + `depends_on.minio` cho `data-quality-service` (trước đó service này chỉ có `DATABASE_URL`, chưa đọc được MinIO). **⚠️ Còn tồn đọng — cần bạn xác nhận trước khi chuyển `tested`**: sandbox không có Docker/Postgres/MinIO/RabbitMQ thật nên chưa chạy được `docker compose exec data-quality-service alembic upgrade head` (áp migration `0001` — đây là migration ĐẦU TIÊN của service này) trên Postgres thật, và chưa kiểm thử thật với MinIO/RabbitMQ thật (hiện dùng `LocalDiskFileStorage`/`LoggingEventPublisher` khi thiếu biến môi trường `MINIO_ENDPOINT`/`RABBITMQ_URL`, đúng pattern các UC khác); `RABBITMQ_URL` cũng chưa được set trong `docker-compose.yml` cho cả `ingestion-service` lẫn `data-quality-service` (tồn đọng từ trước, không phát sinh mới ở UC-029 — 2 service tạm thời chỉ log sự kiện, chưa publish RabbitMQ thật)) |
| UC-030 | Phân tích PDF/bản quét + OCR | Hệ thống tự động (OCR Quy trình xử lý) | done (**⚠️ đã fix bug nghiêm trọng phát hiện qua log `docker compose logs data-quality-service`**: file use case bị đặt sai tên `app/application/use_cases/ocr_extract_pdf,py` — dấu PHẨY thay vì dấu CHẤM trước `py` — khiến Python không coi đây là module `.py`, `ModuleNotFoundError: No module named 'app.application.use_cases.ocr_extract_pdf'` khi `main.py` import, service không khởi động được; đã `mv` đổi tên đúng thành `ocr_extract_pdf.py`, đã tự kiểm tra lại toàn bộ file Python trong `data-quality-service` bằng `python3 -m py_compile` (pass hết, không còn lỗi tên file tương tự) — backend đã có sẵn từ trước trong sandbox này, chỉ riêng lỗi tên file này là mới phát hiện;  — `app/infrastructure/ocr_engine.py`, `app/interfaces/api/ocr_job_router.py` (prefix `/ocr-jobs`, đã `include_router` trong `main.py`), use case `app/application/use_cases/ocr_extract_pdf,py` (`OcrExtractionService.receive_and_process()`: đọc PDF/scan qua `FileStorage` (bucket `raw-documents`) → chạy OCR PaddleOCR/olmOCR qua cổng `OCRClient`/`get_ocr_engine()` → trích xuất văn bản + bảng → lưu `ocr_jobs`/`ocr_extracted_tables` (curated) → kích hoạt + đẩy sự kiện `ocr.completed` + `parsing.requested` cho UC-029 khi trích được nội dung), migration `alembic/versions/0002_uc030_create_ocr_tables.py` (nối tiếp `0001` UC-029), test `tests/test_uc030_ocr_extraction.py`; **lượt làm việc hiện tại chỉ bổ sung phần frontend còn thiếu** (chưa từng có UI cho UC-030 dù backend đã xong): `frontend/src/api/ocrJobs.js` (4 hàm: `receiveOcrRequested`, `listOcrJobs`, `getOcrJob`, `listOcrTables` — dùng chung `dataQualityClient` (baseURL `/api/data-quality`) export từ `api/parsingJobs.js`, cùng service `data-quality-service` với UC-029, không cần proxy dev mới); trang `/ocr-jobs` (`frontend/src/pages/dataquality/OcrJobsPage.jsx`, theo đúng cấu trúc `ParsingJobsPage.jsx` UC-029 — form mô phỏng kích hoạt thủ công sự kiện `ocr.requested`: chọn văn bản đã tiếp nhận qua UC-024 (`listVanBanDocuments`, tự điền `raw_object_key`/`so_ky_hieu`/`data_source_id`) hoặc tự nhập tay `raw_object_key`, chọn công cụ OCR (PADDLEOCR/OLMOCR, để trống dùng mặc định); hiển thị kết quả: trạng thái, công cụ yêu cầu/đã dùng, số trang xử lý, số bảng trích xuất, 2 cờ đã đẩy sự kiện `ocr.completed`/`parsing.requested`, văn bản trích xuất, bảng dữ liệu trích xuất từng bảng (`GET /ocr-jobs/{id}/tables`), nhật ký từng bước; bảng lịch sử toàn bộ phiên OCR + nút xem lại chi tiết 1 phiên); cập nhật `frontend/src/App.jsx` (import `OcrJobsPage` + icon `FileScan`, thêm route `/ocr-jobs`, thêm thẻ module ở trang chủ sau thẻ \"Tiếp nhận văn bản QLVBĐH\") + `frontend/src/nav.js` (import icon `FileScan`, thêm mục \"Phân tích PDF/bản quét + OCR\" vào nhóm \"Chuẩn hóa và quản trị dữ liệu\" ngay sau mục \"Phân tích dữ liệu có cấu trúc\" của UC-029). **Đã tự kiểm tra**: kiểm tra cân bằng ngoặc `()[]{}` cho `App.jsx`/`nav.js`/`api/ocrJobs.js`/`OcrJobsPage.jsx` bằng script Python (pass); router/use case/migration/test backend không sửa gì (giữ nguyên như đã có). **⚠️ Chưa chạy được trong sandbox này (không có Internet để cài node_modules/fastapi/sqlalchemy — `npm install` báo lỗi 403 registry.npmjs.org)**: chưa tự chạy `npm run build` để xác nhận biên dịch JSX không lỗi, chưa tự chạy `pytest services/data-quality-service -q` để xác nhận `test_uc030_ocr_extraction.py` vẫn pass sau khi cài đặt lại, chưa chạy `docker compose exec data-quality-service alembic upgrade head` (áp migration `0002`) trên Postgres thật — cần bạn chạy các lệnh này để xác nhận trước khi chuyển `tested`. **Cập nhật lượt sau — bật OCR THẬT (PaddleOCR)**: theo yêu cầu (máy người dùng đã có sẵn PaddleOCR), đổi từ `NoOpOcrEngine` giả lập sang `PaddleOcrTableEngine` thật (class đã có sẵn trong `ocr_engine.py` từ trước, chỉ chưa được bật): `requirements.txt` bỏ comment + ghim phiên bản `paddlepaddle==2.6.2`, `paddleocr==2.9.1`, `pymupdf==1.24.11`; `Dockerfile` thêm cài đặt `libgl1`/`libglib2.0-0`/`libgomp1` (opencv-python, dependency của paddleocr, cần các thư viện hệ thống này để import được trên image `python:3.11-slim`, nếu không sẽ lỗi `ImportError: libGL.so.1: cannot open shared object file`); `docker-compose.yml` thêm biến môi trường `OCR_USE_REAL_ENGINE=true` + `OCR_ENGINE=PADDLEOCR` cho `data-quality-service` (đọc bởi `get_ocr_engine()`) — olmOCR CHƯA cài (người dùng chọn chỉ dùng PaddleOCR trong lượt này). **Lưu ý quan trọng cần bạn biết**: `paddlepaddle`+`paddleocr` khá nặng (~1-2GB các gói phụ thuộc), lần build Docker đầu tiên sẽ lâu hơn đáng kể; lần đầu container chạy `PaddleOCR(use_angle_cls=True, lang="vi")` sẽ tự tải model nhận dạng (cần Internet, model lưu cache trong container — nếu container bị xoá sẽ tải lại, cân nhắc mount volume cache `~/.paddleocr` nếu muốn giữ qua các lần rebuild); **cần bạn tự `docker compose build data-quality-service && docker compose up -d data-quality-service` rồi theo dõi `docker compose logs -f data-quality-service` để xác nhận build/tải model + khởi động thành công trước khi coi là xong** — sandbox này không có Docker/Internet đủ để tự kiểm thử bước này) |
| UC-031 | Ánh xạ trường sang dạng chuẩn | Hệ thống tự động (Bộ ánh xạ dữ liệu) | done (tiếp tục trong service `data-quality-service` (cùng service UC-029/UC-030). Domain (`app/domain/entities.py`): entity `MappingRule` (quy tắc ánh xạ có phiên bản `version`, `rule_type`=DIRECT/CATALOG_LOOKUP, `catalog_map` cho tra cứu danh mục chuẩn, `normalize_case`, `dataset_id=None`=quy tắc chung), `MappingJob` (1 lượt xử lý = 1 sự kiện `mapping.requested`, `STATUSES`=(RECEIVED,RUNNING,COMPLETED,FAILED), cùng pattern vòng đời `ParsingJob`/`OcrJob`), `MappingRejection` (bước 2), `UnmappedQueueItem` (bước 3, `STATUSES`=(PENDING,RESOLVED) — RESOLVED dành cho UC-032 dùng sau), `MappedStandardRecord` (đầu ra); `app/domain/exceptions.py` (`MappingJobNotFound`, `InvalidMappingJob`, `InvalidMappingRule`, `NoParsedRecordsToMap`); `app/domain/repositories.py` (interface `MappingRuleRepository`, `MappingJobRepository`, `MappingRejectionRepository`, `UnmappedQueueRepository`, `MappedStandardRecordRepository`). Application: `app/application/use_cases/map_field_to_standard.py` (`FieldMappingService.receive_and_process()` — nhận `mapping.requested` (parsing_job_id [+dataset_id tuỳ chọn, mặc định lấy theo `parsing_job.dataset_id`]), đọc lại `ParsedRecord.has_error=False` của `parsing_job_id` đó (đầu ra bước 4 UC-029), rồi chạy đúng 3 bước theo yêu cầu: (1) tra cứu `MappingRuleRepository.get_active_rules_for_dataset()` — ưu tiên quy tắc gắn `dataset_id` cụ thể, version lớn nhất đang active — áp dụng `apply_rule()` (DIRECT: trim+đổi hoa/thường; CATALOG_LOOKUP: tra `catalog_map` theo khoá trim+upper) để chuẩn hoá field; (2) "trường bắt buộc" lấy từ `ParsingJob.schema_fields` (`nullable=False`, đã có sẵn từ UC-018/UC-029) — KHÔNG phụ thuộc `MappingRule` — nếu sau chuẩn hoá còn rỗng/None thì từ chối CẢ DÒNG, ghi 1+ `MappingRejection` (`mapping_rejections`), dòng bị từ chối không lưu vào `mapped_standard_records`; (3) giá trị CATALOG_LOOKUP không khớp `catalog_map` (dù trường đó có bắt buộc hay không) luôn được đẩy vào `UnmappedQueueItem` (`unmapped_value_queue`, status PENDING) cho Phụ trách Dữ liệu — độc lập với việc dòng có bị từ chối ở bước 2 hay không; trường không có quy tắc nào → giữ nguyên giá trị (pass-through), không coi là "chưa ánh xạ"; 0 bản ghi `has_error=False` nào → job `FAILED` kèm `NoParsedRecordsToMap`, không raise lỗi HTTP). `app/application/use_cases/manage_mapping_rule.py` (`MappingRuleService` — CRUD tối thiểu add/list cho `mapping_rules`: BCKTKT CHƯA có UC riêng quản trị bảng này — nhóm danh mục UC-33..36 chỉ quản lý danh mục nghiệp vụ đơn vị/khoản mục NSNN/nhóm tài sản/mặt hàng, không phải quy tắc ánh xạ trường — nên bổ sung service tối thiểu này làm dữ liệu đầu vào bắt buộc cho bước 1, cùng tinh thần UC-018 phải đăng ký `schema_fields` trước khi UC-029 có gì để phân tích). Infrastructure: `app/infrastructure/field_normalizer.py` (thuần Python, không phụ thuộc DB/HTTP: `apply_rule()`, `is_empty()`); `app/infrastructure/db/models.py` (`MappingRuleModel` bảng `mapping_rules`, `MappingJobModel` bảng `mapping_jobs`, `MappingRejectionModel` bảng `mapping_rejections`, `UnmappedQueueItemModel` bảng `unmapped_value_queue`, `MappedStandardRecordModel` bảng `mapped_standard_records` — schema `curated` (ADR: tài liệu nghiệp vụ gốc gọi 2 bảng đầu là "metadata.mapping_rules"/"metadata.mapping_rejections" nhưng theo ADR-001 mỗi service 1 schema Postgres duy nhất, cùng cách xử lý ingestion-service UC-018)); `app/infrastructure/db/repository_impl.py` (5 `SqlAlchemy*Repository` tương ứng, `get_active_rules_for_dataset()` ưu tiên quy tắc gắn dataset cụ thể > quy tắc chung, version lớn nhất trong cùng phạm vi). Interface: `app/interfaces/api/schemas.py` (`MappingRuleCreate`/`MappingRuleResponse`, `MappingRequestedEvent` mô phỏng đúng payload sự kiện `mapping.requested` thật, `MappingJobResponse`, `MappingRejectionResponse`, `UnmappedQueueItemResponse`, `MappedStandardRecordResponse`); `app/interfaces/api/mapping_rule_router.py` (prefix `/mapping-rules`): `POST ""` đăng ký quy tắc (422 nếu CATALOG_LOOKUP thiếu `catalog_map` hoặc `rule_type`/`normalize_case` không hợp lệ), `GET ""` (lọc `dataset_id`/`field_name`/`is_active`); `app/interfaces/api/mapping_job_router.py` (prefix `/mapping-jobs`): `POST ""` nhận sự kiện `mapping.requested` + chạy trọn pipeline (201, trả `MappingJobResponse` dù COMPLETED hay FAILED; 404 nếu `parsing_job_id` không tồn tại), `GET ""` (lọc `dataset_id`/`parsing_job_id`/`status`), `GET "/{id}"` (404 nếu không có), `GET "/{id}/rejections"`, `GET "/{id}/unmapped-queue"` (để UC-032 xem lại sau này), `GET "/{id}/standard-records"`; `app/main.py` đã `include_router` cả 2 router mới. Migration: `alembic/versions/0003_uc031_create_mapping_tables.py` (nối tiếp `0002` UC-030, tạo 5 bảng trên + index trong schema `curated`, có `downgrade()` đầy đủ). `requirements.txt`/`Dockerfile`: không cần thêm gói mới (chỉ dùng FastAPI/SQLAlchemy/Pydantic đã có sẵn từ UC-029/030). Test: `tests/test_uc031_field_mapping.py` (13 test case qua `TestClient`, SQLite in-memory) — quy tắc CATALOG_LOOKUP áp dụng + chọn đúng version lớn nhất đang active; quy tắc DIRECT chuẩn hoá hoa/thường; từ chối cả dòng khi trường bắt buộc rỗng sau chuẩn hoá (kèm giá trị đó cũng bị đẩy vào hàng đợi nếu là CATALOG_LOOKUP không khớp); giá trị chưa ánh xạ ở trường KHÔNG bắt buộc chỉ đẩy vào hàng đợi, không làm từ chối dòng; trường không có quy tắc giữ nguyên giá trị; lọc danh sách theo `dataset_id`/`parsing_job_id`; 404 khi xem job/rejections không tồn tại; 404 khi `parsing_job_id` không tồn tại; parsing_job tồn tại nhưng không có bản ghi hợp lệ nào → job `FAILED` (không raise lỗi HTTP); 422 khi đăng ký quy tắc CATALOG_LOOKUP thiếu `catalog_map` hoặc `rule_type` không hợp lệ; lọc danh sách quy tắc theo `dataset_id`+`field_name`. Frontend: `frontend/src/api/mappingJobs.js` (8 hàm gọi API, dùng CHUNG `dataQualityClient` export từ `api/parsingJobs.js` — không cần proxy dev mới, đã có sẵn từ UC-029); trang `/mapping-jobs` (`frontend/src/pages/dataquality/MappingJobsPage.jsx`, theo đúng cấu trúc `ParsingJobsPage.jsx` — khối đăng ký quy tắc ánh xạ (bước 1, form field_name/rule_type/version/catalog_map JSON hoặc normalize_case + bảng quy tắc đã đăng ký của tập dữ liệu), khối mô phỏng kích hoạt thủ công sự kiện `mapping.requested` (chọn 1 phiên phân tích UC-029 đã "Đã ánh xạ" của tập dữ liệu), khối kết quả (trạng thái, tổng kiểm soát, bảng dòng bị từ chối bước 2, bảng hàng đợi chưa ánh xạ bước 3, JSON bản ghi đã chuẩn hoá), bảng lịch sử phiên ánh xạ); cập nhật `frontend/src/App.jsx` (import `MappingJobsPage`, route `/mapping-jobs`) + `frontend/src/nav.js` (thêm mục "Ánh xạ trường sang dạng chuẩn" vào nhóm "Chuẩn hóa và quản trị dữ liệu" ngay sau "Phân tích dữ liệu có cấu trúc"). **⚠️ Chưa tự chạy được trong sandbox này (không có Internet để cài `fastapi`/`sqlalchemy`/`pytest`/`node_modules` — `pip install`/`npm install` đều báo lỗi không kết nối được registry)**: đã tự kiểm tra kỹ bằng tĩnh — `python3 -m py_compile` toàn bộ file Python mới/sửa (pass), kiểm tra cân bằng ngoặc `()[]{}` cho `MappingJobsPage.jsx` (pass), đối chiếu thủ công tên class/hàm import chéo giữa `repository_impl.py`/`mapping_rule_router.py`/`mapping_job_router.py` khớp nhau, đối chiếu `row_index` bắt đầu từ 0 (theo `parse_structured_data.py`) và kiểu `float` của `DECIMAL` (theo `structured_parser.py`) khi viết assertion test — nhưng CHƯA tự chạy được `pytest services/data-quality-service -q` để xác nhận 13 test mới thật sự pass, CHƯA chạy `npm run build`, CHƯA chạy `docker compose exec data-quality-service alembic upgrade head` (áp migration `0003`) trên Postgres thật. Cần bạn chạy các lệnh này để xác nhận trước khi chuyển `tested`.) |
| UC-032 | Xử lý hàng đợi chưa ánh xạ | Phụ trách Dữ liệu | done (tiếp tục trong service `data-quality-service` (cùng service UC-029/030/031). Khác biệt so với UC-029..031: actor là con người ("Phụ trách Dữ liệu"), không phải hệ thống tự động — nên không có 1 hàm `receive_and_process()` chạy trọn luồng, mà có các endpoint tương tác riêng theo đúng 3 bước của flow. Domain (`app/domain/entities.py`): mở rộng `UnmappedQueueItem` (bước 3 của UC-031) thêm các trường lưu kết quả xử lý `resolution_action` (`MAP`/`CREATE_NEW`/`REJECT`), `resolved_value`, `resolution_reason`, `resolved_at` + phương thức `resolve()` (validate `status` phải đang `PENDING`, `resolved_value` bắt buộc khi `MAP`/`CREATE_NEW`, `reason` bắt buộc khi `REJECT`) và `lookup_key()` (chuẩn hoá trim+upper, dùng để so khớp "giá trị tương tự" ở bước 3, cùng cách `MappingRule.lookup_key()`). `app/domain/exceptions.py` thêm `UnmappedQueueItemNotFound`, `InvalidUnmappedQueueResolution`. `app/domain/repositories.py` mở rộng `UnmappedQueueRepository` thêm `get_by_id()`, `update()`, `list_queue()` (bước 1 — không giới hạn theo 1 `mapping_job_id`, lọc `dataset_id`/`field_name`/`status`), `find_similar_pending()` (bước 3 — tìm các mục PENDING khác cùng `dataset_id`+`field_name`+giá trị nguồn đã chuẩn hoá). Application (`app/application/use_cases/resolve_unmapped_queue.py`, mới): `UnmappedQueueService` — `list_queue()` (bước 1), `resolve_item()` (bước 2+3 trong 1 lần gọi, có tham số `apply_to_similar`): (2) `action=MAP`/`CREATE_NEW` → gọi `_save_new_mapping()` tạo 1 `MappingRule` PHIÊN BẢN MỚI (CATALOG_LOOKUP, gắn `dataset_id` cụ thể của mục hàng đợi để được ưu tiên áp dụng theo logic `get_active_rules_for_dataset()` đã có từ UC-031 — quy tắc gắn dataset cụ thể luôn thắng quy tắc chung dù version thấp hơn), merge `catalog_map` của quy tắc đang active (nếu có) + thêm khoá `raw_value` chuẩn hoá → `standard_value` — TỪ LẦN CHẠY UC-031 TIẾP THEO, giá trị này tự ánh xạ được, không rơi vào hàng đợi nữa (kiểm chứng bằng test round-trip); `action=REJECT` → không tạo quy tắc, chỉ ghi nhận `reason`; (3) `apply_to_similar=true` → tìm qua `find_similar_pending()` rồi áp dụng CÙNG kết quả xử lý (dùng lại đúng 1 `MappingRule` đã tạo ở bước 2, không tạo thêm quy tắc trùng lặp) cho từng mục tương tự, trả về `affected_items`. Infrastructure: `app/infrastructure/db/models.py` thêm 4 cột vào `UnmappedQueueItemModel` (`resolution_action`, `resolved_value`, `resolution_reason`, `resolved_at`); `app/infrastructure/db/repository_impl.py` bổ sung `get_by_id()`/`update()`/`list_queue()`/`find_similar_pending()` cho `SqlAlchemyUnmappedQueueRepository`. **Sửa bug hạ tầng có sẵn từ UC-029..031** (`app/infrastructure/db/session.py`): `sqlite:///:memory:` không có `poolclass=StaticPool` khiến MỖI connection mới (mỗi request qua `get_db()`) mở ra 1 CSDL trống riêng biệt → lỗi `OperationalError: no such table` — phát hiện khi tự chạy `pytest` cho UC-032 (lỗi này khiến toàn bộ 45 test UC-029/030/031 trước đó cũng fail khi chạy qua `pytest` — đã thêm `poolclass=StaticPool` chỉ khi `DATABASE_URL` là sqlite in-memory, không ảnh hưởng SQLite trên đĩa (dev) hay Postgres (production)). Interface: `app/interfaces/api/schemas.py` thêm `resolution_action`/`resolved_value`/`resolution_reason`/`resolved_at` vào `UnmappedQueueItemResponse` (tương thích ngược), `ResolveUnmappedQueueRequest` (`action`, `standard_value`, `reason`, `apply_to_similar`), `ResolveUnmappedQueueResponse` (`item`, `updated_rule`, `affected_items`, `affected_count`); `app/interfaces/api/unmapped_queue_router.py` (mới, prefix `/unmapped-queue`): `GET ""` (bước 1, mặc định `status=PENDING`, lọc `dataset_id`/`field_name`), `GET "/{id}"` (404 nếu không có), `POST "/{id}/resolve"` (bước 2+3, 404 nếu không tìm thấy mục, 422 nếu `action` không hợp lệ/thiếu `standard_value` khi MAP/CREATE_NEW/thiếu `reason` khi REJECT/mục đã RESOLVED trước đó); `app/main.py` đã `include_router`. Migration: `alembic/versions/0004_uc032_unmapped_queue_resolution.py` (nối tiếp `0003`, `ADD COLUMN` 4 cột mới vào `unmapped_value_queue`, có `downgrade()` đầy đủ). Test: `tests/test_uc032_unmapped_queue.py` (13 test case qua `TestClient`, SQLite in-memory, **đã tự chạy pass**) — bước 1: lọc mặc định PENDING, lọc theo `field_name` không khớp trả rỗng, 404 khi xem mục không tồn tại; bước 2: `MAP` tạo đúng phiên bản `MappingRule` mới (version nối tiếp, giữ khoá cũ + thêm khoá mới) và mục biến mất khỏi hàng đợi PENDING, `CREATE_NEW` hoạt động tương tự, `REJECT` bắt buộc `reason` (422 nếu thiếu) và KHÔNG tạo quy tắc mới, thiếu `standard_value` khi MAP → 422, `action` không hợp lệ → 422, xử lý mục không tồn tại → 404, xử lý lại mục đã RESOLVED → 422; bước 3: `apply_to_similar=true` áp dụng đồng loạt cho mục PENDING khác cùng giá trị (kể cả khác hoa/thường/khoảng trắng) nhưng CHỈ trong cùng `dataset_id`+`field_name`, chỉ tạo 1 quy tắc mới dù nhiều mục tương tự; vòng lặp khép kín: sau khi resolve, chạy 1 phiên UC-029→UC-031 MỚI với cùng giá trị nguồn → ánh xạ được ngay, `unmapped_values_count=0`. **Đã tự chạy**: `pytest services/data-quality-service -q` **58 passed** (45 test cũ UC-029/030/031 + `test_health` + 13 test mới UC-032, tất cả pass sau khi sửa bug `StaticPool`); `pytest services/ingestion-service -q` **171 passed** (không đổi, không sửa gì service này); `python3 -m py_compile` toàn bộ file Python thay đổi/mới (pass); `ast.parse` cho file Alembic mới (pass); kiểm tra cân bằng ngoặc `()[]{}` cho toàn bộ file JS/JSX thay đổi/mới (pass). Frontend: `frontend/src/api/unmappedQueue.js` (mới, 3 hàm — dùng chung `dataQualityClient`, không cần proxy dev mới); trang `/unmapped-queue` (`frontend/src/pages/dataquality/UnmappedQueuePage.jsx`, mới) — bộ lọc bước 1 (tập dữ liệu/trường/trạng thái, mặc định PENDING), bảng hàng đợi hiển thị trực tiếp form xử lý trên mỗi dòng PENDING (chọn hành động MAP/CREATE_NEW/REJECT, nhập giá trị chuẩn hoặc lý do, checkbox "Áp dụng cho các giá trị tương tự" mặc định bật = bước 3), hiển thị kết quả xử lý cho mục đã RESOLVED, thông báo số lượng mục đã áp dụng đồng loạt; cập nhật `frontend/src/App.jsx` (import `UnmappedQueuePage` + icon `Inbox`, thêm route `/unmapped-queue`, thêm thẻ module ở trang chủ sau thẻ "Phân tích PDF/bản quét + OCR") + `frontend/src/nav.js` (import icon `Inbox`, thêm mục "Xử lý hàng đợi chưa ánh xạ" vào nhóm "Chuẩn hóa và quản trị dữ liệu"). **Đã tự chạy `npm install && npm run build`** — biên dịch thành công (đã dọn `node_modules`/`dist` sau khi build thử). Không cần sửa `requirements.txt`/`Dockerfile`/`docker-compose.yml` (không thêm thư viện mới). **⚠️ Còn tồn đọng — cần bạn xác nhận trước khi chuyển `tested`**: sandbox không có Docker/Postgres thật nên chưa chạy được `docker compose exec data-quality-service alembic upgrade head` (áp migration `0004`) trên Postgres thật; bản sửa `session.py` (thêm `StaticPool`, chỉ áp dụng cho SQLite in-memory) nên được bạn rà lại 1 lượt vì là file hạ tầng dùng chung cho mọi UC của service.) |
| UC-033 | Quản lý danh mục đơn vị | Quản trị Danh mục | done (tiếp tục trong service `data-quality-service` (cùng service UC-029..032), vẫn đúng nhóm nghiệp vụ "CHUẨN HÓA VÀ QUẢN TRỊ DỮ LIỆU" nhưng khác nhóm chức năng: đây là DANH MỤC nghiệp vụ (đơn vị hành chính/tài chính gắn cho dữ liệu ngân sách/tài sản/văn bản đã chuẩn hoá — dùng cho UC-034..036/042..046), KHÔNG PHẢI "cơ cấu tổ chức" nội bộ hệ thống của UC-001 (`auth-identity-service`, dùng cho RBAC/gán người dùng — `frontend/src/pages/OrgUnitsPage.jsx`), nên không dùng lại bảng `org_units` sẵn có mà tạo bảng riêng `curated.org_unit_catalog`. Domain (`app/domain/entities.py`): entity `OrgUnitCatalogEntry` (cây phân cấp qua `parent_id` tự tham chiếu, `code` duy nhất toàn danh mục, `status`=ACTIVE/CLOSED, `version` tăng mỗi lần sửa, `effective_from`/`effective_to`, `lifecycle_action`=CLOSE/SPLIT/MERGE, `split_from_id`/`merged_from_ids` truy vết nguồn gốc sau tách/sáp nhập, phương thức `bump_version()`/`close()`), `OrgUnitCatalogVersion` (lịch sử phiên bản append-only, ghi mỗi lần thêm mới version=1 hoặc sửa/đóng/tách/sáp nhập); `app/domain/exceptions.py` (`OrgUnitCatalogNotFound`, `OrgUnitCatalogCodeAlreadyExists` — bước 2 "kiểm tra trùng mã", `InvalidOrgUnitCatalog`, `OrgUnitCatalogAlreadyClosed`, `InvalidOrgUnitCatalogLifecycle`); `app/domain/repositories.py` (interface `OrgUnitCatalogRepository` — `add`/`update`/`get_by_id`/`get_by_code`/`list` (lọc `parent_id`/`unit_type`/`status`, dùng sentinel `"__unset__"` để phân biệt "không lọc theo parent_id" với "chỉ lấy đơn vị gốc" (`parent_id=None`))/`list_all`, `OrgUnitCatalogVersionRepository`). Application: `app/application/use_cases/manage_org_unit_catalog.py` (`OrgUnitCatalogService`) — bước 1 `get_tree()` (dựng cây lồng nhau `OrgUnitTreeNode` từ `list_all()`, mặc định gồm cả đơn vị đã đóng để vẫn thấy dấu vết tách/sáp nhập), `list_versions()`; bước 2 `create_unit()` (kiểm tra `get_by_code()` trước khi tạo, validate `parent_id` tồn tại, tạo entry `version=1` + ghi 1 bản ghi `OrgUnitCatalogVersion`); bước 3 `update_unit()` (chặn sửa nếu `status=CLOSED`, `bump_version()` + ghi lịch sử phiên bản mới, `parent_id` dùng sentinel `"__unset__"` để phân biệt "giữ nguyên" với "đổi về gốc" (`None`), chặn tự làm cha của chính mình); bước 4 `close_unit()` (chặn đóng 2 lần, lưu `effective_to`), `split_unit()` (đơn vị gốc bị đóng với `effective_to=effective_from`, `lifecycle_action=SPLIT`; tạo ≥2 đơn vị mới cùng `parent_id` với gốc, `effective_from` chung, `split_from_id` trỏ về gốc; kiểm tra trùng mã cho TỪNG đơn vị mới trước khi tạo bất kỳ đơn vị nào), `merge_units()` (≥2 đơn vị nguồn đang ACTIVE bị đóng với `effective_to=effective_from`, `lifecycle_action=MERGE`; tạo 1 đơn vị mới với `merged_from_ids` trỏ về các nguồn, kiểm tra trùng mã đơn vị mới trước khi đóng bất kỳ nguồn nào). Infrastructure: `app/infrastructure/db/models.py` (`OrgUnitCatalogModel` bảng `org_unit_catalog` — unique index `code`, index `parent_id`/`status`; `OrgUnitCatalogVersionModel` bảng `org_unit_catalog_versions` — FK `unit_id`, cùng schema `curated`); `app/infrastructure/db/repository_impl.py` (`SqlAlchemyOrgUnitCatalogRepository`, `SqlAlchemyOrgUnitCatalogVersionRepository`, `merged_from_ids` lưu dạng JSON text). Interface: `app/interfaces/api/schemas.py` (`OrgUnitCatalogResponse`/`OrgUnitTreeNodeResponse` (đệ quy, có `model_rebuild()`)/`OrgUnitCatalogVersionResponse`/`OrgUnitCatalogCreate`/`OrgUnitCatalogUpdate` (có `clear_parent` để phân biệt "không đổi" với "đổi về gốc")/`CloseOrgUnitRequest`/`SplitOrgUnitRequest`+`SplitOrgUnitResponse`/`MergeOrgUnitRequest`+`MergeOrgUnitResponse`); `app/interfaces/api/org_unit_catalog_router.py` (mới, prefix `/org-unit-catalog`): `GET "/tree"` (bước 1, tham số `include_closed`), `GET ""` (lọc `parent_id`/`only_root`/`unit_type`/`status`), `GET "/{id}"`, `GET "/{id}/versions"`, `POST ""` (bước 2, 201, 409 nếu trùng mã, 422 nếu `unit_type`/`parent_id` không hợp lệ), `PUT "/{id}"` (bước 3, 404/422), `POST "/{id}/close"` (bước 4, 404/409 nếu đã đóng), `POST "/{id}/split"` (bước 4, 404/409/422), `POST "/merge"` (bước 4, 404/409/422); `app/main.py` đã `include_router`. Migration: `alembic/versions/0005_uc033_create_org_unit_catalog.py` (nối tiếp `0004`, tạo 2 bảng `org_unit_catalog`+`org_unit_catalog_versions` + index/unique constraint, `downgrade()` đầy đủ). Test: `tests/test_uc033_org_unit_catalog.py` (21 test case qua `TestClient`, SQLite in-memory) — bước 1: cây phân cấp có đúng quan hệ cha-con, lọc `parent_id`/`only_root`, 404 khi xem đơn vị không tồn tại; bước 2: tạo mới lưu đúng `version=1` + 1 bản ghi lịch sử, 409 khi trùng mã, 422 khi `unit_type` hoặc `parent_id` không hợp lệ; bước 3: sửa thông tin tăng `version` lên 2 + thêm bản ghi lịch sử thứ 2, 404 khi sửa đơn vị không tồn tại, 422 khi sửa đơn vị đã đóng, đổi `parent_id` thành công; bước 4: đóng lưu đúng `effective_to`+`lifecycle_action=CLOSE`, 409 khi đóng 2 lần, 404 khi đóng đơn vị không tồn tại; tách đóng đúng đơn vị gốc + tạo đúng số đơn vị mới với `effective_from`/`split_from_id`/`parent_id` đúng, 422 khi tách với <2 đơn vị mới hoặc đơn vị gốc đã đóng, 409 khi mã đơn vị mới trùng đơn vị đã có; sáp nhập đóng đúng các đơn vị nguồn + tạo đúng đơn vị mới với `effective_from`/`merged_from_ids` đúng, 422 khi <2 đơn vị nguồn, 404 khi đơn vị nguồn không tồn tại, 409 khi mã đơn vị mới trùng. Frontend: `frontend/src/api/orgUnitCatalog.js` (mới, 9 hàm — dùng chung `dataQualityClient` export từ `api/parsingJobs.js`, không cần proxy dev mới); trang `/org-unit-catalog` (`frontend/src/pages/dataquality/OrgUnitCatalogPage.jsx`, mới) — cột trái: cây phân cấp thu gọn/mở rộng theo từng đơn vị (bước 1, badge loại đơn vị/trạng thái/version, checkbox chọn nhiều đơn vị để sáp nhập), cột phải: panel thao tác chuyển đổi theo tab Thêm mới (bước 2)/Sửa (bước 3)/Đóng/Tách/Sáp nhập (bước 4) tuỳ theo đơn vị đang chọn, kèm bảng lịch sử phiên bản của đơn vị đang chọn; cập nhật `frontend/src/App.jsx` (import `OrgUnitCatalogPage` + icon `Network`, thêm thẻ module ở trang chủ sau thẻ "Xử lý hàng đợi chưa ánh xạ", thêm route `/org-unit-catalog`) + `frontend/src/nav.js` (thêm mục "Quản lý danh mục đơn vị" vào nhóm "Chuẩn hóa và quản trị dữ liệu", dùng lại icon `Network` đã import sẵn). Không cần sửa `requirements.txt`/`Dockerfile`/`docker-compose.yml` (không thêm thư viện mới). **⚠️ Chưa tự chạy được trong sandbox này (không có Internet để cài `fastapi`/`sqlalchemy`/`pytest`/`node_modules` — `pip install`/`npm install` đều báo lỗi không kết nối được registry)**: đã tự kiểm tra bằng `python3 -m py_compile` toàn bộ file Python mới/sửa (pass) và đối chiếu thủ công tên class/hàm import chéo giữa `repository_impl.py`/`org_unit_catalog_router.py`/`schemas.py` khớp nhau — nhưng CHƯA tự chạy được `pytest services/data-quality-service -q` để xác nhận 21 test mới thật sự pass (và không phá vỡ 58 test cũ UC-029..032), CHƯA chạy `npm run build`, CHƯA chạy `docker compose exec data-quality-service alembic upgrade head` (áp migration `0005`) trên Postgres thật. Cần bạn chạy các lệnh này để xác nhận trước khi chuyển `tested`.) |
| UC-034 | Quản lý danh mục khoản mục NSNN | Quản trị Danh mục | todo |
| UC-035 | Quản lý danh mục nhóm tài sản | Quản trị Danh mục | done (tiếp tục trong service `data-quality-service` (cùng service UC-029..034), theo đúng mẫu UC-033/034 (danh mục + quản lý phiên bản). Domain (`app/domain/entities.py`): entity `AssetGroupCatalogEntry` (nhóm tài sản cố định theo Thông tư 45/2018/TT-BTC sửa đổi TT162/2014/TT-BTC — gọi tắt TT48/TT162 theo `docs/use_cases.json`; `code` duy nhất toàn danh mục, `regulation` ∈ {TT45, TT162}, `useful_life_years` tham khảo, `version` tăng mỗi lần sửa, `status` ACTIVE/CLOSED, `bump_version()`), `AssetGroupCatalogVersion` (lịch sử phiên bản append-only, ghi mỗi lần thêm mới version=1 hoặc sửa), `AssetDepreciationRate` (bước 3 "Khai báo tỉ lệ khấu hao theo nhóm" — bản ghi append-only mỗi lượt khai báo, `depreciation_rate_percent` validate trong khoảng (0,100]); `app/domain/exceptions.py` thêm `AssetGroupNotFound`, `AssetGroupCodeAlreadyExists` (bước 2 "kiểm tra trùng mã"), `InvalidAssetGroup`, `InvalidAssetDepreciationRate`; `app/domain/repositories.py` thêm interface `AssetGroupCatalogRepository`, `AssetGroupCatalogVersionRepository`, `AssetDepreciationRateRepository`. Application: `app/application/use_cases/manage_asset_group_catalog.py` (`AssetGroupCatalogService`) — bước 1 `list_groups()`/`get()`/`list_versions()` (lọc `regulation`/`status`); bước 2 `create_group()` (kiểm tra trùng mã, lưu version=1 + ghi lịch sử), `update_group()` (chặn sửa nếu đã `CLOSED`, `useful_life_years="__unset__"` sentinel để phân biệt "giữ nguyên" với "xoá giá trị", `bump_version()` + ghi lịch sử phiên bản mới); bước 3 `declare_depreciation_rate()` (append-only, không ghi đè lượt khai báo trước — hệ thống lưu đúng nghĩa), `list_depreciation_rates()` (mới nhất trước), `get_current_depreciation_rate()` (lượt khai báo gần nhất). Infrastructure: `app/infrastructure/db/models.py` thêm `AssetGroupCatalogModel` (bảng `asset_group_catalog`, unique `code`), `AssetGroupCatalogVersionModel` (bảng `asset_group_catalog_versions`, FK `group_id`), `AssetDepreciationRateModel` (bảng `asset_depreciation_rates`, FK `asset_group_id`, cột `depreciation_rate_percent` kiểu `Float` — đã bổ sung import `Float` từ SQLAlchemy) — cùng schema `curated` như UC-029..034; `app/infrastructure/db/repository_impl.py` thêm `SqlAlchemyAssetGroupCatalogRepository`, `SqlAlchemyAssetGroupCatalogVersionRepository`, `SqlAlchemyAssetDepreciationRateRepository`. Interface: `app/interfaces/api/schemas.py` thêm `AssetGroupCatalogResponse`/`AssetGroupCatalogVersionResponse`/`AssetGroupCatalogCreate`/`AssetGroupCatalogUpdate` (có `clear_useful_life_years` để phân biệt "không đổi" với "xoá giá trị", theo đúng mẫu `clear_parent` của UC-033)/`AssetDepreciationRateResponse`/`AssetDepreciationRateDeclare`; `app/interfaces/api/asset_group_catalog_router.py` (mới, prefix `/asset-group-catalog`): `GET ""` (bước 1, lọc `regulation`/`status`), `GET "/{id}"`, `GET "/{id}/versions"`, `POST ""` (bước 2, 201, 409 nếu trùng mã, 422 nếu `regulation` không hợp lệ), `PUT "/{id}"` (bước 2, 404/422, 422 nếu đã đóng), `POST "/{id}/depreciation-rates"` (bước 3, 201, 404 nếu nhóm không tồn tại, 422 nếu tỉ lệ ngoài (0,100]), `GET "/{id}/depreciation-rates"` (lịch sử mới nhất trước), `GET "/{id}/depreciation-rates/current"` (lượt gần nhất, trả `null` nếu chưa khai báo); `app/main.py` đã `include_router`. Migration: `alembic/versions/0007_uc035_create_asset_group_catalog.py` (nối tiếp `0006` → `down_revision="0006"`, tạo 3 bảng `asset_group_catalog`/`asset_group_catalog_versions`/`asset_depreciation_rates` + index/unique constraint trong schema `curated`, `downgrade()` đầy đủ). Test: `tests/test_uc035_asset_group_catalog.py` (20 test case qua `TestClient`, SQLite in-memory) — bước 1: hiển thị danh sách, lọc theo `regulation`/`status`, 404 khi xem nhóm không tồn tại; bước 2: tạo mới lưu đúng `version=1` + 1 bản ghi lịch sử, 409 khi trùng mã, 422 khi `regulation` không hợp lệ, sửa thông tin tăng `version` lên 2 + thêm bản ghi lịch sử thứ 2, 404 khi sửa nhóm không tồn tại, 422 khi sửa nhóm đã đóng, xoá `useful_life_years` qua `clear_useful_life_years` và giữ nguyên khi không truyền; bước 3: khai báo tỉ lệ khấu hao lưu đúng dữ liệu, 404 khi nhóm không tồn tại, 422 khi tỉ lệ vượt 100, danh sách khai báo append-only mới nhất trước, lấy đúng tỉ lệ hiện hành (lượt khai báo gần nhất) + trả `null` khi chưa từng khai báo. Frontend: `frontend/src/api/assetGroupCatalog.js` (mới, 8 hàm — dùng chung `dataQualityClient` export từ `api/parsingJobs.js`, không cần proxy dev mới); trang `/asset-group-catalog` (`frontend/src/pages/dataquality/AssetGroupCatalogPage.jsx`, mới) — bố cục master-detail: cột trái bảng danh mục (lọc theo văn bản căn cứ/trạng thái, bước 1), cột phải panel thao tác theo tab Thêm mới (bước 2)/Sửa (bước 2)/Khai báo tỉ lệ khấu hao (bước 3) tuỳ theo nhóm đang chọn, kèm bảng lịch sử tỉ lệ khấu hao đã khai báo + bảng lịch sử phiên bản của nhóm đang chọn; cập nhật `frontend/src/App.jsx` (import `AssetGroupCatalogPage` + icon `Percent`, thêm thẻ module ở trang chủ sau thẻ "Danh mục khoản mục NSNN", thêm route `/asset-group-catalog`) + `frontend/src/nav.js` (import icon `Percent`, thêm mục "Quản lý danh mục nhóm tài sản" vào nhóm "Chuẩn hóa và quản trị dữ liệu" ngay sau "Danh mục khoản mục NSNN"). Không cần sửa `requirements.txt`/`Dockerfile`/`docker-compose.yml` (không thêm thư viện mới — `Float` đã có sẵn trong `sqlalchemy`). **⚠️ Chưa tự chạy được trong sandbox này (mạng bị tắt theo cấu hình — `pip install`/`npm install` đều không kết nối được registry)**: đã tự kiểm tra bằng `python3 -m py_compile` toàn bộ file Python mới/sửa (pass), `ast.parse` cho file Alembic mới (pass), kiểm tra chuỗi `down_revision` `0007→0006→...→0001` khớp đúng, kiểm tra cân bằng ngoặc `()[]{}` cho `App.jsx`/`nav.js`/`api/assetGroupCatalog.js`/`AssetGroupCatalogPage.jsx` (pass) — nhưng CHƯA tự chạy được `pytest services/data-quality-service -q` để xác nhận 20 test mới thật sự pass (và không phá vỡ các test UC-029..034 hiện có), CHƯA chạy `npm run build`, CHƯA chạy `docker compose exec data-quality-service alembic upgrade head` (áp migration `0007`) trên Postgres thật. Cần bạn chạy các lệnh này để xác nhận trước khi chuyển `tested`.) |
| UC-036 | Quản lý danh mục mặt hàng, loại văn bản, nguồn vốn | Quản trị Danh mục | todo |
| UC-037 | Phê duyệt thay đổi danh mục nhạy cảm | Lãnh đạo Phòng nghiệp vụ Sở Tài chính | done (tiếp tục trong service `data-quality-service` (cùng service UC-029..036), nối tiếp UC-036 bước 3 "Đề nghị thay đổi danh mục nhạy cảm" — tái sử dụng NGUYÊN VẸN `CatalogEntryService.propose_change()/approve_change()/reject_change()` đã có sẵn từ UC-036, KHÔNG viết lại logic áp dụng thay đổi; UC-037 chỉ bổ sung tầng nghiệp vụ riêng: bắt buộc lý do phê duyệt, dựng + hiển thị diff, và ghi nhật ký append-only. Domain (`app/domain/entities.py`): entity mới `CatalogChangeAuditLog` (nhật ký append-only — `action`=APPROVED/REJECTED, `decided_by`, `decision_reason` BẮT BUỘC không được rỗng — validate ngay ở `__post_init__`, `diff_snapshot` JSON text chụp lại diff tại đúng thời điểm quyết định); `app/domain/exceptions.py` thêm `InvalidCatalogChangeApproval`; `app/domain/repositories.py` thêm interface `CatalogChangeAuditLogRepository` (`add`/`list`, KHÔNG có `update`/`delete` — đúng tính chất nhật ký append-only). Application (`app/application/use_cases/approve_catalog_change.py`, mới): `CatalogChangeApprovalService` nhận vào 1 `CatalogEntryService` (composition, không kế thừa) + `CatalogChangeAuditLogRepository` — bước 1 `list_pending_requests()` (lọc `status=PENDING`, tuỳ chọn `catalog_type`); bước 2 `build_diff()`/`get_diff()` (so sánh giá trị HIỆN TẠI của `CatalogEntry` với từng trường `proposed_*` của `CatalogChangeRequest`, CHỈ trả về trường thực sự được đề nghị đổi — bỏ qua trường `None`, kèm cờ `changed` mỗi trường); bước 3+4 `approve()` (validate `reason` không rỗng → chụp diff TRƯỚC khi áp dụng → gọi `CatalogEntryService.approve_change()` có sẵn từ UC-036 để áp dụng thay đổi + tăng version + ghi lịch sử phiên bản → ghi 1 `CatalogChangeAuditLog`), `reject()` (validate `reason` → gọi `reject_change()` có sẵn, KHÔNG áp dụng thay đổi → vẫn ghi 1 `CatalogChangeAuditLog`); bước 5 `list_audit_logs()` (lọc `request_id`/`entry_id`/`catalog_type`/`action`). Interface: `app/interfaces/api/schemas.py` thêm `CatalogChangeDiffFieldResponse`/`CatalogChangeDiffResponse`/`CatalogChangeApprovalDecision` (khác `CatalogChangeReviewRequest` của UC-036 ở chỗ `reason: str = Field(min_length=1, ...)` BẮT BUỘC, tự động trả 422 nếu thiếu/rỗng qua Pydantic — không cần validate tay ở router)/`CatalogChangeAuditLogResponse`/`CatalogChangeApprovalResultResponse`/`CatalogChangeRejectionResultResponse`; `app/interfaces/api/catalog_change_approval_router.py` (mới, prefix `/catalog-change-approvals`): `GET "/pending"` (bước 1, lọc `catalog_type`), `GET "/{request_id}/diff"` (bước 2, 404 nếu yêu cầu không tồn tại), `POST "/{request_id}/approve"` (bước 3+4+5, 404/422 — 422 khi yêu cầu đã xử lý trước đó hoặc thiếu lý do), `POST "/{request_id}/reject"` (bước 3+5, 404/422), `GET "/audit-logs"` (bước 5, lọc `request_id`/`entry_id`/`catalog_type`/`action`); `app/main.py` đã `include_router`. Infrastructure: `app/infrastructure/db/models.py` thêm `CatalogChangeAuditLogModel` (bảng `catalog_change_audit_logs`, FK `request_id`→`catalog_change_requests.id` + FK `entry_id`→`catalog_entries.id`, cùng schema `curated`); `app/infrastructure/db/repository_impl.py` thêm `SqlAlchemyCatalogChangeAuditLogRepository`. Migration: `alembic/versions/0009_uc037_catalog_change_audit_logs.py` (nối tiếp `0008` UC-036 → `down_revision="0008"`, tạo bảng `catalog_change_audit_logs` + 4 index (`request_id`/`entry_id`/`catalog_type`/`action`) trong schema `curated`, `downgrade()` đầy đủ). Test: `tests/test_uc037_catalog_change_approval.py` (14 test case qua `TestClient`, SQLite in-memory, **đã tự chạy pytest và PASS**) — bước 1: hiển thị yêu cầu chờ duyệt, lọc theo `catalog_type`, danh sách chờ duyệt KHÔNG còn chứa yêu cầu đã xử lý; bước 2: diff hiển thị đúng CHỈ các trường được đề nghị đổi (kèm `old_value`/`new_value`/`changed`), bỏ qua trường không đổi (`unit` không xuất hiện khi chỉ đề nghị đổi `name`+`description`), 404 khi yêu cầu không tồn tại; bước 3+4: duyệt áp dụng đúng thay đổi vào `CatalogEntry` (tăng version, yêu cầu chuyển `APPROVED`), 422 khi thiếu lý do, 404 khi yêu cầu không tồn tại, 422 khi duyệt lại yêu cầu đã xử lý; từ chối KHÔNG áp dụng thay đổi (giữ nguyên `name`+`version`), 422 khi thiếu lý do; bước 5: duyệt/từ chối đều ghi đúng 1 bản ghi nhật ký kèm `decision_reason` + `diff_snapshot` (chứa giá trị đề nghị), tra cứu nhật ký lọc theo `request_id`/`entry_id`/`catalog_type`/`action` đúng. **Đã tự cài đặt `fastapi`/`sqlalchemy`/`pydantic`/`pytest`/`httpx` qua `pip install --break-system-packages` (khác các lượt UC trước — lần này sandbox có Internet) và TỰ CHẠY**: `pytest services/data-quality-service -q` **151 passed, 5 failed** — 5 test fail đều thuộc `tests/test_uc033_org_unit_catalog.py` (tách/sáp nhập đơn vị), đã xác nhận đây là lỗi CÓ SẴN TỪ TRƯỚC (không liên quan UC-037): chạy riêng `pytest tests/test_uc033_org_unit_catalog.py -q` cũng cho đúng kết quả 5 failed/17 passed dù không đụng tới bất kỳ file nào của UC-033 trong lượt này — cần rà soát riêng ở lượt sau, KHÔNG chặn UC-037; `python3 -m py_compile` toàn bộ file Python mới/sửa (pass); `ast.parse` cho file Alembic mới (pass). Frontend: `frontend/src/api/catalogChangeApprovals.js` (mới, 5 hàm — dùng chung `dataQualityClient` export từ `api/parsingJobs.js`, không cần proxy dev mới); trang `/catalog-change-approvals` (`frontend/src/pages/dataquality/CatalogChangeApprovalsPage.jsx`, mới) — cột trái: danh sách yêu cầu chờ duyệt (bước 1, lọc theo danh mục); cột phải: bảng diff (bước 2, tô màu đỏ giá trị cũ/xanh giá trị mới), form phê duyệt/từ chối bắt buộc nhập người duyệt + lý do (bước 3+4+5), danh sách nhật ký phê duyệt của yêu cầu đang xem (bước 5); cập nhật `frontend/src/App.jsx` (import `CatalogChangeApprovalsPage`, thêm route `/catalog-change-approvals`, thêm thẻ module ở trang chủ sau thẻ UC-036 dùng icon `ClipboardCheck` đã import sẵn) + `frontend/src/nav.js` (thêm mục "Phê duyệt thay đổi danh mục nhạy cảm" vào nhóm "Chuẩn hóa và quản trị dữ liệu" ngay sau UC-036, dùng lại icon `ClipboardCheck` đã import sẵn). **Đã tự chạy `npm install && npm run build`** — biên dịch thành công, không lỗi (đã dọn `node_modules`/`dist` sau khi build thử). Không cần sửa `requirements.txt`/`Dockerfile`/`docker-compose.yml` (không thêm thư viện mới). **⚠️ Còn tồn đọng — cần bạn xác nhận trước khi chuyển `tested`**: sandbox không có Docker/Postgres thật nên chưa chạy được `docker compose exec data-quality-service alembic upgrade head` (áp migration `0009`) trên Postgres thật; chưa tự kiểm thử qua UI thật (chỉ mới build thành công); đề nghị bạn tiện thể rà lại 5 test fail có sẵn của UC-033 nêu trên khi có dịp.) |
| UC-038 | Quản lý quy tắc kiểm tra chất lượng | Phụ trách Dữ liệu, Quản trị Dữ liệu | done (tiếp tục trong service `data-quality-service` (cùng service UC-029..037). Domain (`app/domain/entities.py`): entity mới `QualityRule` (`field_names: List[str]`, `rule_type` thuộc 4 giá trị `COMPLETENESS`(đầy đủ)/`VALIDITY`(hợp lệ)/`UNIQUENESS`(duy nhất)/`CONSISTENCY`(nhất quán) -- validate `VALIDITY` bắt buộc `params` có ít nhất 1 trong `regex`/`allowed_values`/`min_value`/`max_value`, `CONSISTENCY` bắt buộc `params.expression`, `dataset_id=None` = quy tắc CHUNG áp dụng mọi tập dữ liệu, `weight` trọng số giữa các quy tắc CÙNG loại khi tổng hợp điểm, `version` tăng mỗi lần sửa (`bump_version()`)); `QualityRuleVersion` (lịch sử append-only); `QualityScoreConfig` (bước 3 "Cấu hình ngưỡng + trọng số cho điểm" -- `pass_threshold` [0,100], `rule_type_weights: Dict[str,float]` trọng số THEO NHÓM loại quy tắc dùng để tổng hợp điểm cuối (khác `QualityRule.weight` là trọng số GIỮA các quy tắc trong CÙNG 1 nhóm), `dataset_id=None` = cấu hình MẶC ĐỊNH, unique theo `dataset_id`); `QualityScoreConfigVersion` (lịch sử append-only). `app/domain/exceptions.py` thêm `QualityRuleNotFound`/`InvalidQualityRule`/`QualityScoreConfigNotFound`/`InvalidQualityScoreConfig`. `app/domain/repositories.py` thêm 4 interface `QualityRuleRepository`/`QualityRuleVersionRepository`/`QualityScoreConfigRepository`/`QualityScoreConfigVersionRepository`. Application (`app/application/use_cases/manage_quality_rule.py`, mới): `QualityRuleService` -- bước 1 `list_rules(dataset_id, rule_type, is_active)` (đọc `metadata.quality_rules`, lọc theo `rule_type` để xem riêng đầy đủ/hợp lệ/duy nhất/nhất quán); bước 2 `create_rule()`/`update_rule()` (hệ thống lưu vào `metadata.quality_rules` + version, tăng version + ghi lịch sử `QualityRuleVersion` qua `_record_rule_version()`, `update_rule()` tái chạy `__post_init__()` để validate lại toàn bộ sau khi sửa); bước 3 `save_score_config()` (upsert theo `dataset_id` -- tạo mới version=1 nếu chưa có, ngược lại cập nhật + tăng version + ghi lịch sử `QualityScoreConfigVersion`), `get_score_config(dataset_id)`/`get_score_config_by_id()`/`list_score_configs()`/`list_score_config_versions()`. Kết quả (`QualityRule`+`QualityScoreConfig` đang `is_active=True`) sẽ được UC-039 "Chạy kiểm tra chất lượng dữ liệu" đọc lại để chạy quy tắc + tính điểm + so ngưỡng. Interface: `app/interfaces/api/schemas.py` thêm `QualityRuleResponse`/`QualityRuleVersionResponse`/`QualityRuleCreate`/`QualityRuleUpdate`/`QualityScoreConfigResponse`/`QualityScoreConfigVersionResponse`/`QualityScoreConfigSave`; `app/interfaces/api/quality_rule_router.py` (mới, prefix `/quality-rules`): `GET ""` (bước 1, lọc `dataset_id`/`rule_type`/`is_active`), `GET "/{rule_id}"`, `GET "/{rule_id}/versions"`, `POST ""` (bước 2 thêm, 422 `INVALID_QUALITY_RULE`), `PUT "/{rule_id}"` (bước 2 sửa, 404/422), `GET "/score-configs/list"`, `GET "/score-configs/by-dataset"` (query `dataset_id` tuỳ chọn, 404 `QUALITY_SCORE_CONFIG_NOT_FOUND` nếu chưa có), `GET "/score-configs/{config_id}"`, `GET "/score-configs/{config_id}/versions"`, `PUT "/score-configs"` (bước 3, upsert theo `dataset_id` trong body, 422 `INVALID_QUALITY_SCORE_CONFIG`) -- các route `/score-configs/...` có 2 đoạn path nên KHÔNG xung đột với route `/{rule_id}` (1 đoạn path, cùng pattern đã dùng ở `catalog_entry_router.py`/`change-requests`); `app/main.py` đã `include_router`. Infrastructure: `app/infrastructure/db/models.py` thêm `QualityRuleModel`(bảng `quality_rules`, lưu `field_names`/`params` dạng JSON text)/`QualityRuleVersionModel`(bảng `quality_rule_versions`)/`QualityScoreConfigModel`(bảng `quality_score_configs`, `UniqueConstraint(dataset_id)`)/`QualityScoreConfigVersionModel`(bảng `quality_score_config_versions`), cùng schema `curated`; `app/infrastructure/db/repository_impl.py` thêm 4 Sql impl tương ứng + helper `_quality_rule_to_entity()`/`_quality_score_config_to_entity()`. Migration: `alembic/versions/0010_uc038_create_quality_rules.py` (nối tiếp `0009` UC-037 → `down_revision="0009"`, tạo 4 bảng + index + unique constraint `dataset_id` của `quality_score_configs`, `downgrade()` đầy đủ). Test: `tests/test_uc038_quality_rules.py` (23 test case qua `TestClient`, SQLite in-memory) -- bước 1: hiển thị danh sách, lọc theo `rule_type`/`dataset_id`/`is_active`, 404 khi không tồn tại; bước 2: tạo `COMPLETENESS`/`UNIQUENESS` thành công version=1 + lưu lịch sử, `VALIDITY` bắt buộc có `params` (422 nếu rỗng, thành công với `regex`), `CONSISTENCY` bắt buộc `params.expression` (422 nếu thiếu), 422 khi `field_names` rỗng, sửa quy tắc tăng version + ghi lịch sử đúng `change_note`, 404/422 khi sửa quy tắc không tồn tại/`weight` không hợp lệ; bước 3: tạo mới cấu hình điểm version=1, cập nhật tăng version (giữ nguyên `id`) + lưu đúng lịch sử 2 bản ghi, cấu hình mặc định khi không truyền `dataset_id`, 404 khi tra cứu cấu hình dataset chưa có, 422 khi `pass_threshold` ngoài [0,100] hoặc `rule_type_weights` có khoá không hợp lệ, xem danh sách toàn bộ cấu hình. **CHƯA tự chạy được `pytest`**: sandbox lượt này KHÔNG có Internet (`pip install` báo `No matching distribution found` cho `fastapi`/`pytest`, khác lượt UC-037 trước đó) nên không cài được `fastapi`/`sqlalchemy`/`pydantic`/`pytest`/`httpx` để chạy `pytest services/data-quality-service -q`; đã bù bằng: `python3 -m py_compile` toàn bộ file Python mới/sửa (pass, không lỗi cú pháp) + rà tay kỹ theo đúng pattern đã PASS của UC-036/037 (`CatalogEntryService`/`catalog_entry_router.py`) về cấu trúc version/lịch sử/409-404-422. Frontend: `frontend/src/api/qualityRules.js` (mới, dùng chung `dataQualityClient`) — `QUALITY_RULE_TYPES`, `listQualityRules`/`getQualityRule`/`listQualityRuleVersions`/`createQualityRule`/`updateQualityRule`/`saveQualityScoreConfig`/`getQualityScoreConfigByDataset`/`listQualityScoreConfigs`/`listQualityScoreConfigVersions`; trang `/quality-rules` (`frontend/src/pages/dataquality/QualityRulesPage.jsx`, mới) — tab lọc theo 4 loại quy tắc, cột trái: danh sách quy tắc (lọc thêm `dataset_id`/trạng thái), cột phải: form thêm/sửa quy tắc (trường `params` động theo `rule_type` -- regex/allowed_values/min/max cho `VALIDITY`, expression cho `CONSISTENCY`) + lịch sử phiên bản; khối dưới cùng: form "Cấu hình ngưỡng + trọng số cho điểm" theo `dataset_id` (ngưỡng đạt + 4 trọng số theo nhóm loại quy tắc) + lịch sử cấu hình điểm; cập nhật `frontend/src/App.jsx` (thêm icon `Gauge`, import `QualityRulesPage`, thêm thẻ module ở trang chủ sau thẻ UC-037, thêm route `/quality-rules`) + `frontend/src/nav.js` (thêm icon `Gauge`, thêm mục "Quản lý quy tắc kiểm tra chất lượng" vào nhóm "Chuẩn hóa và quản trị dữ liệu" ngay sau UC-037). **CHƯA tự chạy được `npm install`/`npm run build`** (cùng lý do không có Internet); đã bù bằng kiểm tra cú pháp: `node --check` cho `qualityRules.js` (pass) + script Python đếm cân bằng ngoặc `()`/`{}`/`[]` cho `QualityRulesPage.jsx`/`App.jsx`/`nav.js`/`qualityRules.js` (tất cả cân bằng, không lệch). Không cần sửa `requirements.txt`/`Dockerfile`/`docker-compose.yml`/`package.json` (không thêm thư viện mới). **⚠️ Còn tồn đọng — cần bạn xác nhận/tự chạy khi có Internet trước khi chuyển `tested`**: `pytest services/data-quality-service -q` (đặc biệt `tests/test_uc038_quality_rules.py`, 23 test case mới) và `cd frontend && npm install && npm run build`; sau đó `docker compose exec data-quality-service alembic upgrade head` để áp migration `0010` trên Postgres thật; nhắc lại tồn đọng cũ từ UC-037: 5 test fail có sẵn ở `tests/test_uc033_org_unit_catalog.py` chưa được rà soát.) |
| UC-039 | Chạy kiểm tra chất lượng dữ liệu | Hệ thống tự động (Quality Service) | done (backend domain/application/infrastructure của UC-039 đã tồn tại sẵn từ trước (`app/domain/entities.py::QualityCheckJob/QualityCheckRuleResult/QualityPublishedRecord/QualityExceptionQueueItem`, `app/application/use_cases/run_quality_check.py::QualityCheckService`, migration `alembic/versions/0011_uc039_create_quality_check_tables.py`, test `tests/test_uc039_quality_check.py`) nhưng CHƯA hoạt động được — lượt này rà soát lại theo mục 1 RULE.md và phát hiện + sửa 4 lỗi có sẵn chặn UC-039 chạy đúng: (1) `app/interfaces/api/quality_check_router.py` bị COPY NHẦM nguyên nội dung của `quality_rule_router.py` (cùng prefix `/quality-rules`, không có endpoint `/quality-checks` nào cả) — viết lại đúng router UC-039, prefix `/quality-checks`: `POST \"\"` (nhận sự kiện `mapping.completed` mô phỏng, chạy trọn luồng UC-039, 404 `NO_STANDARD_RECORDS_TO_CHECK` nếu `mapping_job_id` chưa có bản ghi chuẩn hoá), `GET \"\"` (danh sách, lọc `dataset_id`/`mapping_job_id`/`status`), `GET \"/{id}\"`, `GET \"/{id}/rule-results\"`, `GET \"/{id}/published-records\"`, `GET \"/{id}/exception-items\"`, `GET \"/exception-queue/list\"` (UC-040 bước 1 xem hàng đợi toàn bộ, lọc `dataset_id`/`status`, đặt TRƯỚC route động `/{id}` cùng lý do đã ghi chú ở UC-038); (2) `app/interfaces/api/quality_rule_router.py` (lỗi có sẵn từ UC-038): route tĩnh `PUT \"/score-configs\"` bị đăng ký SAU route động `PUT \"/{rule_id}\"` nên bị "nuốt" mất (đúng như comment cảnh báo NGAY TRONG file nhưng không được tuân theo lúc viết) — di chuyển `PUT \"/score-configs\"` lên trước `PUT \"/{rule_id}\"`; (3) `_load_applicable_rules()` ở `run_quality_check.py` gọi `quality_rule_repo.list(dataset_id=None, is_active=True)` để lấy "quy tắc CHUNG", nhưng `SqlAlchemyQualityRuleRepository.list()` coi `dataset_id=None` là "KHÔNG lọc" (trả về TẤT CẢ quy tắc mọi dataset) — bổ sung interface method mới `QualityRuleRepository.list_general(is_active)` lọc đúng `dataset_id IS NULL` + implement `SqlAlchemyQualityRuleRepository.list_general()`, sửa `_load_applicable_rules()` gọi hàm mới; (4) logic ưu tiên quy tắc riêng khi trùng khoá `(rule_type, field_names)` với quy tắc chung chỉ xét quy tắc riêng ĐANG BẬT (`is_active=True`) để tính khoá ghi đè, nên 1 quy tắc riêng ĐANG TẮT (chủ ý "dataset này không dùng quy tắc chung loại này") không chặn được quy tắc chung — sửa lấy TOÀN BỘ quy tắc riêng (kể cả tắt) để tính khoá ghi đè, chỉ dùng quy tắc riêng ĐANG BẬT để đánh giá. Thêm `tests/conftest.py` (mới) — fixture `_reset_db_per_module` (autouse, scope="module") reset schema SQLite TRƯỚC MỖI FILE test, tránh quy tắc chất lượng CHUNG tạo ở `test_uc038_quality_rules.py` rò rỉ sang `test_uc039_quality_check.py` chạy sau (do `StaticPool` dùng chung 1 CSDL `:memory:` xuyên suốt tiến trình pytest — lỗi cô lập test có sẵn, không phải lỗi nghiệp vụ). **Đã tự cài `fastapi`/`sqlalchemy`/`pydantic`/`pytest`/`httpx` (sandbox lượt này có Internet) và TỰ CHẠY**: `pytest services/data-quality-service -q` → **182 passed, 5 failed** — 5 fail đều thuộc `tests/test_uc033_org_unit_catalog.py`, XÁC NHẬN LẠI đây là tồn đọng CÓ SẴN TỪ TRƯỚC UC-037 (không liên quan thay đổi lượt này); riêng `tests/test_uc039_quality_check.py` (10 test) + `tests/test_uc038_quality_rules.py` (23 test) chạy độc lập và cùng nhau đều **PASS 100%**. Frontend (mới): `frontend/src/api/qualityChecks.js` — `runQualityCheck`/`listQualityChecks`/`getQualityCheck`/`listQualityCheckRuleResults`/`listQualityCheckPublishedRecords`/`listQualityCheckExceptionItems`/`listQualityExceptionQueue`, dùng chung `dataQualityClient`; trang `/quality-checks` (`frontend/src/pages/dataquality/QualityChecksPage.jsx`, mới) — chọn tập dữ liệu, chọn 1 phiên ánh xạ (UC-031) đã "Hoàn tất" để mô phỏng gửi sự kiện `mapping.completed` (nút kích hoạt thủ công do actor là "Hệ thống tự động", không có form nhập liệu nghiệp vụ), hiển thị kết quả lượt kiểm tra (trạng thái/điểm tổng hợp/điểm theo nhóm loại quy tắc/kết quả từng quy tắc bước 2), bản ghi đã công bố (bước 3a) hoặc dòng đẩy hàng đợi ngoại lệ kèm lý do (bước 3b), khối hàng đợi ngoại lệ đang chờ của cả dataset (tham chiếu nhanh, xử lý đầy đủ thuộc UC-040), và lịch sử các lượt kiểm tra; cập nhật `frontend/src/App.jsx` (thêm icon `BadgeCheck`, import `QualityChecksPage`, thêm thẻ module ở trang chủ sau thẻ UC-038, thêm route `/quality-checks`) + `frontend/src/nav.js` (thêm icon `BadgeCheck`, thêm mục "Chạy kiểm tra chất lượng dữ liệu" vào nhóm "Chuẩn hóa và quản trị dữ liệu" ngay sau UC-038). **Đã tự chạy `npm install && npm run build`** — biên dịch thành công (`vite build` không lỗi, cảnh báo chunk >500kB chỉ là cảnh báo hiệu năng chung của toàn app, không phải lỗi). Không cần sửa `requirements.txt`/`Dockerfile`/`docker-compose.yml`/`package.json` (không thêm thư viện mới). **⚠️ Còn tồn đọng — cần bạn xác nhận trước khi coi là hoàn tất trên môi trường thật**: sandbox không có Docker/Postgres thật nên chưa chạy được `docker compose exec data-quality-service alembic upgrade head` (áp migration `0011`, đã có sẵn từ trước) trên Postgres thật; chưa tự kiểm thử qua UI thật (chỉ mới build thành công + chạy `pytest` qua API); nhắc lại tồn đọng CŨ (không phải do lượt này): 5 test fail có sẵn ở `tests/test_uc033_org_unit_catalog.py` vẫn CHƯA được rà soát — đề nghị ưu tiên xử lý ở lượt kế tiếp trước khi làm UC-040.) |
| UC-040 | Xử lý ngoại lệ chất lượng | Phụ trách Dữ liệu | done (tiếp tục trong service `data-quality-service` (cùng service UC-029..039), đọc tiếp hàng đợi `quality_exception_queue` do UC-039 bước 3b đẩy vào. Domain (`app/domain/entities.py::QualityExceptionQueueItem`, sửa tại chỗ — trước đây chỉ có `STATUSES=(PENDING,RESOLVED)` không có cơ chế lưu quyết định xử lý): thêm `RESOLUTION_ACTIONS=(FIX,REJECT,REQUEST_SOURCE)` + 4 trường mới `resolution_action`/`corrected_fields`/`resolution_reason`/`resolved_at`; thêm method `failed_rule_types()` (danh sách không trùng các `rule_type` dòng này không đạt, dùng ở bước 3 để nhóm "cùng loại"); thêm method `resolve(action, corrected_fields, reason, resolved_at)` — `FIX` bắt buộc `corrected_fields` (merge vào `standardized_fields` hiện có của dòng), `REJECT`/`REQUEST_SOURCE` bắt buộc `reason`, chặn xử lý lại dòng đã `RESOLVED` (cùng tinh thần `UnmappedQueueItem.resolve()` của UC-032). `app/domain/exceptions.py` thêm `QualityExceptionQueueItemNotFound`/`InvalidQualityExceptionResolution`/`NoMatchingExceptionItemsForBatch`. `app/domain/repositories.py` bổ sung `get_by_id()`/`update()` vào interface `QualityExceptionQueueRepository` (trước đây chỉ có `add_many`/`list_for_job`/`list_queue`, đủ cho UC-039 ghi nhưng UC-040 cần đọc/ghi lại 1 dòng cụ thể). Application (`app/application/use_cases/resolve_quality_exception.py`, mới): `QualityExceptionService` — bước 1 `list_queue(dataset_id, status="PENDING")` (đọc `quality_exception_queue`, không giới hạn theo 1 lượt kiểm tra); bước 2 `resolve_item(item_id, action, corrected_fields, reason)` — `FIX` gọi `item.resolve()` rồi công bố dòng đã sửa vào kho chuẩn hoá (tạo `QualityPublishedRecord` qua `QualityPublishedRecordRepository.add_many()` + phát sự kiện `curated.publish.requested`, CÙNG sự kiện UC-039 bước 3a dùng để UC-041 đọc tiếp, payload thêm `source="uc040_exception_fix"` để phân biệt nguồn gốc), `REJECT` chỉ lưu quyết định (không công bố, không phát sự kiện — cùng tinh thần UC-032 action `REJECT`), `REQUEST_SOURCE` phát sự kiện mới `quality.exception.source_requested` (payload `dataset_id`/`quality_exception_item_ids`/`row_indices`/`reason`, cho ingestion-service/đơn vị nguồn đọc tiếp yêu cầu nộp lại dữ liệu); bước 3 `resolve_batch(dataset_id, rule_type, action, corrected_fields, reason)` — lấy toàn bộ dòng PENDING của `dataset_id` (`list_queue`), lọc còn lại các dòng có `rule_type` khớp `failed_rule_types()`, 422 `NO_MATCHING_EXCEPTION_ITEMS_FOR_BATCH` nếu rỗng, áp dụng ĐỒNG LOẠT cùng 1 quyết định (action/corrected_fields/reason) cho toàn bộ dòng khớp, gộp phát 1 sự kiện `curated.publish.requested` (record_count=tổng số dòng FIX) hoặc 1 sự kiện `quality.exception.source_requested` (item_ids=toàn bộ) thay vì phát riêng từng dòng. Interface: `app/interfaces/api/schemas.py` mở rộng `QualityExceptionQueueItemResponse` (thêm 4 trường mới) + thêm `ResolveQualityExceptionRequest`/`Response`, `ResolveQualityExceptionBatchRequest`/`Response`; `app/interfaces/api/quality_exception_router.py` (mới, prefix `/quality-exceptions`): `GET ""` (bước 1, mặc định `status=PENDING`, lọc `dataset_id`), `GET "/{item_id}"` (404 nếu không có), `POST "/{item_id}/resolve"` (bước 2, 404/422), `POST "/batch-resolve"` (bước 3, 422 khi action sai hoặc không có dòng khớp) — route tĩnh `/batch-resolve` đặt SAU route `GET "/{item_id}"` nhưng dùng method `POST` nên không xung đột (khác cảnh báo thứ tự route tĩnh/động đã gặp ở UC-038/039, ở đây method khác nhau nên FastAPI phân biệt được theo verb, không cần đổi thứ tự); `app/main.py` đã `include_router`. Infrastructure: `app/infrastructure/db/models.py::QualityExceptionQueueItemModel` thêm 4 cột `resolution_action`(String 20)/`corrected_fields_json`(Text, nullable)/`resolution_reason`(Text, nullable)/`resolved_at`(String 40, nullable); `app/infrastructure/db/repository_impl.py::SqlAlchemyQualityExceptionQueueRepository` cập nhật `add_many()`/`_to_entity()` đọc/ghi 4 cột mới, thêm `get_by_id()`/`update()`. Migration: `alembic/versions/0012_uc040_quality_exception_resolution.py` (nối tiếp `0011` UC-039 → `down_revision="0011"`, 4 `op.add_column()` trên bảng có sẵn `quality_exception_queue` schema `curated`, cùng khuôn mẫu `0004_uc032_unmapped_queue_resolution.py`, `downgrade()` đầy đủ). Test: `tests/test_uc040_quality_exception_handling.py` (mới, 15 test case qua `TestClient`, SQLite in-memory, tái dùng helper dựng `MappingJob` COMPLETED + quy tắc `COMPLETENESS` + `POST /quality-checks` để sinh sẵn hàng đợi ngoại lệ giống `test_uc039_quality_check.py`) — bước 1: xem hàng đợi mặc định PENDING, lọc theo `dataset_id` không khớp trả rỗng, 404 khi lấy 1 ngoại lệ không tồn tại; bước 2: `FIX` công bố vào kho chuẩn hoá + phát đúng 1 sự kiện `curated.publish.requested` (kiểm tra qua `LoggingEventPublisher.published`), 422 khi `FIX` thiếu `corrected_fields`, `REJECT` không công bố + không phát sự kiện nào, 422 khi `REJECT` thiếu `reason`, `REQUEST_SOURCE` phát đúng 1 sự kiện `quality.exception.source_requested` với đúng payload, 422 khi xử lý lại dòng đã `RESOLVED`; bước 3: `REJECT` hàng loạt 3 dòng cùng `rule_type=COMPLETENESS` cùng lúc, `FIX` hàng loạt 2 dòng công bố đồng loạt + chỉ phát 1 sự kiện gộp `record_count=2`, 422 `NO_MATCHING_EXCEPTION_ITEMS_FOR_BATCH` khi `rule_type` không khớp dòng PENDING nào, xử lý hàng loạt dataset A không ảnh hưởng hàng đợi PENDING của dataset B. **CHƯA tự chạy được `pytest`**: sandbox lượt này KHÔNG có Internet (`pip install` báo `No matching distribution found` cho `fastapi`) nên không cài được `fastapi`/`sqlalchemy`/`pydantic`/`pytest`/`httpx`; đã bù bằng `python3 -m py_compile` toàn bộ file Python mới/sửa (pass, không lỗi cú pháp) + rà tay kỹ theo đúng pattern đã PASS 100% của UC-032 (`UnmappedQueueService`/`unmapped_queue_router.py`) và UC-039 (`QualityCheckService`) về cấu trúc entity/repository/service/router/migration. Frontend: `frontend/src/api/qualityExceptions.js` (mới, dùng chung `dataQualityClient`) — `listQualityExceptions`/`getQualityException`/`resolveQualityException`/`batchResolveQualityExceptions`; trang `/quality-exceptions` (`frontend/src/pages/dataquality/QualityExceptionsPage.jsx`, mới) — bộ lọc tập dữ liệu + trạng thái (bước 1), khối "Xử lý hàng loạt ngoại lệ cùng loại" (bước 3, chọn `rule_type` từ danh sách các loại quy tắc đang PENDING của dataset đã chọn + chọn quyết định FIX/REJECT/REQUEST_SOURCE), bảng hàng đợi với form xử lý từng dòng ngay trên dòng bảng (bước 2, editor thêm/bớt cặp `field_name=value` khi FIX); cập nhật `frontend/src/App.jsx` (thêm import `QualityExceptionsPage`, thẻ module ở trang chủ sau thẻ UC-039 dùng icon `ShieldAlert` đã có sẵn trong import, thêm route `/quality-exceptions`) + `frontend/src/nav.js` (thêm mục "Xử lý ngoại lệ chất lượng" vào nhóm "Chuẩn hóa và quản trị dữ liệu" ngay sau UC-039, dùng icon `ShieldAlert` đã import sẵn). **CHƯA tự chạy được `npm install`/`npm run build`** (cùng lý do không có Internet); đã bù bằng script Python đếm cân bằng ngoặc `()`/`{}`/`[]` cho `QualityExceptionsPage.jsx` (cân bằng, không lệch) + rà tay theo đúng cấu trúc `UnmappedQueuePage.jsx`/`QualityChecksPage.jsx`. Không cần sửa `requirements.txt`/`Dockerfile`/`docker-compose.yml`/`package.json` (không thêm thư viện mới). **⚠️ Còn tồn đọng — cần bạn xác nhận/tự chạy khi có Internet trước khi chuyển `tested`**: `pytest services/data-quality-service -q` (đặc biệt `tests/test_uc040_quality_exception_handling.py`, 15 test case mới, cùng lúc với toàn bộ suite để xác nhận không phá vỡ UC-032/UC-039 hiện có) và `cd frontend && npm install && npm run build`; sau đó `docker compose exec data-quality-service alembic upgrade head` để áp migration `0012` trên Postgres thật; nhắc lại tồn đọng CŨ (không phải do lượt này): 5 test fail có sẵn ở `tests/test_uc033_org_unit_catalog.py` vẫn CHƯA được rà soát.) |
| UC-041 | Công bố vào kho chuẩn hoá + batch_summary | Hệ thống tự động (Curated Service) | todo |
| UC-042 | Đăng ký siêu dữ liệu tập dữ liệu | Quản trị Dữ liệu | todo |
| UC-043 | Định nghĩa chỉ tiêu trong Lớp ngữ nghĩa | Quản trị Dữ liệu | done (backend đã có sẵn từ trước trong `data-quality-service` (`app/domain/entities.py::SemanticIndicator/SemanticIndicatorVersion/IndicatorTestRun/IndicatorAuditLog`, `app/application/use_cases/manage_semantic_indicator.py::SemanticIndicatorService`, router `app/interfaces/api/semantic_indicator_router.py` prefix `/semantic-indicators` đã `include_router`, test `tests/test_uc043_semantic_indicator.py`) nhưng frontend còn là file rỗng — lượt này bổ sung frontend: `frontend/src/api/semanticIndicators.js` (9 hàm gọi API, dùng chung `dataQualityClient` export từ `api/parsingJobs.js`, không cần proxy dev mới); trang `/semantic-indicators` (`frontend/src/pages/dataquality/SemanticIndicatorsPage.jsx` — cột trái: danh sách chỉ tiêu lọc theo lĩnh vực/trạng thái (tra cứu); cột phải: tab Bước 1 "Tạo chỉ tiêu mới" (tên/mô tả/biểu thức/lĩnh vực) / Bước 3 "Sửa — quản lý phiên bản" (tăng version + audit, có thể đổi trạng thái DRAFT/ACTIVE/INACTIVE); khi đã chọn 1 chỉ tiêu: khối Bước 2 "Kiểm thử trên truy vấn mẫu" (nhập tập bản ghi mẫu dạng JSON, chạy `POST /{id}/test`, hiển thị kết quả SUCCESS/FAILED + lịch sử kiểm thử) và khối lịch sử phiên bản + nhật ký audit); cập nhật `frontend/src/App.jsx` (import `SemanticIndicatorsPage` + icon `FlaskConical`, thêm thẻ module ở trang chủ sau thẻ UC-042, thêm route `/semantic-indicators`) + `frontend/src/nav.js` (import icon `FlaskConical`, thêm mục "Định nghĩa chỉ tiêu trong Lớp ngữ nghĩa" vào nhóm "Chuẩn hóa và quản trị dữ liệu" ngay sau UC-042). **Đã tự kiểm tra**: kiểm tra cân bằng ngoặc `()[]{}` cho `App.jsx`/`nav.js`/`api/semanticIndicators.js`/`SemanticIndicatorsPage.jsx` (pass); `node --check` cho `api/semanticIndicators.js` (pass, JS thuần); không sửa gì backend (giữ nguyên như đã có). **⚠️ Chưa tự chạy được trong sandbox này (mạng bị tắt — `npm install` báo `403 Forbidden` registry.npmjs.org)**: chưa chạy được `npm run build` để xác nhận biên dịch JSX (không có `node_modules`/`vite` cài sẵn), chưa chạy `pytest services/data-quality-service -q` để xác nhận lại `test_uc043_semantic_indicator.py` vẫn pass — cần bạn tự chạy 2 lệnh này để xác nhận trước khi chuyển `tested`; đồng thời PLAN.md trước đó ghi UC-043 là `todo` dù backend đã tồn tại sẵn — có thể do lượt làm backend trước chưa cập nhật bảng này, đề nghị bạn xác nhận lại UC-041/042 cũng đang ở trạng thái tương tự (route/thẻ module đã có trên `App.jsx` dù PLAN.md ghi `todo`) khi rà soát.) |
| UC-044 | Phê duyệt chỉ tiêu | Chủ quản Nghiệp vụ | todo |
| UC-045 | Truy vết nguồn gốc bản ghi | Kiểm toán viên | done (**sửa trạng thái**: bảng này trước đó ghi `todo` nhưng backend + frontend đã được code xong từ trước trong `data-quality-service` — có thể do lượt làm trước quên cập nhật PLAN.md, phát hiện lại khi rà soát để làm UC-046. Backend đã có sẵn: entity/use case `app/application/use_cases/trace_record_lineage.py` (`RecordLineageService`, đọc lại NGUYÊN VẸN qua khoá ngoại có sẵn xuyên suốt UC-029..041: `CuratedDmRecord.quality_check_job_id → QualityCheckJob.mapping_job_id → MappingJob.parsing_job_id → ParsingJob`, cùng `row_index` giữ nguyên xuyên suốt để dựng lại 5 bước RAW→PARSING→MAPPING→QUALITY→PUBLISH — use case CHỈ ĐỌC, không tạo bảng mới), router `app/interfaces/api/record_lineage_router.py` (prefix `/record-lineage`, đã `include_router`): `GET /curated-records/{id}` (bước 1 "Chọn bản ghi curated"), `GET /curated-records/{id}/chain` (bước 2 "Xem nguồn gốc dữ liệu qua các bước"), `GET /curated-records/{id}/steps/{step}` (bước 3 "Xem chi tiết từng bước" — dữ liệu vào/ra + phép biến đổi); test `tests/test_uc045_record_lineage.py` — đã tự chạy `pytest services/data-quality-service -q tests/test_uc045_record_lineage.py` **PASS 100%** (chạy cùng `test_uc046_provenance_report.py` cũng pass, không xung đột). Frontend đã có sẵn: `frontend/src/api/recordLineage.js`, trang `/record-lineage` (`frontend/src/pages/dataquality/RecordLineagePage.jsx` — chọn tập dữ liệu rồi chọn bản ghi curated, xem chuỗi 5 bước dạng badge, xem chi tiết từng bước dạng vào/ra JSON), route + thẻ module trang chủ (`App.jsx`) + mục nav "Truy vết nguồn gốc bản ghi" (`nav.js`). Đã tự chạy `npm install && npm run build` — biên dịch thành công. Không cần migration (use case chỉ đọc). **Còn tồn đọng chung với UC-046**: sandbox không có Docker/Postgres thật nên chưa kiểm thử qua UI với dữ liệu Postgres thật; 31 test fail có sẵn từ trước ở UC-033/038/039/040/041 (đã xác nhận không liên quan UC-045/046, xem ghi chú UC-046 bên dưới) vẫn cần được rà soát riêng ở lượt sau.) |
| UC-046 | Xuất báo cáo nguồn gốc dữ liệu | Kiểm toán viên | done (tiếp tục trong service `data-quality-service` (cùng service UC-029..045), TÁI SỬ DỤNG NGUYÊN VẸN `RecordLineageService` của UC-045 làm lõi truy vết — không viết lại logic dựng chuỗi 5 bước, chỉ bổ sung tầng "chọn phạm vi" để gộp nhiều bản ghi vào 1 báo cáo + kết xuất PDF. Luồng đúng theo yêu cầu: (1) Chọn phạm vi (tập dữ liệu / bản ghi / nguồn) → hệ thống hiển thị; (2) Sinh báo cáo nguồn gốc dữ liệu → hệ thống kết xuất PDF; (3) Kết xuất PDF → hệ thống trả file. Đây là use case CHỈ ĐỌC — không tạo bảng mới, KHÔNG CÓ migration Alembic. Domain (`app/domain/exceptions.py`): thêm `InvalidProvenanceReportScope` (422, `scope_type` không thuộc DATASET/RECORD/SOURCE hoặc `scope_value` không phải số nguyên hợp lệ), `ProvenanceReportScopeNotFound` (404, không có bản ghi nào khớp phạm vi). Application (`app/application/use_cases/export_data_provenance_report.py`, mới): `DataProvenanceReportService` nhận vào 1 `RecordLineageService` (composition) + `CuratedDmRecordRepository` + `ParsingJobRepository` — `_resolve_records()` (bước 1 "Chọn phạm vi"): `RECORD` → 1 `CuratedDmRecord` theo id (404 `CuratedDmRecordNotFound` nếu không có — TÁI DÙNG exception có sẵn của UC-045); `DATASET` → toàn bộ `CuratedDmRecord` của 1 `dataset_id` qua `CuratedDmRecordRepository.list_by_dataset()` có sẵn; `SOURCE` → gián tiếp qua `ParsingJob.data_source_id` (trường denormalized có sẵn từ UC-029, lưu kèm sự kiện `parsing.requested`) để suy ra tập `dataset_id` liên quan, rồi gộp `list_by_dataset()` của từng dataset (khử trùng theo id). `build_report()` (bước 2 "Sinh báo cáo"): với mỗi bản ghi khớp phạm vi (giới hạn `limit`, mặc định 50, tối đa 500 — cờ `truncated=true` nếu bị cắt bớt), gọi `RecordLineageService.get_chain()` để lấy chuỗi 5 bước (giống UC-045 bước 2); mặc định CHỈ lấy thêm chi tiết đầy đủ từng bước (`RecordLineageService.get_step_detail()`, giống UC-045 bước 3) khi phạm vi là `RECORD` (1 bản ghi duy nhất, tránh báo cáo `DATASET`/`SOURCE` quá nặng) — người dùng có thể chủ động bật `include_step_details=true` cho mọi phạm vi; tính thêm `fully_traced_count` (số bản ghi có đủ 5 bước `available=true`). Infrastructure (`app/infrastructure/provenance_report_generator.py`, mới): `ReportLabProvenanceReportGenerator` — sinh PDF bằng `reportlab` (Platypus), CÙNG khuôn mẫu `auth-identity-service/app/infrastructure/audit_report_generator.py` (UC-09): trang tiêu đề + thông tin phạm vi/thời điểm sinh/tổng số bản ghi, bảng tổng hợp trạng thái 5 bước theo từng bản ghi, và (nếu có `step_details`) phần chi tiết đầu vào/đầu ra/phép biến đổi từng bước cho từng bản ghi (escape ký tự đặc biệt tránh vỡ layout). Interface: `app/interfaces/api/schemas.py` thêm `ProvenanceReportRecordResponse`/`ProvenanceReportResponse` (tái dùng `LineageChainResponse`/`LineageStepDetailResponse` có sẵn của UC-045); `app/interfaces/api/provenance_report_router.py` (mới, prefix `/provenance-reports`): `GET "/preview"` (bước 1-2, trả JSON để xem trước trên giao diện trước khi kết xuất — 404/422 qua `_domain_error_to_http`), `GET "/export"` (bước 2-3, sinh báo cáo rồi trả thẳng file PDF tải xuống, `Content-Disposition: attachment`, không lưu lại trên server); `app/main.py` đã `include_router`. `requirements.txt` thêm `reportlab==4.2.2` (đã có sẵn dùng ở `auth-identity-service`, lần đầu dùng ở `data-quality-service`). KHÔNG có migration Alembic mới (use case chỉ đọc, không thêm bảng/cột). Test: `tests/test_uc046_provenance_report.py` (mới, 13 test case qua `TestClient`, SQLite in-memory, tái dùng helper `_build_full_chain()` theo đúng khuôn mẫu `test_uc045_record_lineage.py`) — bước 1: 422 khi `scope_type` không hợp lệ, 422 khi `scope_value` không phải số, 404 khi phạm vi RECORD/DATASET/SOURCE không khớp bản ghi nào; bước 1-2 phạm vi RECORD: xem trước trả đúng 1 bản ghi kèm `step_details` đầy đủ 5 bước (mặc định bật); phạm vi DATASET: trả đúng toàn bộ bản ghi của dataset, mặc định KHÔNG kèm `step_details`, có thể chủ động bật qua `include_step_details=true`, giới hạn đúng theo `limit` + cờ `truncated=true`; phạm vi SOURCE: gộp đúng các dataset cùng `data_source_id`, không lẫn dataset của nguồn khác; bước 2-3: kết xuất PDF trả đúng `content-type: application/pdf` + header `attachment` + nội dung bắt đầu bằng `%PDF` cho cả phạm vi RECORD và DATASET, 404 khi phạm vi không khớp thì KHÔNG trả PDF (trả lỗi JSON như thường lệ). **Đã tự cài `fastapi`/`sqlalchemy`/`pydantic`/`pytest`/`httpx`/`reportlab` qua `pip install -r requirements.txt --break-system-packages` (sandbox lượt này có Internet) và TỰ CHẠY**: `pytest tests/test_uc046_provenance_report.py -q` **13 passed**; `pytest tests/test_uc045_record_lineage.py tests/test_uc046_provenance_report.py -q` **24 passed** (không xung đột UC-045); `pytest -q` toàn bộ service **255 passed, 31 failed** — đã ĐỐI CHIẾU bằng cách chạy lại `pytest -q --ignore=tests/test_uc046_provenance_report.py` cho ra ĐÚNG 31 failed/242 passed (giống hệt danh sách fail, thuộc `test_uc033_org_unit_catalog.py`/`test_uc038_quality_rules.py`/`test_uc039_quality_check.py`/`test_uc040_quality_exception_handling.py`/`test_uc041_curated_publish.py`) → XÁC NHẬN 31 fail này là tồn đọng CÓ SẴN TỪ TRƯỚC UC-046, không phải do thay đổi lượt này, cần rà soát riêng ở lượt sau (ưu tiên trước UC-047+); `python3 -m py_compile` toàn bộ file Python trong service (pass). Frontend: `frontend/src/api/provenanceReports.js` (mới, dùng chung `dataQualityClient` export từ `api/parsingJobs.js`, không cần proxy dev mới) — `previewProvenanceReport()` (bước 1-2 xem trước), `exportProvenanceReportPdf()` (bước 2-3, gọi `responseType: "blob"` rồi tự tạo link tải xuống theo tên file server trả về qua header `Content-Disposition`); trang `/provenance-reports` (`frontend/src/pages/dataquality/ProvenanceReportPage.jsx`, mới, theo đúng cấu trúc `RecordLineagePage.jsx` UC-045) — form bước 1 chọn loại phạm vi (DATASET/RECORD/SOURCE) + nhập mã + giới hạn số bản ghi + tuỳ chọn kèm chi tiết từng bước (ẩn khi phạm vi RECORD vì mặc định luôn bật), 2 nút "Xem trước báo cáo" (bước 1-2, gọi `/preview`) và "Kết xuất PDF" (bước 2-3, gọi `/export` rồi tải file); bảng tổng hợp trạng thái 5 bước theo badge cho từng bản ghi trong báo cáo; khối chi tiết từng bước (đầu vào/đầu ra dạng JSON) khi có `step_details`; cập nhật `frontend/src/App.jsx` (thêm icon `FileOutput`, import `ProvenanceReportPage`, thêm thẻ module ở trang chủ ngay sau thẻ UC-045, thêm route `/provenance-reports`) + `frontend/src/nav.js` (thêm icon `FileOutput`, thêm mục "Xuất báo cáo nguồn gốc dữ liệu" vào nhóm "Chuẩn hóa và quản trị dữ liệu" ngay sau UC-045). **Đã tự chạy `npm install && npm run build`** — biên dịch thành công, không lỗi (cảnh báo chunk >500kB chỉ là cảnh báo hiệu năng chung của toàn app, không phải lỗi; đã dọn `node_modules`/`dist` sau khi build thử). Không cần sửa `Dockerfile`/`docker-compose.yml`/`package.json`. **⚠️ Còn tồn đọng — cần bạn xác nhận trước khi chuyển `tested`**: sandbox không có Docker/Postgres thật nên chưa chạy được `docker compose exec data-quality-service alembic upgrade head` để xác nhận `reportlab` mới thêm không ảnh hưởng gì tới các migration hiện có (thực ra không liên quan vì UC-046 không có migration, nhưng nên rebuild lại image do `requirements.txt` đổi); chưa tự kiểm thử qua UI thật (chỉ mới build thành công + chạy `pytest` qua API); đề nghị bạn tiện thể rà lại 31 test fail có sẵn nêu trên (UC-033/038/039/040/041) khi có dịp, trước khi làm UC-047+.) |

## Nhóm: KHAI THÁC: BẢNG ĐIỀU KHIỂN VÀ BÁO CÁO — service: `reporting-service`

| UC | Tên | Tác nhân | Status |
|---|---|---|---|
| UC-047 | Xem Bảng điều khiển điều hành | Lãnh đạo Sở Tài chính, Cán bộ tổng hợp Sở TC | done (UC ĐẦU TIÊN của service `reporting-service` — trước đó chỉ có khung Clean Architecture rỗng (`main.py` chỉ có `/health`). Luồng đúng theo yêu cầu: (1) Chọn Bảng điều khiển từ danh mục → hệ thống hiển thị danh sách; (2) Xem Bảng điều khiển → hệ thống hiển thị (nhúng iframe) từ Superset; (3) Ghim bảng điều khiển yêu thích → hệ thống lưu vào tùy chọn cá nhân. Domain (`app/domain/entities.py`): entity `Dashboard` (code/name/description/category ∈ {NGAN_SACH, TAI_SAN_CONG, DAU_TU_CONG, GIA, TONG_HOP}/superset_dashboard_uid/embed_url/is_active) và `DashboardFavorite` (user_id + dashboard_id — tùy chọn cá nhân, không ghim trùng); `app/domain/exceptions.py` thêm `DashboardCodeAlreadyExists`/`DashboardNotFound`/`DashboardInactive`/`InvalidDashboard`/`DashboardAlreadyPinned`/`DashboardFavoriteNotFound`; `app/domain/repositories.py` thêm interface `DashboardRepository`, `DashboardFavoriteRepository`. Application (`app/application/use_cases/manage_dashboard.py`, mới): `DashboardService` (`register` — nghiệp vụ hỗ trợ để danh mục có dữ liệu, không trùng `code`; `get` — bước 2 xem chi tiết; `list_catalog` — bước 1 xem danh mục, lọc `only_active`/`category`; `activate`/`deactivate`) và `DashboardFavoriteService` (`pin` — bước 3, kiểm tra dashboard tồn tại + đang active + chưa ghim trùng; `unpin`; `list_for_user` — danh sách đã ghim của 1 user). Infrastructure: `app/infrastructure/db/models.py` thêm `DashboardModel` (bảng `dashboards`, unique `code`) và `DashboardFavoriteModel` (bảng `dashboard_favorites`, FK `dashboard_id → dashboards.id`, unique `(user_id, dashboard_id)`); `app/infrastructure/db/repository_impl.py` thêm `SqlAlchemyDashboardRepository`, `SqlAlchemyDashboardFavoriteRepository`; đã sửa `app/infrastructure/db/session.py` thêm `StaticPool` cho SQLite in-memory (giống mẫu `ingestion-service`, cần thiết để `TestClient` dùng nhiều session mà vẫn thấy chung 1 DB). Interface: `app/interfaces/api/schemas.py` (mới: `DashboardCreate`/`DashboardResponse`/`DashboardFavoriteCreate`/`DashboardFavoriteResponse`/`ErrorResponse`); `app/interfaces/api/dashboard_router.py` (mới, prefix `/dashboards`): `POST \"\"` đăng ký dashboard vào danh mục, `GET \"\"` danh mục (lọc `only_active` mặc định true, `category`), `GET \"/favorites?user_id=\"` danh sách đã ghim, `GET \"/{id}\"` xem chi tiết (trả `embed_url` để frontend nhúng iframe), `POST \"/{id}/activate\"`/`POST \"/{id}/deactivate\"`, `POST \"/{id}/favorite\"` ghim (body `user_id`), `DELETE \"/{id}/favorite?user_id=\"` bỏ ghim; `app/main.py` đã `include_router(dashboard_router)`. Migration: `alembic.ini` + `alembic/env.py` (mới, theo đúng mẫu `ingestion-service`, `version_table=\"alembic_version_reporting\"` để tránh đụng `alembic_version` chung schema `public`) + `alembic/versions/0001_uc047_create_dashboards.py` (tạo schema `reporting`, bảng `dashboards`/`dashboard_favorites`, SEED sẵn 4 dashboard mẫu theo 4 lĩnh vực NGAN_SACH/TAI_SAN_CONG/DAU_TU_CONG/GIA vì BCKTKT chưa có UC riêng quản lý danh mục dashboard — có thể sửa qua API `POST /dashboards`); đã sửa `Dockerfile` thêm `COPY alembic.ini` + `COPY alembic` (tránh lỗi \"No config file\" từng gặp ở UC-015). Test: `tests/test_uc47_dashboard.py` (mới, 14 test case qua `TestClient`, SQLite in-memory): đăng ký + xem chi tiết, 422 khi `category` không hợp lệ, 409 khi trùng `code`, 404 khi xem dashboard không tồn tại, xem danh mục (bước 1) + lọc theo `category`, mặc định `only_active=true` ẩn dashboard đã vô hiệu hoá, ghim + bỏ ghim (bước 3) đúng theo `user_id`, 409 khi ghim trùng, 404 khi ghim dashboard không tồn tại, 409 khi ghim dashboard đã vô hiệu hoá, 404 khi bỏ ghim dashboard chưa ghim, kích hoạt lại dashboard. **Đã tự cài `pip install -r requirements.txt --break-system-packages` và TỰ CHẠY `pytest services/reporting-service -q` — 14 passed** (gồm cả `tests/test_health.py` có sẵn từ trước); `python3 -m py_compile` toàn bộ file Python thay đổi (pass); đã kiểm tra `alembic history` chỉ có 1 revision `0001` hợp lệ (base → 0001, head). Frontend: `frontend/src/api/dashboards.js` (mới, `reportingClient` baseURL `/api/reporting`) — `listDashboards`/`getDashboard`/`registerDashboard`/`listFavoriteDashboards`/`pinFavoriteDashboard`/`unpinFavoriteDashboard`; trang `/dashboards` (`frontend/src/pages/DashboardsPage.jsx`, mới — bước 1: bộ lọc lĩnh vực + lưới thẻ danh mục, tách riêng khối \"Đã ghim yêu thích\" ở trên, nút ghim/bỏ ghim trên từng thẻ dùng `user.id` từ `useAuth()`); trang `/dashboards/:id` (`frontend/src/pages/DashboardDetailPage.jsx`, mới — bước 2: nhúng `<iframe src={embed_url}>` từ Superset, nút ghim/bỏ ghim ở góc — bước 3); cập nhật `frontend/src/App.jsx` (import 2 trang mới, icon `LayoutDashboard`, 2 route `/dashboards` + `/dashboards/:id`, thêm thẻ module UC-047 vào trang chủ ngay sau UC-046); cập nhật `frontend/src/nav.js` (mục \"Bảng điều khiển\" ở nhóm \"Khai thác\" trước đó `disabled: true` trỏ `/dashboard` → nay BẬT, đổi tên \"Bảng điều khiển điều hành\", trỏ đúng `/dashboards`); cập nhật `frontend/vite.config.js` thêm proxy dev mới `/api/reporting` → `http://localhost:8004` (reporting-service TRƯỚC ĐÓ CHƯA CÓ proxy nào vì chưa có UC nào implement). **Đã tự chạy `npm install && npm run build` — biên dịch thành công** (cảnh báo chunk >500kB chỉ là cảnh báo hiệu năng chung, không phải lỗi). **⚠️ Còn tồn đọng — cần bạn xác nhận trước khi chuyển `tested`**: sandbox không có Docker/Postgres/Superset thật nên chưa chạy được `docker compose exec reporting-service alembic upgrade head` và chưa xác nhận `embed_url` nhúng được thật với 1 Superset instance thật (hiện seed bằng URL mẫu `http://localhost:8088/superset/dashboard/...` — cần đổi `SUPERSET_BASE_URL` thật khi tích hợp; Superset cần bật `ENABLE_EMBEDDED_SUPERSET`/cấu hình CSP `x-frame-options` cho phép nhúng iframe từ domain frontend); chưa test qua UI thật (chỉ mới build + `pytest` qua API); lưu ý sẵn có bug từ trước (không thuộc UC-047): `frontend/vite.config.js` thiếu proxy `/api/ingestion` dù `frontend/src/api/dataSources.js` và nhiều trang UC-015+ đang dùng `ingestionClient` baseURL `/api/ingestion` — không sửa trong lượt này vì ngoài phạm vi UC-047, nên rà soát riêng.) |
| UC-048 | Áp bộ lọc + xem chi tiết Bảng điều khiển | Lãnh đạo Sở Tài chính, Cán bộ tổng hợp Sở TC | done (tiếp tục trong service `reporting-service` (cùng service UC-047), TÁI SỬ DỤNG `Dashboard`/`DashboardRepository`/`SqlAlchemyDashboardRepository` có sẵn của UC-047, không viết lại. Luồng đúng theo yêu cầu: (1) Áp bộ lọc Bảng điều khiển (năm, đơn vị, lĩnh vực) → hệ thống truy vấn lại qua Superset; (2) Xem chi tiết KPI → hệ thống hiển thị phân rã chi tiết; (3) So sánh cùng kỳ năm trước → hệ thống truy thêm metric so sánh; (4) Yêu cầu AI giải thích KPI → hệ thống gọi AI Bộ điều phối. Domain (`app/domain/entities.py`): thêm `DashboardKpi` (danh mục KPI thuộc 1 dashboard: `dashboard_id`/`code` duy nhất trong phạm vi dashboard/`name`/`unit_of_measure`/`higher_is_better`/`is_active`), `DashboardFilter` (value object năm bắt buộc + đơn vị/lĩnh vực tuỳ chọn, validate năm 1900-2100), `KpiExplanation` (append-only, lưu 1 lượt "Yêu cầu AI giải thích KPI"); `app/domain/exceptions.py` thêm `DashboardKpiCodeAlreadyExists`/`DashboardKpiNotFound`/`InvalidDashboardKpi`/`InvalidDashboardFilter`/`SupersetQueryFailed`/`AIOrchestratorCallFailed`; `app/domain/repositories.py` thêm interface `DashboardKpiRepository`, `KpiExplanationRepository`, và 2 PORT quan trọng: `SupersetDashboardQueryClient` (bước 1-3, `query_kpi_values`/`query_kpi_breakdown`/`query_kpi_prior_year_value`) và `AIOrchestratorClient` (bước 4, `explain_kpi`). Application (`app/application/use_cases/apply_dashboard_filters.py`, mới): `DashboardKpiService` (đăng ký/liệt kê KPI — nghiệp vụ hỗ trợ để dashboard có KPI để lọc/xem/so sánh, theo mẫu `DashboardService.register` của UC-047); `DashboardFilterQueryService` (`apply_filters` bước 1 — truy vấn lại Superset trả giá trị mọi KPI active theo bộ lọc; `get_kpi_detail` bước 2 — giá trị + phân rã chi tiết 1 KPI; `compare_with_prior_year` bước 3 — truy vấn thêm với `year-1`, tính `delta`/`delta_percent`); `KpiExplanationService` (`request_explanation` bước 4 — dựng lại ngữ cảnh đầy đủ gồm giá trị hiện tại + phân rã + so sánh cùng kỳ TRƯỚC KHI gọi AI Bộ điều phối để lời giải thích bám đúng số liệu người dùng đang xem, rồi lưu append-only qua `KpiExplanationRepository`; `list_explanations` xem lại lịch sử). Infrastructure: `app/infrastructure/superset_query_client.py` (mới) — `NoOpSupersetDashboardQueryClient` sinh dữ liệu XÁC ĐỊNH (hash MD5 của dashboard/KPI/bộ lọc, KHÔNG gọi Superset thật) để chạy/test được ngay, có ghi chú rõ cần thay bằng client gọi Superset Chart Data API thật (`POST /api/v1/chart/data`) khi tích hợp — chỉ cần đổi factory ở router; `app/infrastructure/ai_orchestrator_client.py` (mới) — `HttpAIOrchestratorClient` gọi HTTP sang `ai-service` (`POST {AI_SERVICE_URL}/ai-orchestrator/kpi-explanations`, mặc định `http://hy-ai-service:8000` theo `container_name` trong docker-compose.yml) là "AI Bộ điều phối" thật; `NoOpAIOrchestratorClient` fallback khi biến môi trường `AI_SERVICE_URL` chưa cấu hình (dev/test); factory `get_ai_orchestrator_client()` chọn theo biến môi trường; `app/infrastructure/db/models.py` thêm `DashboardKpiModel` (bảng `dashboard_kpis`, FK `dashboard_id → dashboards.id`, unique `(dashboard_id, code)`) và `KpiExplanationModel` (bảng `kpi_explanations`, FK `dashboard_id`, append-only); `app/infrastructure/db/repository_impl.py` thêm `SqlAlchemyDashboardKpiRepository`, `SqlAlchemyKpiExplanationRepository`. Interface: `app/interfaces/api/schemas.py` thêm `DashboardKpiCreate`/`DashboardKpiResponse`/`DashboardFilterInput`/`ApplyFiltersResponse`/`KpiDetailResponse`/`KpiComparisonResponse`/`KpiExplanationRequest`/`KpiExplanationResponse`; `app/interfaces/api/kpi_query_router.py` (mới, cùng prefix `/dashboards`): `POST "/{id}/kpis"` đăng ký KPI, `GET "/{id}/kpis"` danh mục KPI (lọc `only_active`), `POST "/{id}/filters/apply"` bước 1, `GET "/{id}/kpis/{code}/detail?year=&org_unit_code=&sector="` bước 2, `GET "/{id}/kpis/{code}/comparison?year=..."` bước 3, `POST "/{id}/kpis/{code}/ai-explanation"` bước 4 (body `requested_by`/`year`/`org_unit_code`/`sector`), `GET "/{id}/kpis/{code}/ai-explanations"` xem lịch sử; `app/main.py` đã `include_router(kpi_query_router)`. Migration: `alembic/versions/0002_uc048_create_dashboard_kpis.py` (mới, nối tiếp `0001` của UC-047) — tạo bảng `dashboard_kpis`/`kpi_explanations` trong schema `reporting`, SEED sẵn 1-2 KPI mẫu cho mỗi trong 4 dashboard đã seed ở UC-047 (theo `code` dashboard, vd `THU_NGAN_SACH`/`CHI_NGAN_SACH` cho `DB-NGAN-SACH-TONG-HOP`) để UC-048 có dữ liệu dùng ngay — có thể sửa/thêm qua API `POST /dashboards/{id}/kpis`. Service `ai-service` (TRƯỚC ĐÓ CHỈ CÓ khung Clean Architecture rỗng, `main.py` chỉ có `/health`, CHƯA CÓ UC nào) — bổ sung điểm vào TỐI THIỂU của "AI Bộ điều phối" (KHÔNG triển khai đầy đủ UC-076 "AI giải thích KPI trên Bảng điều khiển", UC-076 vẫn `todo` và sẽ MỞ RỘNG implementation này sau — định tuyến mô hình UC-087, mẫu prompt UC-084..086, ghi AI Audit Log UC-010 — không viết lại endpoint): `app/domain/exceptions.py` thêm `InvalidKpiExplanationRequest`; `app/domain/repositories.py` thêm port `KpiExplanationGenerator`; `app/application/use_cases/generate_kpi_explanation.py` (mới) — `KpiExplanationOrchestratorService.explain_kpi()` xác thực tối thiểu (có `kpi_name`, có `current_value`) rồi giao cho `KpiExplanationGenerator`; `app/infrastructure/kpi_explanation_generator.py` (mới) — `RuleBasedKpiExplanationGenerator` sinh lời giải thích tiếng Việt bằng QUY TẮC suy luận trên số liệu (giá trị hiện tại, % tăng/giảm so cùng kỳ, thành phần đóng góp lớn nhất trong phân rã) — KHÔNG gọi LLM thật, đã ghi chú rõ chỗ cần thay khi UC-084..089 sẵn sàng; `app/interfaces/api/schemas.py` (mới) — `KpiExplanationContext`/`KpiExplanationResponse`; `app/interfaces/api/ai_orchestrator_router.py` (mới, prefix `/ai-orchestrator`): `POST "/kpi-explanations"`; `app/main.py` đã `include_router`. KHÔNG có migration Alembic ở `ai-service` (endpoint không trạng thái, không cần DB). Test: `services/reporting-service/tests/test_uc48_kpi_query.py` (mới, 18 test case qua `TestClient`, SQLite in-memory) — đăng ký/liệt kê KPI + 409 trùng mã + 404 dashboard không tồn tại; bước 1: áp bộ lọc trả đúng giá trị mọi KPI, KẾT QUẢ ỔN ĐỊNH (deterministic) giữa 2 lần gọi cùng bộ lọc, đổi `org_unit_code` thì giá trị đổi theo, 404 dashboard không tồn tại, 422 năm không hợp lệ; bước 2: xem chi tiết trả đúng giá trị + phân rã (đủ trường `label`/`value`), 404 KPI không tồn tại; bước 3: so sánh cùng kỳ trả đúng `current_year`/`prior_year=year-1`/`delta`/`delta_percent`, 404 dashboard không tồn tại; bước 4: yêu cầu AI giải thích trả `explanation`/`model` khác rỗng, gọi 2 lần thì lịch sử `GET .../ai-explanations` có đúng 2 bản ghi, 404 KPI/dashboard không tồn tại; `services/ai-service/tests/test_uc048_ai_orchestrator.py` (mới, 7 test case) — giải thích thành công có nhắc tên KPI + giá trị + xu hướng tăng/giảm + thành phần đóng góp lớn nhất, không nhắc "so cùng kỳ" khi thiếu `prior_value`, 422 khi thiếu `kpi_name`/`current_value`. **Đã tự cài `pip install -r requirements.txt --break-system-packages` (cả 2 service) và TỰ CHẠY**: `pytest services/reporting-service -q` **35 passed** (gồm 14 test UC-047 có sẵn + 3 test `test_health.py`/`test_uc47_dashboard.py`/`test_uc47_guest_token.py` + 18 test UC-048 mới); `pytest services/ai-service -q` **7 passed**; `python3 -m py_compile` toàn bộ file Python thay đổi ở cả 2 service (pass); đã kiểm tra `alembic history` của `reporting-service` — chuỗi `None → 0001 → 0002` hợp lệ, head đúng `0002`. Frontend: cập nhật `frontend/src/api/dashboards.js` (thêm `listDashboardKpis`/`registerDashboardKpi`/`applyDashboardFilters`/`getKpiDetail`/`getKpiComparison`/`requestKpiAiExplanation`/`listKpiAiExplanations`, TÁI DÙNG `reportingClient` có sẵn, không cần proxy dev mới); cập nhật `frontend/src/pages/DashboardDetailPage.jsx` (thêm khối "UC-048 — Áp bộ lọc & xem chi tiết KPI" ngay dưới khung nhúng Superset của UC-047: form bước 1 nhập năm/đơn vị/lĩnh vực + nút "Áp bộ lọc" → bảng giá trị từng KPI kèm nút "Xem chi tiết" cho từng dòng; bấm "Xem chi tiết" gọi đồng thời bước 2 (phân rã) + bước 3 (so sánh cùng kỳ, hiển thị ▲/▼ % kèm màu) + tải sẵn lịch sử giải thích AI; nút "Yêu cầu AI giải thích KPI" (bước 4) hiển thị kết quả + cập nhật lịch sử ngay). KHÔNG cần sửa `frontend/src/App.jsx`/`frontend/src/nav.js`/`frontend/vite.config.js` (UC-048 dùng lại đúng trang `/dashboards/:id` và proxy `/api/reporting` đã có từ UC-047, không thêm route/module mới). Cập nhật `docker-compose.yml`: thêm biến môi trường `AI_SERVICE_URL: http://hy-ai-service:8000` cho `reporting-service` + `depends_on.ai-service` (trước đó `reporting-service` không phụ thuộc `ai-service`). **Đã tự chạy `npm install && npm run build` — biên dịch thành công**, không lỗi (chỉ cảnh báo chunk >500kB, hiệu năng chung, không phải lỗi); đã kiểm tra `docker-compose.yml` hợp lệ bằng `python3 -c "import yaml; yaml.safe_load(...)"`. **⚠️ Còn tồn đọng — cần bạn xác nhận trước khi chuyển `tested`**: sandbox không có Docker/Postgres thật nên chưa chạy được `docker compose exec reporting-service alembic upgrade head` để xác nhận migration `0002` áp được lên DB thật nối tiếp `0001`, và chưa xác nhận thật sự gọi được `hy-ai-service` qua mạng Docker nội bộ (chỉ mới test qua `NoOpAIOrchestratorClient` do sandbox không set `AI_SERVICE_URL`); `NoOpSupersetDashboardQueryClient` hiện là dữ liệu GIẢ LẬP xác định (không phải Superset thật) — cần thay bằng client gọi Superset Chart Data API thật khi tích hợp, đã ghi chú rõ trong code chỗ cần đổi; UC-076 (AI giải thích KPI trên Bảng điều khiển) vẫn `todo`, `RuleBasedKpiExplanationGenerator` chỉ là bản tối giản theo quy tắc, CHƯA gọi LLM thật — cần thay khi UC-084..089 sẵn sàng.) |
| UC-049 | Chọn báo cáo theo mẫu + cấu hình bộ lọc | Cán bộ tổng hợp Sở Tài chính | todo |
| UC-050 | Sinh + kết xuất báo cáo | Cán bộ tổng hợp Sở Tài chính | done (tiếp tục trong service `reporting-service` (cùng service UC-047..049), TÁI SỬ DỤNG NGUYÊN VẸN `ReportTemplateRepository`/`ReportFilterConfigRepository` có sẵn của UC-049 làm đầu vào — không viết lại. Luồng đúng theo yêu cầu: (1) Sinh báo cáo theo mẫu + bộ lọc -> hệ thống truy vấn Lớp ngữ nghĩa + kết xuất; (2) Kết xuất PDF -> hệ thống trả file; (3) Kết xuất Excel -> hệ thống trả file. Domain (`app/domain/entities.py`): thêm `GeneratedReportLog` (nhật ký append-only mỗi lượt kết xuất PDF/Excel — lưu lại đúng bộ lọc đã dùng + số dòng kết xuất được, dùng để tra cứu lịch sử). `app/domain/exceptions.py` thêm `NoReportFilterConfigToGenerate` (422, không truyền bộ lọc trực tiếp và cũng chưa có cấu hình đã lưu ở UC-049), `SemanticLayerQueryFailed` (502, phòng lỗi hạ tầng truy vấn Lớp ngữ nghĩa). `app/domain/repositories.py` thêm port `SemanticLayerReportQueryClient` (bước 1: `query_report_rows(template, filters) -> List[Dict]`) và interface `GeneratedReportLogRepository`. Application (`app/application/use_cases/generate_and_export_report.py`, mới): `ReportGenerationService` — `generate()` (bước 1: lấy mẫu báo cáo đang active, dựng bộ lọc — nếu KHÔNG truyền `year`/`period_type` thì dùng lại cấu hình đã lưu ở UC-049 bước 3 qua `ReportFilterConfigRepository.get()`, ngược lại dựng bộ lọc mới từ tham số truyền vào (không ghi đè cấu hình đã lưu) — rồi gọi cổng `SemanticLayerReportQueryClient.query_report_rows()`); `generate_and_record()` (gọi `generate()` rồi ghi 1 `GeneratedReportLog`, dùng bởi endpoint export PDF/Excel — bước 2/3 "hệ thống trả file"); `list_logs_for_user()` (lịch sử kết xuất). Infrastructure: `app/infrastructure/semantic_layer_report_client.py` (mới) — `NoOpSemanticLayerReportQueryClient` sinh dữ liệu mẫu XÁC ĐỊNH (hash MD5 theo mã mẫu + bộ lọc + tên cột), số dòng phụ thuộc bộ lọc (có đơn vị cụ thể -> ít dòng hơn, mô phỏng đúng ngữ nghĩa "áp bộ lọc thu hẹp") — có ghi chú rõ cần thay bằng client gọi Lớp ngữ nghĩa thật (UC-043 `SemanticIndicatorService` ở `data-quality-service`) khi tích hợp, chỉ cần đổi factory; `app/infrastructure/report_pdf_generator.py` (mới) — `ReportLabReportPdfGenerator` sinh PDF bằng `reportlab` (Platypus, khổ ngang A4), cùng khuôn mẫu `data-quality-service/.../provenance_report_generator.py` (UC-046); `app/infrastructure/report_excel_generator.py` (mới) — `OpenpyxlReportExcelGenerator` sinh file `.xlsx` bằng `openpyxl` (tiêu đề + bộ lọc + bảng dữ liệu có auto-filter + freeze header, lần đầu dùng `openpyxl` để GHI Excel trong hệ thống — trước đó `data-quality-service` chỉ dùng để ĐỌC ở UC-029); `app/infrastructure/db/models.py` thêm `GeneratedReportLogModel` (bảng `generated_report_logs`, FK `template_id -> report_templates.id`); `app/infrastructure/db/repository_impl.py` thêm `SqlAlchemyGeneratedReportLogRepository`. Interface: `app/interfaces/api/schemas.py` thêm `ReportGenerationFilterInput`/`GeneratedReportFiltersResponse`/`GeneratedReportResponse`/`GeneratedReportLogResponse`; `app/interfaces/api/report_generation_router.py` (mới, prefix `/report-templates/{template_id}/reports`): `POST "/generate"` (bước 1, trả JSON xem trước — chưa xuất file), `GET "/export.pdf"` (bước 2, trả file PDF tải xuống + ghi log), `GET "/export.xlsx"` (bước 3, trả file Excel tải xuống + ghi log), `GET "/logs"` (lịch sử kết xuất của người dùng cho mẫu này); `app/main.py` đã `include_router(report_generation_router)`. `requirements.txt` thêm `reportlab==4.2.2`, `openpyxl==3.1.5` (lần đầu dùng ở service này). Migration: `alembic/versions/0004_uc050_create_generated_report_logs.py` (nối tiếp `0003` UC-049 -> `down_revision="0003"`, tạo bảng `generated_report_logs` schema `reporting` + 2 index, `downgrade()` đầy đủ) — đã kiểm tra `alembic history` chuỗi `0001->0002->0003->0004` hợp lệ, head đúng `0004`. Test: `tests/test_uc50_generate_export_report.py` (mới, 15 test case qua `TestClient`, SQLite in-memory) — bước 1: sinh báo cáo với bộ lọc truyền trực tiếp trả đúng `row_count`/cấu trúc cột, dùng lại cấu hình đã lưu ở UC-049 khi không truyền bộ lọc, 422 `NO_REPORT_FILTER_CONFIG_TO_GENERATE` khi không truyền gì và cũng chưa lưu cấu hình, 404 khi mẫu báo cáo không tồn tại, có `org_unit_code` thì số dòng ít hơn không lọc (đúng ngữ nghĩa "áp bộ lọc"); bước 2: xuất PDF trả đúng `content-type: application/pdf` + header `attachment` + nội dung bắt đầu `%PDF` + ghi đúng 1 dòng log `format=PDF`, 404 khi mẫu không tồn tại; bước 3: xuất Excel trả đúng `content-type` `.xlsx` (`application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`) + nội dung bắt đầu `PK` (magic bytes ZIP) + ghi đúng 1 dòng log `format=EXCEL`, 422 khi không có bộ lọc; xuất cả PDF lẫn Excel cho cùng mẫu thì lịch sử có đúng 2 dòng (2 định dạng khác nhau). **Đã tự cài `pip install -r requirements.txt --break-system-packages` và TỰ CHẠY `pytest services/reporting-service -q` — 66 passed** (gồm toàn bộ test UC-047/048/049 có sẵn + 15 test UC-050 mới; phát hiện + tự sửa 1 lỗi nhỏ lúc viết: `GeneratedReportFiltersResponse` thiếu `model_config = {"from_attributes": True}` khiến Pydantic không đọc được entity `ReportFilterConfig` dạng dataclass — đã fix ngay và test pass); `python3 -m py_compile` toàn bộ file Python thay đổi (pass); đã kiểm tra `alembic history` bằng `ScriptDirectory.walk_revisions()` — head đúng `0004 -> 0003 -> 0002 -> 0001`. Frontend: `frontend/src/api/reportTemplates.js` thêm `generateReport`/`exportReportPdf`/`exportReportExcel`/`listGeneratedReportLogs` (tái dùng `reportingClient` có sẵn từ UC-047, không cần proxy dev mới; hàm export dùng `responseType: "blob"` rồi tự tạo link tải xuống theo tên file server trả về qua header `Content-Disposition`, cùng pattern `api/provenanceReports.js` UC-046); cập nhật `frontend/src/pages/ReportTemplatesPage.jsx` (KHÔNG tạo trang/route mới — gắn thẳng vào trang `/report-templates` đã có ở UC-049, ngay dưới khối "Cấu hình bộ lọc": thêm card "Sinh + kết xuất báo cáo" gồm 3 nút Sinh báo cáo (xem trước)/Kết xuất PDF/Kết xuất Excel dùng đúng bộ lọc đang nhập ở khung trên (không cần lưu trước), bảng xem trước dữ liệu vừa sinh, và bảng lịch sử các lượt kết xuất của người dùng cho mẫu đang chọn) + cập nhật subtitle trang nhắc tới UC-050. KHÔNG cần sửa `App.jsx`/`nav.js`/`vite.config.js` (dùng lại đúng route/proxy đã có từ UC-049). **Đã tự chạy `npm install && npm run build` — biên dịch thành công**, không lỗi (chỉ cảnh báo chunk >500kB, hiệu năng chung, không phải lỗi; đã dọn `node_modules`/`dist` sau khi build thử). **⚠️ Còn tồn đọng — cần bạn xác nhận trước khi chuyển `tested`**: sandbox không có Docker/Postgres thật nên chưa chạy được `docker compose exec reporting-service alembic upgrade head` để áp migration `0004` trên Postgres thật; `NoOpSemanticLayerReportQueryClient` hiện là dữ liệu GIẢ LẬP xác định (không phải Lớp ngữ nghĩa thật) — cần thay bằng client gọi UC-043 `SemanticIndicatorService` thật khi tích hợp, đã ghi chú rõ trong code chỗ cần đổi; chưa tự kiểm thử qua UI thật (chỉ mới build thành công + chạy `pytest` qua API).) |
| UC-051 | Cấu hình báo cáo theo lịch | Cán bộ tổng hợp Sở Tài chính | tested (tiếp tục trong service `reporting-service` (cùng service UC-047..050). Luồng đúng theo yêu cầu: (1) Cấu hình lịch (hàng ngày/hàng tuần/hàng tháng) -> hệ thống lưu lịch; (2) Cấu hình người nhận (email) -> hệ thống lưu; (3) Hệ thống tự động sinh + gửi email báo cáo theo lịch -> tác vụ định kỳ (cron). Domain (`app/domain/entities.py`): thêm `ReportSchedule` (lịch báo cáo theo mẫu, `FREQUENCIES = (DAILY, WEEKLY, MONTHLY)`, `time_of_day` "HH:MM", `day_of_week`/`day_of_month` tuỳ tần suất, bộ lọc riêng tuỳ chọn — nếu không đặt thì dùng lại cấu hình đã lưu ở UC-049 giống UC-050, `is_active`, `last_run_at`), `ReportScheduleRecipient` (email người nhận, validate định dạng, không lặp lại trong cùng 1 lịch), `ReportScheduleRunLog` (nhật ký append-only mỗi lần cron chạy 1 lịch, `STATUSES = (SUCCESS, FAILED)`). `app/domain/exceptions.py` thêm lỗi tương ứng (lịch không tồn tại, người nhận trùng/không hợp lệ, chưa có người nhận khi bật lịch...). `app/domain/repositories.py` thêm `ReportScheduleRepository`/`ReportScheduleRecipientRepository`/`ReportScheduleRunLogRepository` và cổng `ReportEmailSender` (`send_report_email`). Application: `app/application/use_cases/manage_report_schedule.py` (`ReportScheduleService` — tạo/sửa/bật-tắt lịch, thêm/xoá người nhận, liệt kê lịch + lịch sử chạy của người dùng); `app/application/use_cases/run_scheduled_reports.py` (`ReportScheduleRunnerService.run_due()` — quét `list_active()`, xác định lịch nào tới hạn theo `frequency`/`time_of_day`/`day_of_week`/`day_of_month` so với giờ hiện tại UTC, với mỗi lịch tới hạn: gọi lại `ReportGenerationService` (tái dùng nguyên vẹn từ UC-050, không viết lại) để sinh dữ liệu + xuất PDF/Excel theo `format` của lịch, gửi email tới toàn bộ người nhận qua cổng `ReportEmailSender`, ghi 1 `ReportScheduleRunLog` (SUCCESS/FAILED) và cập nhật `last_run_at`). Infrastructure: `app/infrastructure/report_email_sender.py` (mới) — `SmtpReportEmailSender` gửi qua SMTP thật đọc từ biến môi trường `REPORT_SMTP_HOST/PORT/USERNAME/PASSWORD/USE_TLS/FROM`, `InMemoryReportEmailSender` dùng khi `REPORT_SMTP_HOST` trống (dev/test, chỉ ghi log không gửi thật) — factory `get_report_email_sender()` tự chọn theo cấu hình; `app/infrastructure/scheduler_runner.py` (mới) — dùng `APScheduler` (`BackgroundScheduler`) chạy job nội bộ quét mỗi `REPORT_SCHEDULER_POLL_SECONDS` (mặc định 60s) gọi `ReportScheduleRunnerService.run_due()`, chỉ khởi động khi biến môi trường `REPORT_SCHEDULER_ENABLED=true` (mặc định TẮT — tránh chạy nền ngoài ý muốn lúc `pytest`/dev SQLite), gắn vào `app/main.py` qua `startup`/`shutdown` event; `app/infrastructure/db/models.py` thêm `ReportScheduleModel`/`ReportScheduleRecipientModel`/`ReportScheduleRunLogModel`; `app/infrastructure/db/repository_impl.py` thêm `SqlAlchemyReportScheduleRepository`/`SqlAlchemyReportScheduleRecipientRepository`/`SqlAlchemyReportScheduleRunLogRepository` (theo đúng khuôn mẫu `SqlAlchemyGeneratedReportLogRepository` UC-050). Interface: `app/interfaces/api/schemas.py` thêm schema request/response cho lịch + người nhận + lịch sử chạy; `app/interfaces/api/report_schedule_router.py` (mới, prefix `/report-templates/{template_id}/schedules`) — CRUD lịch (`POST`/`GET`/`GET {id}`/`PUT {id}`), bật/tắt lịch, thêm/xoá người nhận, chạy thử 1 lịch ngay lập tức (không chờ cron, hữu ích để kiểm tra cấu hình), xem lịch sử chạy; `app/main.py` đã `include_router(report_schedule_router)` + gọi `start_background_scheduler()`/`stop_background_scheduler()` ở `startup`/`shutdown`. `requirements.txt` thêm `apscheduler==3.10.4`. Migration: `alembic/versions/0005_uc051_create_report_schedules.py` (nối tiếp `0004` UC-050 -> `down_revision="0004"`, tạo 3 bảng `report_schedules`/`report_schedule_recipients`/`report_schedule_run_logs` schema `reporting` + index + `UniqueConstraint(schedule_id, email)`, `downgrade()` đầy đủ) — chuỗi `alembic history` `0001->0002->0003->0004->0005` hợp lệ, head đúng `0005`. Test: `tests/test_uc51_report_schedule.py` (24 test case qua `TestClient`, SQLite in-memory, dùng `InMemoryReportEmailSender` để không gửi email thật) — tạo/sửa lịch hợp lệ theo từng tần suất (DAILY không cần `day_of_week`/`day_of_month`, WEEKLY bắt buộc `day_of_week` 0-6, MONTHLY bắt buộc `day_of_month` 1-31), lỗi validate khi thiếu tham số bắt buộc theo tần suất; thêm/xoá người nhận, lỗi email không hợp lệ + trùng lặp; `run_due()` chỉ chạy đúng lịch tới hạn (bỏ qua lịch `is_active=False` hoặc chưa tới giờ), chạy xong ghi đúng 1 `ReportScheduleRunLog` SUCCESS + cập nhật `last_run_at`, mô phỏng lỗi gửi email -> ghi log FAILED không crash tiến trình cron; endpoint chạy thử 1 lịch ngay lập tức trả đúng kết quả + ghi log; 404 khi lịch/mẫu không tồn tại. **Đã tự chạy `pytest services/reporting-service -q` — 90 passed** (gồm toàn bộ test UC-047..050 có sẵn + 24 test UC-051 mới; phát hiện + tự sửa: `app/infrastructure/db/repository_impl.py` lúc viết dở còn thiếu hẳn 3 class `SqlAlchemyReportScheduleRepository`/`SqlAlchemyReportScheduleRecipientRepository`/`SqlAlchemyReportScheduleRunLogRepository` dù domain/infrastructure khác + router + test đã viết xong, khiến `app/main.py` import lỗi và toàn bộ 7 file test của service không collect được — đã bổ sung đủ 3 class theo đúng khuôn mẫu `SqlAlchemyGeneratedReportLogRepository`, test pass lại bình thường); `python3 -m py_compile` toàn bộ file Python thay đổi (pass); đã kiểm tra `alembic history` head đúng `0005 -> 0004 -> ... -> 0001`. Frontend: `frontend/src/api/reportTemplates.js` thêm các hàm gọi API lịch báo cáo (tạo/sửa/bật-tắt lịch, thêm/xoá người nhận, chạy thử, xem lịch sử — tái dùng `reportingClient` có sẵn); cập nhật `frontend/src/pages/ReportTemplatesPage.jsx` (KHÔNG tạo trang/route mới — gắn vào trang `/report-templates` đã có, ngay dưới card "Sinh + kết xuất báo cáo" của UC-050: thêm card "Lịch báo cáo tự động" quản lý danh sách lịch + người nhận + lịch sử chạy cho mẫu đang chọn) + cập nhật subtitle trang nhắc tới UC-051. Hạ tầng: cập nhật `docker-compose.yml` (thêm biến môi trường `REPORT_SMTP_HOST/PORT/USERNAME/PASSWORD/USE_TLS/FROM` + `REPORT_SCHEDULER_ENABLED`/`REPORT_SCHEDULER_POLL_SECONDS` cho service `reporting-service`, đọc từ `.env` qua cú pháp `${VAR:-default}`, mặc định `REPORT_SCHEDULER_ENABLED=false` an toàn) và `.env.example` (thêm cùng nhóm biến, có ghi chú tiếng Việt giải thích). **⚠️ Còn tồn đọng — cần bạn xác nhận trước khi coi là hoàn tất trên môi trường thật**: sandbox không có Docker/Postgres/SMTP thật nên chưa chạy được `docker compose exec reporting-service alembic upgrade head` để áp migration `0005`, và chưa gửi thử 1 email qua SMTP server thật (chỉ mới test qua `InMemoryReportEmailSender` + `pytest`); cần đặt `REPORT_SMTP_*` thật + `REPORT_SCHEDULER_ENABLED=true` trong `.env` khi triển khai để cron thật sự chạy; chưa tự kiểm thử qua UI thật.) |
| UC-052 | Đăng ký nhận cảnh báo dashboard | Lãnh đạo Sở Tài chính, Cán bộ tổng hợp Sở TC | done (tiếp tục trong service `reporting-service` (cùng service UC-047..051), TÁI SỬ DỤNG NGUYÊN VẸN `Dashboard`/`DashboardRepository`, `DashboardKpi`/`DashboardKpiRepository`, và cổng `SupersetDashboardQueryClient` (+ `NoOpSupersetDashboardQueryClient`) có sẵn của UC-047/UC-048 — không viết lại. Luồng đúng theo yêu cầu: (1) Cấu hình ngưỡng cảnh báo trên KPI -> hệ thống lưu; (2) Chọn kênh nhận (email/Slack/Webhook) -> hệ thống lưu; (3) Khi vượt ngưỡng -> hệ thống gửi cảnh báo qua kênh đã chọn. Domain (`app/domain/entities.py`): thêm `DashboardAlertRule` (`OPERATORS=(">",">=","<","<=")`, ngưỡng gắn 1 KPI của 1 dashboard theo bộ lọc năm/đơn vị/lĩnh vực giống `DashboardFilter` UC-048, method `is_breached(kpi_value)` + `activate()`/`deactivate()`), `DashboardAlertChannel` (`CHANNEL_TYPES=(EMAIL,SLACK,WEBHOOK)`, validate `destination` — email đúng định dạng, Slack/Webhook phải là URL `http(s)://`), `DashboardAlertLog` (nhật ký append-only mỗi lượt gửi, `STATUSES=(SENT,FAILED)`). `app/domain/exceptions.py` thêm `DashboardAlertRuleNotFound`/`InvalidDashboardAlertRule`/`DashboardAlertChannelNotFound`/`InvalidDashboardAlertChannel`/`NoActiveDashboardAlertChannel` (422, ngưỡng vượt nhưng chưa có kênh nào đang bật)/`AlertDispatchFailed`. `app/domain/repositories.py` thêm interface `DashboardAlertRuleRepository`/`DashboardAlertChannelRepository`/`DashboardAlertLogRepository` + cổng (port) `AlertDispatcher` (`dispatch(channel_type, destination, subject, message)`). Application (`app/application/use_cases/manage_dashboard_alert.py`, mới): `DashboardAlertRuleService` (bước 1 `configure_rule` — 404 dashboard/KPI không tồn tại, 422 dữ liệu không hợp lệ; `update_rule`/`activate`/`deactivate`/`list_for_dashboard`/`list_for_user`); `DashboardAlertChannelService` (bước 2 `add_channel` — 404 ngưỡng không tồn tại, 422 kênh không hợp lệ; `activate`/`deactivate`/`delete_channel`/`list_for_rule`); `DashboardAlertEvaluationService` (bước 3 `evaluate_rule(rule_id)` — dựng lại `DashboardFilter` từ ngưỡng, gọi `SupersetDashboardQueryClient.query_kpi_values()` (TÁI DÙNG cổng UC-048) lấy giá trị KPI hiện tại, so với ngưỡng qua `rule.is_breached()`; nếu KHÔNG vượt trả `triggered=false` kèm lý do; nếu vượt lấy toàn bộ kênh đang bật của ngưỡng (422 `NO_ACTIVE_DASHBOARD_ALERT_CHANNEL` nếu rỗng), gửi cảnh báo qua từng kênh bằng cổng `AlertDispatcher`, ghi 1 `DashboardAlertLog` SENT/FAILED cho mỗi kênh (lỗi gửi 1 kênh không chặn các kênh còn lại); `evaluate_all_active()` quét toàn bộ ngưỡng đang bật, dùng bởi tác vụ định kỳ; `list_logs()` xem lại lịch sử). Infrastructure: `app/infrastructure/alert_dispatcher.py` (mới) — `CompositeAlertDispatcher` gửi thật: EMAIL qua SMTP (`smtplib`, đọc `ALERT_SMTP_*`, mặc định kế thừa `REPORT_SMTP_*` của UC-051 nếu `ALERT_SMTP_HOST` để trống), SLACK/WEBHOOK qua `requests.post()` JSON tới `destination`; `InMemoryAlertDispatcher` (mặc định, dev/test — không gọi mạng, chỉ ghi lại `sent_alerts` để test kiểm tra) + factory `get_alert_dispatcher()` chọn theo biến môi trường `ALERT_DISPATCH_ENABLED` (mặc định `false`, cùng tinh thần `REPORT_SCHEDULER_ENABLED`/`get_report_email_sender()` của UC-051 — tránh gọi mạng ngoài ý muốn lúc `pytest`/dev); `app/infrastructure/db/models.py` thêm `DashboardAlertRuleModel`/`DashboardAlertChannelModel`/`DashboardAlertLogModel`; `app/infrastructure/db/repository_impl.py` thêm `SqlAlchemyDashboardAlertRuleRepository`/`SqlAlchemyDashboardAlertChannelRepository`/`SqlAlchemyDashboardAlertLogRepository`. Interface: `app/interfaces/api/schemas.py` thêm `DashboardAlertRuleCreate`/`Update`/`Response`, `DashboardAlertChannelCreate`/`Response`, `DashboardAlertLogResponse`, `DashboardAlertEvaluationResponse`; `app/interfaces/api/dashboard_alert_router.py` (mới, router chính prefix `/dashboards`): `POST \"/{dashboard_id}/alert-rules\"` (bước 1), `GET \"/{dashboard_id}/alert-rules\"` (lọc `kpi_code`), `GET/PUT \"/{dashboard_id}/alert-rules/{rule_id}\"`, `POST .../activate`|`deactivate`, `POST \"/{dashboard_id}/alert-rules/{rule_id}/channels\"` (bước 2), `GET .../channels` (lọc `only_active`), `POST .../channels/{channel_id}/activate`|`deactivate`, `DELETE .../channels/{channel_id}`, `POST \"/{dashboard_id}/alert-rules/{rule_id}/evaluate\"` (bước 3, \"đánh giá ngay\" — dùng chung logic với tác vụ định kỳ), `GET .../logs` (lịch sử gửi); router phụ `user_router` prefix riêng `/dashboard-alerts` (tránh xung đột với route động `dashboard_id: int`, cùng lý do UC-046 dùng prefix riêng `/provenance-reports`): `GET \"/dashboard-alerts/rules?user_id=\"` xem ngưỡng theo người dùng xuyên nhiều dashboard; `app/main.py` đã `include_router(dashboard_alert_router)` + `include_router(dashboard_alert_user_router)`. Migration: `alembic/versions/0006_uc052_create_dashboard_alerts.py` (nối tiếp `0005` UC-051 -> `down_revision=\"0005\"`, tạo 3 bảng `dashboard_alert_rules`/`dashboard_alert_channels`/`dashboard_alert_logs` schema `reporting` + index + FK, `downgrade()` đầy đủ) — đã kiểm tra `alembic history` chuỗi `0001->...->0006` hợp lệ, head đúng `0006`. Test: `tests/test_uc52_dashboard_alert.py` (mới, 22 test case qua `TestClient`, SQLite in-memory, dùng `_inmemory_singleton` của `alert_dispatcher.py` để kiểm tra không gửi mạng thật) — bước 1: cấu hình ngưỡng lưu đúng, 404 dashboard/KPI không tồn tại, 422 toán tử không hợp lệ, lọc danh sách theo `kpi_code`, sửa ngưỡng, bật/tắt, 404 khi xem ngưỡng không tồn tại; bước 2: thêm kênh EMAIL/SLACK/WEBHOOK lưu đúng, 422 email/URL không hợp lệ, 404 khi thêm kênh cho ngưỡng không tồn tại, tắt kênh bị loại khỏi `only_active=true`, xoá kênh; bước 3: đánh giá VƯỢT ngưỡng gửi đúng tới toàn bộ kênh đang bật (dùng tính xác định của `NoOpSupersetDashboardQueryClient`, giá trị mô phỏng luôn trong khoảng [1000,1800] nên ngưỡng \"> 0\" LUÔN vượt, \"< 500\" KHÔNG BAO GIỜ vượt) + ghi đúng log SENT + lịch sử tra cứu lại được, KHÔNG vượt ngưỡng thì không gửi/không ghi log, 422 khi vượt ngưỡng nhưng chưa có kênh đang bật, kênh đã tắt bị bỏ qua khi đánh giá, ngưỡng đang tắt thì không đánh giá (`evaluated=false`), 404 khi đánh giá ngưỡng không tồn tại; liệt kê ngưỡng theo người dùng xuyên nhiều dashboard. **Đã tự cài `pip install -r requirements.txt --break-system-packages` và TỰ CHẠY `pytest services/reporting-service -q` — 112 passed** (gồm toàn bộ test UC-047..051 có sẵn (90) + 22 test UC-052 mới); `python3 -m py_compile` toàn bộ file Python thay đổi (pass). Frontend: `frontend/src/api/dashboards.js` thêm các hàm gọi API ngưỡng/kênh/đánh giá cảnh báo (`listDashboardAlertRules`, `configureDashboardAlertRule`, `updateDashboardAlertRule`, `activateDashboardAlertRule`, `deactivateDashboardAlertRule`, `listDashboardAlertChannels`, `addDashboardAlertChannel`, `activateDashboardAlertChannel`, `deactivateDashboardAlertChannel`, `deleteDashboardAlertChannel`, `evaluateDashboardAlertRule`, `listDashboardAlertLogs`, `listDashboardAlertRulesForUser` — tái dùng `reportingClient` có sẵn, không cần proxy dev mới); `frontend/src/components/DashboardAlertsPanel.jsx` (mới) — component tự chứa (form bước 1 cấu hình ngưỡng theo KPI đang có của dashboard, danh sách ngưỡng kèm bật/tắt + nút \"Đánh giá ngay\" (bước 3, hiển thị kết quả vượt/không vượt ngưỡng ngay tại chỗ), khối mở rộng mỗi ngưỡng quản lý kênh nhận (bước 2, thêm/bật/tắt/xoá kênh EMAIL/SLACK/WEBHOOK) + bảng lịch sử gửi cảnh báo); gắn vào `frontend/src/pages/DashboardDetailPage.jsx` (KHÔNG tạo trang/route mới — nhúng `<DashboardAlertsPanel dashboardId userId>` ngay dưới khối nhúng Superset của UC-047/khối UC-048, cùng cách UC-048/050/051 gắn thêm khối vào trang có sẵn thay vì tạo route mới). KHÔNG cần sửa `App.jsx`/`nav.js`/`vite.config.js` (dùng lại đúng route `/dashboards/:id` và proxy `/api/reporting` đã có từ UC-047). Hạ tầng: cập nhật `docker-compose.yml` (thêm biến môi trường `ALERT_DISPATCH_ENABLED`/`ALERT_SMTP_HOST/PORT/USERNAME/PASSWORD/USE_TLS/FROM`/`ALERT_WEBHOOK_TIMEOUT_SECONDS` cho `reporting-service`, đọc từ `.env` qua cú pháp `${VAR:-default}`, mặc định `ALERT_DISPATCH_ENABLED=false` an toàn) và `.env.example` (thêm cùng nhóm biến, có ghi chú tiếng Việt giải thích, kênh EMAIL kế thừa `REPORT_SMTP_*` nếu `ALERT_SMTP_HOST` để trống); đã kiểm tra `docker-compose.yml` hợp lệ bằng `python3 -c "import yaml; yaml.safe_load(...)"`. **Đã tự chạy `npm install && npm run build` — biên dịch thành công**, không lỗi (chỉ cảnh báo chunk >500kB, hiệu năng chung, không phải lỗi; đã dọn `node_modules`/`dist` sau khi build thử). **⚠️ Còn tồn đọng — cần bạn xác nhận trước khi chuyển `tested`**: sandbox không có Docker/Postgres/SMTP/Slack thật nên chưa chạy được `docker compose exec reporting-service alembic upgrade head` để áp migration `0006` trên Postgres thật, và chưa gửi thử 1 cảnh báo qua SMTP/Slack Incoming Webhook/Webhook thật (chỉ mới test qua `InMemoryAlertDispatcher` + `pytest`); cần đặt `ALERT_SMTP_*`/`ALERT_DISPATCH_ENABLED=true` trong `.env` khi triển khai để gửi cảnh báo thật; hiện UC-052 KHÔNG có tác vụ định kỳ (cron) tự động gọi `evaluate_all_active()` theo chu kỳ (khác UC-051 đã có `scheduler_runner.py`) — bước 3 hiện chạy qua API "đánh giá ngay" (`POST .../evaluate`), cần bổ sung 1 cron riêng (hoặc tái dùng `scheduler_runner.py` UC-051, thêm job mới) ở lượt sau nếu cần tự động hoàn toàn theo thời gian thực; chưa tự kiểm thử qua UI thật (chỉ mới build thành công + chạy `pytest` qua API).) |
| UC-053 | Tra cứu dữ liệu văn bản | Cán bộ chuyên môn ngành Tài chính (Sở/Phòng/xã) | done (tiếp tục trong service `reporting-service` (cùng service UC-047..052), UC ĐẦU TIÊN dùng tới OpenSearch (`docker-compose.yml` đã có sẵn service `opensearch` từ trước nhưng CHƯA service nào dùng tới). Luồng đúng theo yêu cầu: (1) Nhập từ khoá + bộ lọc (cơ quan, ngày, loại văn bản) -> Hệ thống truy vấn OpenSearch + lọc theo quyền; (2) Hiển thị kết quả thuộc phạm vi quyền -> Hệ thống hiển thị; (3) Xem chi tiết văn bản -> Hệ thống hiển thị metadata + file PDF. Đây là use case CHỈ ĐỌC dữ liệu đã lập chỉ mục — KHÔNG tạo bảng Postgres mới, KHÔNG CÓ migration Alembic. Domain (`app/domain/entities.py`): thêm `DocumentMetadata` (1 văn bản đã lập chỉ mục — cùng tên trường `so_ky_hieu`/`loai_van_ban`/`trich_yeu`/`ngay_ban_hanh`/`don_vi_ban_hanh`/`raw_object_key` với `VanBanIntake` của UC-024 `ingestion-service` để nhất quán nguồn gốc dữ liệu, thêm `sensitivity_level` ∈ PUBLIC/INTERNAL/CONFIDENTIAL/SECRET + `don_vi_ban_hanh_unit_id` dùng để "lọc theo quyền"), `DocumentSearchQuery` (bước 1: từ khoá + bộ lọc cơ quan/loại văn bản/khoảng ngày + phân trang, validate ngày YYYY-MM-DD và `ngay_from <= ngay_to`), `DocumentSearchResultItem`/`DocumentSearchPage` (kết quả bước 2), `DocumentAccessContext` (ánh xạ trực tiếp từ `UserPermissionContext` UC-04 `auth-identity-service`: `permitted_domains`/`permitted_unit_id`/`sensitivity_level`). `app/domain/exceptions.py` thêm `InvalidDocumentSearchQuery`/`InvalidDocumentMetadata`/`DocumentNotFound`/`DocumentAccessDenied`(403)/`DocumentSearchFailed`(502). `app/domain/repositories.py` thêm 2 PORT: `DocumentSearchClient` (`search`/`get_by_id`/`index_document`) và `DocumentAccessContextProvider` (`get_document_access_context`). Application (`app/application/use_cases/search_documents.py`, mới): `DocumentSearchService` — `search()` (bước 1-2: validate query -> lấy `DocumentAccessContext` -> nếu miền `VAN_BAN` không nằm trong `permitted_domains` thì trả trang RỖNG (đúng "hiển thị kết quả thuộc phạm vi quyền", không phải lỗi) -> gọi `DocumentSearchClient.search()` với `allowed_sensitivity_levels` (suy từ `sensitivity_level` tối đa của user, theo thứ tự PUBLIC<INTERNAL<CONFIDENTIAL<SECRET) + `permitted_unit_id` ĐÃ ÁP SẴN trong truy vấn); `get_detail()` (bước 3: lấy văn bản theo id, 404 nếu không có, kiểm tra lại quyền — 403 nếu miền/mức nhạy cảm/đơn vị không khớp, PHÒNG TRƯỜNG HỢP truy cập trực tiếp bằng id bỏ qua bước tìm kiếm); `get_file()` (bước 3: gọi `get_detail()` rồi đọc file qua `file_storage.download(raw_object_key)`). `DocumentIndexService` — hạ tầng hỗ trợ lập chỉ mục (KHÔNG phải bước nghiệp vụ của UC-053, actor "Cán bộ chuyên môn" chỉ tra cứu/xem — cùng tinh thần UC-047 seed dashboard mẫu), dùng để nạp dữ liệu văn bản (từ UC-024 tiếp nhận + UC-030 OCR, hiện CHƯA có pipeline tự động nối OpenSearch) vào chỉ mục phục vụ tra cứu ngay. Infrastructure: `app/infrastructure/document_search_client.py` (mới) — `InMemoryDocumentSearchClient` (singleton `_inmemory_singleton` trong tiến trình, cùng khuôn mẫu `InMemoryAlertDispatcher` UC-052 — dev/test không cần OpenSearch chạy thật, so khớp từ khoá KHÔNG PHÂN BIỆT DẤU/HOA-THƯỜNG tiếng Việt qua `unicodedata.normalize`) và `OpenSearchDocumentSearchClient` (dùng `opensearch-py` thật, tự tạo index nếu chưa có, đọc `OPENSEARCH_HOST`/`OPENSEARCH_PORT`/`OPENSEARCH_USERNAME`/`OPENSEARCH_PASSWORD`/`OPENSEARCH_USE_SSL`) + factory `get_document_search_client()` chọn theo biến môi trường `OPENSEARCH_HOST` (để trống -> in-memory, cùng tinh thần `get_alert_dispatcher()` UC-052). `app/infrastructure/document_file_storage.py` (mới) — đọc PDF từ CÙNG bucket MinIO `raw-documents` mà `ingestion-service` UC-024 đã lưu (giữ nguyên tên biến môi trường `MINIO_BUCKET_RAW_DOCUMENTS`/`VAN_BAN_INTAKE_LOCAL_DIR` để 2 service trỏ đúng 1 nơi): `LocalDiskDocumentFileStorage` (dev/test, đọc đĩa cục bộ) + `MinioDocumentFileStorage` (thật, dùng `minio` — lần đầu dùng package `minio` ở `reporting-service`) + factory `get_document_file_storage()`. `app/infrastructure/user_access_context.py` (đã có sẵn từ UC-047, sửa tại chỗ) thêm `NoOpDocumentAccessContextProvider` — dev/test: cho phép mọi miền dữ liệu, không giới hạn đơn vị, mức nhạy cảm tối đa CONFIDENTIAL (không lộ văn bản SECRET mặc định), có ghi chú rõ cần thay bằng implementation gọi `auth-identity-service` UC-04 khi tích hợp thật, chỉ cần đổi factory ở router. Interface: `app/interfaces/api/schemas.py` thêm `DocumentSearchItemResponse`/`DocumentSearchPageResponse`/`DocumentDetailResponse`/`DocumentIndexRequest`; `app/interfaces/api/document_search_router.py` (mới, prefix `/documents`): `GET ""` (bước 1-2, tham số `user_id`/`keyword`/`co_quan`/`loai_van_ban`/`ngay_from`/`ngay_to`/`page`/`page_size`), `GET "/{document_id}"` (bước 3 — metadata, 403/404), `GET "/{document_id}/file"` (bước 3 — trả thẳng file PDF qua `Response(media_type=\"application/pdf\")`, header `Content-Disposition: inline`, 403/404, 404 riêng `DOCUMENT_FILE_NOT_FOUND` nếu đã lập chỉ mục nhưng thiếu file trên đĩa/MinIO), `POST "/index"` (hạ tầng hỗ trợ lập chỉ mục, ghi rõ trong docstring KHÔNG phải bước nghiệp vụ của UC-053); `app/main.py` đã `include_router(document_search_router)`. `requirements.txt` thêm `opensearch-py==2.7.1`, `minio==7.2.9`. KHÔNG có migration Alembic mới (đúng bản chất UC-053 — dữ liệu nằm ở OpenSearch + file storage, không phải Postgres). Test: `tests/test_uc53_document_search.py` (mới, 17 test case qua `TestClient`, dùng `InMemoryDocumentSearchClient` singleton + `LocalDiskDocumentFileStorage` trỏ thư mục tạm — KHÔNG cần OpenSearch/MinIO/Postgres thật) — lập chỉ mục rồi tra cứu theo từ khoá đúng (khớp cả số ký hiệu/trích yếu/đơn vị ban hành), từ khoá không phân biệt dấu/hoa-thường tiếng Việt, lọc theo cơ quan (khớp một phần, không khớp trả rỗng), lọc theo loại văn bản, lọc theo khoảng ngày, 422 khi `ngay_from > ngay_to`, 422 khi `page_size` vượt giới hạn (Pydantic), phân trang đúng `total`/`page`/`page_size`, xem chi tiết đúng, 404 khi xem văn bản không tồn tại, xem file trả đúng `content-type: application/pdf` + nội dung bắt đầu `%PDF`, 404 `DOCUMENT_FILE_NOT_FOUND` khi đã lập chỉ mục nhưng thiếu file, văn bản mức `SECRET` bị ẩn khỏi cả tra cứu lẫn xem chi tiết với quyền mặc định (403 `DOCUMENT_ACCESS_DENIED`) trong khi mức `PUBLIC` vẫn xem được, 422 khi lập chỉ mục thiếu trường bắt buộc hoặc sai định dạng ngày. **Đã tự cài `pip install -r requirements.txt --break-system-packages` và TỰ CHẠY**: `pytest services/reporting-service/tests/test_uc53_document_search.py -q` **17 passed**; `pytest services/reporting-service -q` toàn bộ service **129 passed** (gồm toàn bộ test UC-047..052 có sẵn (112) + 17 test UC-053 mới, không xung đột); `python3 -m py_compile` toàn bộ file Python mới/sửa (pass). Frontend: `frontend/src/api/documents.js` (mới, tái dùng `reportingClient` export từ `api/dashboards.js`, không cần proxy dev mới vì `/api/reporting` đã có từ UC-047) — `searchDocuments`/`getDocumentDetail`/`getDocumentFileUrl` (dùng thẳng làm `src` cho `<iframe>`)/`fetchDocumentFileBlob`/`indexDocument`; trang `/documents` (`frontend/src/pages/DocumentSearchPage.jsx`, mới — bước 1: form từ khoá + bộ lọc cơ quan/loại văn bản/khoảng ngày; bước 2: bảng kết quả kèm phân trang, thông báo rõ khi không có văn bản nào "trong phạm vi quyền của bạn"); trang `/documents/:id` (`frontend/src/pages/DocumentDetailPage.jsx`, mới — bước 3: cột trái metadata dạng bảng, cột phải nhúng `<iframe src={getDocumentFileUrl(...)}>` xem trực tiếp PDF); cập nhật `frontend/src/App.jsx` (thêm icon `FileSearch`, import 2 trang mới, thêm thẻ module ở trang chủ ngay sau thẻ UC-049, thêm 2 route `/documents` + `/documents/:id`) + `frontend/src/nav.js` (thêm icon `FileSearch`, thêm mục "Tra cứu dữ liệu văn bản" vào nhóm "Khai thác" ngay sau UC-049). KHÔNG cần sửa `vite.config.js` (dùng lại đúng proxy `/api/reporting` đã có từ UC-047). Hạ tầng: cập nhật `docker-compose.yml` cho `reporting-service` — thêm biến môi trường `OPENSEARCH_HOST`/`OPENSEARCH_PORT`/`OPENSEARCH_USERNAME`/`OPENSEARCH_PASSWORD`/`OPENSEARCH_USE_SSL` (mặc định `OPENSEARCH_HOST` để trống -> dùng in-memory, an toàn cho dev) + `MINIO_ENDPOINT`/`MINIO_ROOT_USER`/`MINIO_ROOT_PASSWORD`/`MINIO_BUCKET_RAW_DOCUMENTS` (mặc định `MINIO_ENDPOINT` để trống -> dùng đĩa cục bộ) + `depends_on.opensearch`; đã kiểm tra `docker-compose.yml` hợp lệ bằng `python3 -c "import yaml; yaml.safe_load(...)"`. **Đã tự chạy `npm install && npm run build` — biên dịch thành công**, không lỗi (chỉ cảnh báo chunk >500kB, hiệu năng chung, không phải lỗi). **⚠️ Còn tồn đọng — cần bạn xác nhận trước khi chuyển `tested`**: sandbox không có Docker/OpenSearch/MinIO/Postgres thật nên chưa chạy thử `OpenSearchDocumentSearchClient`/`MinioDocumentFileStorage` với hạ tầng thật (chỉ mới test qua `InMemoryDocumentSearchClient`/`LocalDiskDocumentFileStorage`); hiện CHƯA có pipeline tự động nối UC-024 (tiếp nhận văn bản) + UC-030 (OCR) vào OpenSearch — dữ liệu tra cứu hiện phải nạp thủ công qua `POST /documents/index` (hạ tầng hỗ trợ), cần bổ sung consumer sự kiện (vd lắng nghe `van_ban.intake.completed`/kết quả OCR) ở lượt sau để tự động lập chỉ mục; `NoOpDocumentAccessContextProvider` hiện là stub cho phép rộng (mọi miền, mức CONFIDENTIAL) — cần thay bằng implementation gọi `auth-identity-service` UC-04 thật khi tích hợp; chưa tự kiểm thử qua UI thật (chỉ mới build thành công + chạy `pytest` qua API).) |
| UC-054 | Tra cứu dữ liệu tài sản | Cán bộ chuyên môn ngành Tài chính (Sở/Phòng/xã) | todo |
| UC-055 | Tra cứu dữ liệu giá | Cán bộ chuyên môn ngành Tài chính (Sở/Phòng/xã) | done (tiếp tục trong service `reporting-service` (cùng service UC-047..053), UC ĐẦU TIÊN của service này đọc trực tiếp 1 bảng Postgres THẬT nằm ngoài schema `reporting` — dữ liệu giá đúng tên nghiệp vụ `curated.dm_gia` (docs/use_cases.json id 55), cùng 1 instance Postgres với `data-quality-service` (ADR-001: mỗi service 1 schema, nhiều schema cùng 1 Postgres) nhưng service này CHƯA có bảng `dm_gia` theo đúng cấu trúc mặt hàng/địa bàn/kỳ/giá (chỉ có `dm_records` generic JSON từ UC-041) — nên `reporting-service` tự tạo + sở hữu bảng `curated.dm_gia` này (khác `dm_records` của `data-quality-service`), tương tự cách UC-047 tự seed danh mục dashboard khi chưa có UC quản lý riêng. Luồng đúng theo yêu cầu: (1-2) Nhập bộ lọc (mặt hàng, địa bàn, kỳ) -> Hệ thống truy vấn `curated.dm_gia` -> Hiển thị giá theo bảng; (3-4) Hiển thị biểu đồ xu hướng giá theo thời gian -> Hệ thống hiển thị line chart. Domain (`app/domain/entities.py`): thêm `PriceRecord` (1 dòng giá — `mat_hang_code`/`mat_hang_name`/`dia_ban_code`/`dia_ban_name`/`ky` định dạng `YYYY-MM` có validate/`gia`/`don_vi_tinh`/`nguon`), `PriceSearchQuery` (bước 1, validate `ky_from<=ky_to`, phân trang 1-200), `PriceSearchPage` (bước 2), `PriceTrendPoint`/`PriceTrend` (bước 3-4, giá trung bình theo từng kỳ). `app/domain/exceptions.py` thêm `InvalidPriceSearchQuery`/`InvalidPriceRecord`/`PriceDataQueryFailed`. `app/domain/repositories.py` thêm interface `PriceDataRepository` (`search`/`get_trend`/`add`). Application (`app/application/use_cases/query_price_data.py`, mới): `PriceDataQueryService` (`search` bước 1-2, `get_trend` bước 3-4 — validate lại `ky_from`/`ky_to` ở tầng service để trả đúng lỗi nghiệp vụ thay vì lỗi hạ tầng) và `PriceDataIndexService` (hạ tầng hỗ trợ nạp dữ liệu — KHÔNG phải bước nghiệp vụ của UC-055, dùng khi chưa có pipeline UC-041 tự động công bố dữ liệu giá thật vào `dm_gia`, cùng tinh thần UC-053 `DocumentIndexService`). Infrastructure: `app/infrastructure/db/models.py` thêm `DmGiaModel` (bảng `dm_gia`, schema `curated` khi chạy Postgres — `_SCHEMA_CURATED` riêng biệt với `_SCHEMA="reporting"` của các bảng UC-047..052, bỏ qua schema khi SQLite dev/test cùng pattern toàn service); `app/infrastructure/db/repository_impl.py` thêm `SqlAlchemyPriceDataRepository` (lọc mặt hàng/địa bàn theo `ILIKE` khớp cả mã lẫn tên, `get_trend` gộp nhóm theo `ky` tính trung bình + đếm số bản ghi, sắp xếp kỳ tăng dần cho line chart). Interface: `app/interfaces/api/schemas.py` thêm `PriceRecordResponse`/`PriceSearchPageResponse`/`PriceTrendPointResponse`/`PriceTrendResponse`/`PriceRecordIndexRequest`; `app/interfaces/api/price_data_router.py` (mới, prefix `/price-data`): `GET \"\"` (bước 1-2, lọc `mat_hang`/`dia_ban`/`ky_from`/`ky_to` + phân trang, 422 khi bộ lọc không hợp lệ), `GET \"/trend\"` (bước 3-4, cùng bộ lọc trừ phân trang), `POST \"/index\"` (hạ tầng hỗ trợ, 201, 422 khi dữ liệu không hợp lệ); `app/main.py` đã `include_router(price_data_router)`. Migration: `alembic/versions/0007_uc055_create_dm_gia.py` (nối tiếp `0006` UC-052 -> `down_revision=\"0006\"`, `CREATE SCHEMA IF NOT EXISTS curated` (an toàn nếu `data-quality-service` đã tạo trước) + tạo bảng `dm_gia` + 3 index (`mat_hang_code`/`dia_ban_code`/`ky`) + SEED 8 dòng mẫu (Gạo ST25 tại Hà Nội/TP.HCM qua các kỳ 2026-05..08) để UC-055 có dữ liệu tra cứu/xem biểu đồ ngay sau khi khởi tạo — có thể nạp thêm qua API `POST /price-data/index`, `downgrade()` đầy đủ). Test: `tests/test_uc55_price_data.py` (mới, 18 test case qua `TestClient`, SQLite in-memory) — nạp dữ liệu thành công, 422 khi `ky` sai định dạng/thiếu trường bắt buộc (Pydantic), 422 khi `gia` âm; bước 1-2: không lọc trả toàn bộ, lọc theo mặt hàng (khớp cả tên có dấu lẫn mã không dấu), lọc theo địa bàn, lọc theo khoảng kỳ, kết hợp nhiều bộ lọc cùng lúc, 422 khi `ky_from > ky_to`, 422 khi định dạng kỳ sai, phân trang đúng `page_size`; bước 3-4: nhóm đúng theo kỳ + tính đúng giá trung bình (đã kiểm tra cả trường hợp gộp nhiều địa bàn cùng kỳ), sắp xếp kỳ tăng dần, 422 khi khoảng kỳ không hợp lệ, trả mảng rỗng khi không có dữ liệu khớp. **Đã tự cài `pip install -r requirements.txt --break-system-packages` và TỰ CHẠY `pytest services/reporting-service -q` — 145 passed** (gồm toàn bộ test UC-047..053 có sẵn (127) + 18 test UC-055 mới, không xung đột); `python3 -m py_compile` toàn bộ file Python thay đổi (pass). Frontend: `frontend/src/api/priceData.js` (mới, dùng chung `reportingClient` export từ `api/dashboards.js`, không cần proxy dev mới vì `/api/reporting` đã có từ UC-047) — `searchPriceData`/`getPriceTrend`/`indexPriceRecord`; trang `/price-data` (`frontend/src/pages/PriceDataPage.jsx`, mới) — bước 1: form lọc mặt hàng/địa bàn/kỳ từ-đến (input `type=\"month\"`); bước 3-4: biểu đồ xu hướng giá dạng đường tự vẽ bằng SVG thuần (component `PriceTrendChart`, KHÔNG thêm thư viện chart mới — `package.json` hiện chưa có `recharts`/`chart.js`, giữ nguyên để không tăng kích thước bundle) hiển thị trục giá + nhãn kỳ + tooltip khi hover từng điểm; bước 2: bảng giá chi tiết kèm phân trang; cập nhật `frontend/src/App.jsx` (thêm icon `LineChart`, import `PriceDataPage`, thêm thẻ module ở trang chủ ngay sau thẻ UC-053, thêm route `/price-data`) + `frontend/src/nav.js` (thêm icon `LineChart`, thêm mục \"Tra cứu dữ liệu giá\" vào nhóm \"Khai thác\" ngay sau UC-053). KHÔNG cần sửa `vite.config.js`/`package.json`/`Dockerfile`/`docker-compose.yml` (dùng lại đúng proxy `/api/reporting`, không thêm thư viện mới). **Đã tự chạy `npm install && npm run build` — biên dịch thành công**, không lỗi (chỉ cảnh báo chunk >500kB, hiệu năng chung, không phải lỗi; đã dọn `node_modules`/`dist` sau khi build thử). **⚠️ Còn tồn đọng — cần bạn xác nhận trước khi chuyển `tested`**: sandbox không có Docker/Postgres thật nên chưa chạy được `docker compose exec reporting-service alembic upgrade head` để áp migration `0007` trên Postgres thật (lưu ý: câu lệnh `CREATE SCHEMA IF NOT EXISTS curated` cần chạy AN TOÀN dù `data-quality-service` đã tạo schema này trước hay chưa — đã dùng `IF NOT EXISTS`); dữ liệu `dm_gia` hiện chỉ có 8 dòng SEED mẫu — cần bổ sung pipeline thật (UC-041 mở rộng, hoặc UC-025 đồng bộ tăng dần từ hệ thống QL_GIA) để tự động nạp dữ liệu giá thật vào bảng này thay vì nạp thủ công qua `POST /price-data/index`; chưa tự kiểm thử qua UI thật (chỉ mới build thành công + chạy `pytest` qua API).) |
| UC-056 | Tra cứu dữ liệu ngân sách | Cán bộ chuyên môn ngành Tài chính (Sở/Phòng/xã) | done (tiếp tục trong service `reporting-service`, cùng tinh thần UC-055: đọc trực tiếp 1 bảng Postgres THẬT khác schema `reporting` — dữ liệu ngân sách đúng tên nghiệp vụ `curated.dm_ngan_sach` (docs/use_cases.json id 56), cùng 1 instance Postgres, `reporting-service` tự tạo + sở hữu bảng này (giống cách UC-055 sở hữu `dm_gia`). Luồng đúng theo yêu cầu: (1-3) Nhập bộ lọc (đơn vị, khoản mục, kỳ) -> Hệ thống truy vấn `curated.dm_ngan_sach` -> Hiển thị số liệu thu/chi/tạm ứng; (4-5) Xem chi tiết theo đơn vị/khoản mục -> Hệ thống re-query. Domain (`app/domain/entities.py`): thêm `NganSachRecord` (1 dòng ngân sách — `don_vi_code`/`don_vi_ten`/`khoan_muc_code`/`khoan_muc_ten`/`ky` định dạng năm `YYYY` có validate/`thu`/`chi`/`tam_ung` không âm/`don_vi_tinh`/`nguon`), `NganSachSearchQuery` (bước 1, validate `ky_from<=ky_to`, phân trang 1-200), `NganSachSearchPage` (bước 2-3), `NganSachDetailQuery`/`NganSachDetail` (bước 4-5, bắt buộc đúng 1 `don_vi_code`+1 `khoan_muc_code`, trả toàn bộ các kỳ + tổng hợp `tong_thu`/`tong_chi`/`tong_tam_ung`). `app/domain/exceptions.py` thêm `InvalidNganSachSearchQuery`/`InvalidNganSachRecord`/`InvalidNganSachDetailQuery`/`NganSachQueryFailed`. `app/domain/repositories.py` thêm interface `NganSachRepository` (`search`/`get_detail`/`add`). Application (`app/application/use_cases/query_ngan_sach.py`, mới): `NganSachQueryService` (`search` bước 1-3, `get_detail` bước 4-5 — validate lại bộ lọc ở tầng service để trả đúng lỗi nghiệp vụ) và `NganSachIndexService` (hạ tầng hỗ trợ nạp dữ liệu — KHÔNG phải bước nghiệp vụ của UC-056, dùng khi chưa có pipeline UC-041 tự động công bố dữ liệu ngân sách thật, cùng tinh thần `PriceDataIndexService` UC-055). Infrastructure: `app/infrastructure/db/models.py` thêm `DmNganSachModel` (bảng `dm_ngan_sach`, schema `curated` khi chạy Postgres, bỏ qua schema khi SQLite dev/test); `app/infrastructure/db/repository_impl.py` thêm `SqlAlchemyNganSachRepository` (`search` lọc đơn vị/khoản mục theo `ILIKE` khớp cả mã lẫn tên + khoảng kỳ, `get_detail` re-query đúng 1 đơn vị + 1 khoản mục, trả các kỳ sắp xếp tăng dần + tổng thu/chi/tạm ứng). Interface: `app/interfaces/api/schemas.py` thêm `NganSachRecordResponse`/`NganSachSearchPageResponse`/`NganSachDetailResponse`/`NganSachRecordIndexRequest`; `app/interfaces/api/ngan_sach_router.py` (mới, prefix `/ngan-sach`): `GET ""` (bước 1-3, lọc `don_vi`/`khoan_muc`/`ky_from`/`ky_to` + phân trang, 422 khi bộ lọc không hợp lệ), `GET "/detail"` (bước 4-5, bắt buộc `don_vi_code`+`khoan_muc_code`, re-query), `POST "/index"` (hạ tầng hỗ trợ, 201, 422 khi dữ liệu không hợp lệ); `app/main.py` đã `include_router(ngan_sach_router)`. Migration: `alembic/versions/0009_uc056_create_dm_ngan_sach.py` (nối tiếp `0008` UC-055 -> `down_revision="0008"`, `CREATE SCHEMA IF NOT EXISTS curated` (an toàn nếu đã có) + tạo bảng `dm_ngan_sach` + 3 index (`don_vi_code`/`khoan_muc_code`/`ky`) + SEED 12 dòng mẫu (2 đơn vị × 2 khoản mục × 3 kỳ 2024-2026) để UC-056 có dữ liệu tra cứu/xem chi tiết ngay sau khi khởi tạo — có thể nạp thêm qua API `POST /ngan-sach/index`, `downgrade()` đầy đủ). Test: `services/reporting-service/tests/test_uc56_ngan_sach.py` (mới, 15 test case qua `TestClient`, SQLite in-memory) — nạp dữ liệu thành công, 422 khi `ky` sai định dạng, 422 khi số âm; bước 1-3: không lọc trả toàn bộ, lọc theo đơn vị (khớp cả tên có dấu lẫn mã), lọc theo khoản mục, lọc theo khoảng kỳ, kết hợp nhiều bộ lọc, 422 khi `ky_from > ky_to`, 422 khi định dạng kỳ sai, phân trang đúng `page_size`; bước 4-5: trả đúng các kỳ sắp xếp tăng dần + tổng thu/chi/tạm ứng đúng, trả rỗng + tổng 0 khi không có dữ liệu khớp, 422 khi thiếu tham số bắt buộc. **Đã tự chạy `pytest services/reporting-service -q` — 160 passed, 11 failed** (11 lỗi thuộc `test_uc54_tai_san_search.py` — UC-054 đã ở trạng thái `todo`/dở dang từ trước khi bắt đầu UC-056, router `tai_san_router.py` CHƯA được đăng ký vào `app/main.py` và các lớp domain `TaiSan`/`TaiSanFilter`/`TaiSanSearchPage`/`TaiSanRepository`/exceptions liên quan CHƯA tồn tại trong `entities.py`/`repositories.py`/`exceptions.py` — hoàn toàn KHÔNG liên quan/KHÔNG bị ảnh hưởng bởi thay đổi của UC-056, không sửa trong phạm vi yêu cầu này); toàn bộ 15 test UC-056 mới PASS, không xung đột với 145 test UC-047..053/055 có sẵn; `python3 -m py_compile` toàn bộ file Python thay đổi (pass). Frontend: `frontend/src/api/nganSach.js` (mới, dùng chung `reportingClient`) — `searchNganSach`/`getNganSachDetail`/`indexNganSachRecord`; trang `/ngan-sach` (`frontend/src/pages/NganSachPage.jsx`, mới) — bước 1: form lọc đơn vị/khoản mục/kỳ từ-đến (input số năm); bước 2-3: bảng số liệu thu/chi/tạm ứng kèm phân trang, mỗi dòng có nút "Xem chi tiết"; bước 4-5: khi bấm "Xem chi tiết" gọi `GET /ngan-sach/detail` (re-query) hiển thị bảng chi tiết theo từng kỳ + tổng thu/chi/tạm ứng ngay trên cùng trang; cập nhật `frontend/src/App.jsx` (thêm icon `Wallet`, import `NganSachPage`, thêm thẻ module ở trang chủ ngay sau thẻ UC-055, thêm route `/ngan-sach`) + `frontend/src/nav.js` (thêm icon `Wallet`, thêm mục "Tra cứu dữ liệu ngân sách" vào nhóm "Khai thác" ngay sau UC-055). KHÔNG cần sửa `vite.config.js`/`package.json`/`Dockerfile`/`docker-compose.yml` (dùng lại đúng proxy `/api/reporting`, không thêm thư viện mới). **Đã tự chạy `npm install && npm run build` — biên dịch thành công**, không lỗi (chỉ cảnh báo chunk >500kB, hiệu năng chung, không phải lỗi mới; đã dọn `node_modules`/`dist` sau khi build thử). **⚠️ Còn tồn đọng — cần bạn xác nhận trước khi chuyển `tested`**: sandbox không có Docker/Postgres thật nên chưa chạy được `docker compose exec reporting-service alembic upgrade head` để áp migration `0009` trên Postgres thật (`CREATE SCHEMA IF NOT EXISTS curated` an toàn dù schema đã tồn tại hay chưa); dữ liệu `dm_ngan_sach` hiện chỉ có 12 dòng SEED mẫu — cần bổ sung pipeline thật (UC-041 mở rộng, hoặc đồng bộ từ hệ thống TABMIS) để tự động nạp số liệu ngân sách thật vào bảng này thay vì nạp thủ công qua `POST /ngan-sach/index`; chưa tự kiểm thử qua UI thật (chỉ mới build thành công + chạy `pytest` qua API).) |
| UC-057 | Hiển thị độ mới dữ liệu | Tất cả người dùng | todo |

## Nhóm: API VÀ TÍCH HỢP — service: `api-gateway-service`

| UC | Tên | Tác nhân | Status |
|---|---|---|---|
| UC-058 | Quản lý danh mục API | Quản trị API | todo |
| UC-059 | Quản lý API key | Quản trị API | todo |
| UC-060 | Quản lý giới hạn tần suất + gói dịch vụ | Quản trị API | todo |
| UC-061 | Theo dõi mức sử dụng API + chỉ số | Quản trị API | todo |
| UC-062 | Quản lý chứng thư / mTLS cho đơn vị khai thác | Quản trị API | todo |
| UC-063 | Cung cấp cổng tài liệu API | QLVBĐH, IOC, LGSP (đơn vị khai thác) | todo |
| UC-064 | Cung cấp Data API cho IOC | IOC (đơn vị khai thác) | todo |
| UC-065 | Cung cấp API qua LGSP | LGSP (đơn vị khai thác) | todo |
| UC-066 | Cung cấp Search API cho QLVBĐH/cổng nội bộ | QLVBĐH, portal nội bộ | todo |
| UC-067 | Cung cấp QA API có dẫn nguồn | QLVBĐH, portal nội bộ | todo |
| UC-068 | Cung cấp API siêu dữ liệu / tài liệu liên quan | QLVBĐH, portal nội bộ | todo |

## Nhóm: AI VÀ KHAI THÁC VĂN BẢN — service: `ai-service`

| UC | Tên | Tác nhân | Status |
|---|---|---|---|
| UC-069 | Tra cứu ngữ nghĩa văn bản | Cán bộ chuyên môn ngành Tài chính (Sở/Phòng/xã) | todo |
| UC-070 | Xem dẫn nguồn chi tiết + ngữ cảnh trích dẫn | Cán bộ chuyên môn ngành Tài chính (Sở/Phòng/xã) | todo |
| UC-071 | AI hỏi đáp văn bản có dẫn nguồn (RAG) | Lãnh đạo Sở Tài chính, Cán bộ nghiệp vụ Sở TC | todo |
| UC-072 | AI hỏi đáp số liệu (NLQ) | Cán bộ tổng hợp Sở TC, Lãnh đạo Sở Tài chính | todo |
| UC-073 | AI hỏi đáp đa nguồn (hybrid) | Cán bộ tổng hợp Sở TC, Lãnh đạo Sở Tài chính | todo |
| UC-074 | Xem lịch sử phiên AI của cá nhân | Tất cả người dùng | todo |
| UC-075 | AI sinh báo cáo liên ngành | Cán bộ tổng hợp Sở Tài chính | todo |
| UC-076 | AI giải thích KPI trên Bảng điều khiển | Lãnh đạo Sở Tài chính | todo |
| UC-077 | Rà soát hiệu lực văn bản | Cán bộ pháp chế Sở Tài chính | todo |
| UC-078 | Xem quan hệ giữa các văn bản | Cán bộ pháp chế Sở Tài chính | todo |
| UC-079 | Tóm tắt văn bản | Lãnh đạo Sở Tài chính, Cán bộ nghiệp vụ Sở TC | todo |
| UC-080 | So sánh 2 văn bản | Cán bộ pháp chế Sở Tài chính | todo |
| UC-081 | Người dùng đánh giá câu trả lời AI | Tất cả người dùng | todo |
| UC-082 | AI Rà soát phân loại phản hồi | AI Rà soát | todo |
| UC-083 | Kiểm duyệt đầu ra AI hàng tuần | AI Rà soát | todo |
| UC-084 | Thêm/Sửa/Xoá mẫu prompt | Quản trị AI | todo |
| UC-085 | Kiểm thử mẫu prompt | Quản trị AI | todo |
| UC-086 | Kích hoạt / Khôi phục mẫu prompt | Quản trị AI | todo |
| UC-087 | Quản lý định tuyến mô hình | Quản trị AI | todo |
| UC-088 | Lập chỉ mục tự động vào Vector Store | Hệ thống tự động (RAG Quy trình xử lý) | todo |
| UC-089 | Lập chỉ mục lại thủ công Vector Store | Quản trị AI | todo |

## Nhóm: VẬN HÀNH HỆ THỐNG — service: `ops-service`

| UC | Tên | Tác nhân | Status |
|---|---|---|---|
| UC-090 | Sao lưu thủ công (theo yêu cầu) | Quản trị hệ thống | todo |
| UC-091 | Cấu hình lịch sao lưu tự động | Quản trị hệ thống | todo |
| UC-092 | Phục hồi từ bản sao lưu | Quản trị hệ thống | todo |
| UC-093 | Xem trạng thái và lịch sử sao lưu | Quản trị hệ thống | todo |
| UC-094 | Phục hồi sự cố (chuyển sang Trung tâm dữ liệu) | Quản trị hệ thống | todo |
| UC-095 | Xem bảng điều khiển giám sát sức khoẻ hệ thống | Quản trị hệ thống, IT vận hành | todo |
| UC-096 | Cấu hình và nhận cảnh báo | Quản trị hệ thống | todo |
| UC-097 | Xem nhật ký tập trung | Quản trị hệ thống, Developer | todo |
| UC-098 | Truy vết phân tán | Quản trị hệ thống, Developer | todo |
| UC-099 | Theo dõi chi phí AI | Quản trị AI, Tài chính | todo |
| UC-100 | Quản lý tài nguyên cụm + co giãn | Quản trị hệ thống | todo |

## Nhóm: BÁO CÁO ĐỊNH KỲ VÀ ĐỐI SOÁT VỚI CẤP TRÊN (ĐẶC THÙ NGÀNH TÀI CHÍNH) — service: `gov-report-service`

| UC | Tên | Tác nhân | Status |
|---|---|---|---|
| UC-101 | Cấu hình mẫu báo cáo cấp trên theo Thông tư | Quản trị Danh mục, Cán bộ tổng hợp Sở Tài chính | todo |
| UC-102 | Quy trình phê duyệt số liệu báo cáo trước khi gửi cấp trên | Cán bộ tổng hợp Sở TC, Trưởng phòng nghiệp vụ, Phó GĐ Sở, Giám đốc Sở | todo |
| UC-103 | Sinh + gửi báo cáo ngân sách lên Bộ Tài chính / KBNN | Cán bộ tổng hợp Sở Tài chính, Hệ thống tự động | todo |
| UC-104 | Sinh + gửi báo cáo tài sản công / mua sắm tập trung lên cấp trên | Cán bộ tổng hợp Sở Tài chính, Hệ thống tự động | todo |
| UC-105 | Đối soát số liệu thu/chi với Kho bạc Nhà nước (KBNN) tỉnh | Cán bộ Phòng Ngân sách Sở Tài chính | todo |