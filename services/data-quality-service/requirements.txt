fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.2
pydantic==2.9.2
python-dotenv==1.0.1
openpyxl==3.1.5
minio==7.2.9
pika==1.3.2
reportlab==4.2.2

pytest==8.3.3
httpx==0.27.2

# UC-030 (Phân tích PDF/bản quét + OCR) — bộ máy OCR THẬT, KHÔNG cài mặc
# định (rất nặng, cần model deep-learning + khuyến nghị GPU). Mặc định
# dùng NoOpOcrEngine (xem app/infrastructure/ocr_engine.py) cho dev/test.
# Cài thêm khi triển khai thật (đặt OCR_USE_REAL_ENGINE=true):
#   pip install paddlepaddle paddleocr pymupdf   # engine PADDLEOCR
#   pip install olmocr                            # engine OLMOCR